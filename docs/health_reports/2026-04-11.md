# Daily Health Check — 2026-04-11

## Test Results
- **Run 1**: 776 passed, 2 failed, 10 ignored (175.8s)
- **Run 2**: 777 passed, 1 failed, 10 ignored (175.8s)
- Failures are flaky (parallel /tmp/ race): `save_load_round_trip`, `event_system_serialization`

### Failing tests
- `game::tests::save_load_round_trip` (tests.rs:213) — hardcoded `/tmp/test_savegame.json`, races under parallel run
- `game::tests::event_system_serialization` (tests.rs:703) — hardcoded `/tmp/test_events_save.json`, same pattern; **not yet listed in features.json known_issues**

### Test count accuracy
- All within tolerance (validate_features OK).

---

## Design Drift

### ARCHITECTURE.md is significantly stale (second consecutive day — no action taken)
Still lists pre-split sizes: `simulation.rs` at 4844 lines (deleted; split into `src/simulation/`), `game/mod.rs` at 6073 lines (actual: 2592), `game/render.rs` at 4094 lines (deleted; split into `src/game/render/`), `ecs/mod.rs` at 7163 lines (actual: 17). Priorities 1–4 from the refactor plan are done. This doc will mislead future agents.

### VillagerMemory never read in AI (Pillar 2, game_design.md)
Unchanged: written in `systems.rs`, never consulted in `ai.rs` (15 global-stockpile reads persist). Phase 2 knowledge architecture not started.

### Fog-of-war trivially 100% (Pillar 1/2)
`exploration_pct = 100.0` on all 3 seeds. Genuine discovery not functioning.

### No production chain buildings via auto-build
Only Farm/Hut/Workshop/Garrison/Stockpile appear across all 3 seeds. Granary, Bakery, Smithy never auto-built. Production chains (grain→bread, stone→masonry) not reachable in evaluation window — caps Settlement Viability score.

### Default seed dies at tick 6668 (with auto-build on)
Playtest with no explicit seed: GAME OVER, population peaked at 4, survived to Y1 Spring D6.

---

## Architecture

### Module sizes (no non-test file over 3000 lines — check passes)
| File | Lines | Status |
|------|-------|--------|
| `src/game/mod.rs` | 2592 | OK (was 6073) |
| `src/tilemap.rs` | 2515 | Getting big (known) |
| `src/game/build.rs` | 2306 | Getting big |
| `src/simulation/moisture.rs` | 1867 | Largest sim module |
| `src/game/render/mod.rs` | 1300 | OK post-split |
| `src/simulation/wind.rs` | 1540 | OK |

### Compiler warnings
11 warnings (release build): unused `wrapping_idx`, `map_terrain_glyph`, `draw_map_building_marker`, `sunset_hour`, `run_sediment_steps`, `DIRECTIONS`; 2 removable `mut`. Unchanged from yesterday.

### Refactor splits verified intact
- `src/simulation.rs` → does NOT exist ✓ (split to `src/simulation/`)
- `src/game/render.rs` → does NOT exist ✓ (split to `src/game/render/`)

---

## Game Quality Evaluation

### Per-Seed Diagnostics (all at tick 6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Events | Survived |
|------|-----------|------|----------|-------------|--------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 13     | WolfSurge | Yes |
| 137  | 11        | 15   | 1.4      | 16          | 17.9%  | 14     | none   | Yes      |
| 777  | 13        | 18   | 1.4      | 16          | 37.9%  | 14     | none   | Yes      |

Slope distribution: 98.4–98.7% flat, ≤1.6% gentle, 0% steep/cliff. elevation_std = 0.09–0.11 (persistently flat terrain).

### Report Card Summaries
- **Seed 42**: Population: Fragile | Food: Tight (no buffer) | Housing: Overcrowded | Terrain: Diverse | Threats: Low
- **Seed 137**: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight | Terrain: Diverse | Threats: Growing
- **Seed 777**: Population: Healthy (growing) | Food: Tight (no buffer) | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Trend vs 2026-04-10
- Seed 137 food improved: 3 → 15 (possible simulation non-determinism — two runs of same seed differ)
- Seed 777 population improved: 11 → 13
- Seed 42 unchanged: pop=4, food=12 (persistent fragile settlement)
- WolfSurge now active on seed 42 (threat_score 10.68); seeds 137/777 no events

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13–14 biomes (excellent), water 17.9–37.9% (good range). Elevation std 0.09–0.11, 98.5%+ flat tiles — persistent critical issue. |
| Settlement Viability (30%) | 2 | All 3 seeds survive. Food Critical on 2/3 seeds (1.4/cap). Seed 42 pop=4, housing full, growth=0. Production chains never reached. |
| Simulation Health (20%) | 4 | pipe_water 245–305 (all >0), wind_moisture 16K–41K, avg_vegetation 0.41–0.44. All systems active. |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4/11/13, Desert 32%/49%/0.1%). WolfSurge fires on seed 42. No events 137/777. Building variety limited (5 types only). |
| **Weighted Average** | **2.9** | 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 2.9. Unchanged from 2026-04-10. |

### Baseline Check
```
Running seed 137 for 12000 ticks...  PASS: pop=9, water=17.9%, biomes=14
Running seed 42 for 12000 ticks...   PASS: pop=4, water=30.7%, biomes=14
Running seed 777 for 12000 ticks...  PASS: pop=13, water=38.1%, biomes=14
Results: 3 passed, 0 failed
```

---

## Harness Updates

### New checks added to `validate_features.sh`

**Check #11**: Verify that `src/simulation.rs` does NOT exist (split to `src/simulation/` is done — guard against accidental re-creation).

**Check #12**: Verify that `src/game/render.rs` does NOT exist (split to `src/game/render/` is done — same guard).

These are fast file-system checks that prevent architectural regressions without requiring a build.

---

## features.json Updates
- `game_loop.known_issues`: add `event_system_serialization` (tests.rs:703) as flaky — hardcoded `/tmp/test_events_save.json`, races with parallel tests
- `game_loop.last_verified`: 2026-04-11
- `terrain_generation.last_verified`: 2026-04-11
- `simulation_wind.last_verified`: 2026-04-11
- `events.last_verified`: 2026-04-11

---

## Priority Actions

1. **Fix `/tmp/` flaky tests** [game_loop]: Both `save_load_round_trip` (tests.rs:213) and `event_system_serialization` (tests.rs:703) use hardcoded `/tmp/` paths. Replace with `tempfile::NamedTempFile` or `format!("/tmp/test_{}.json", std::process::id())`. This is the only source of test failures and is straightforward to fix.

2. **Update ARCHITECTURE.md** [architecture]: Still describes pre-refactor structure. Correct `simulation.rs` (deleted→split), `game/mod.rs` (6073→2592), `game/render.rs` (deleted→split), `ecs/mod.rs` (7163→17). Add `world_state.rs` (74 lines), `hydrology.rs`. This is blocking accurate agent decision-making.

3. **Investigate food/population non-determinism** [simulation_health]: Seed 137 food reads 3 on one run and 15 on another; seed 777 population reads 11 or 13. Two runs of the same seed at the same tick should produce identical output. Investigate RNG seeding in breeding/death systems — if `thread_rng()` is used anywhere in the game loop, replace with seeded RNG.
