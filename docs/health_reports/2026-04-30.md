# Daily Health Check — 2026-04-30

## Test Results
- **778 passed, 0 failed, 10 ignored** (lib tests, ~278s)
- Test count accuracy: features.json counts all within 50-200% tolerance — OK
- CLAUDE.md claims ~190 tests; actual is 818 (4× off) — stale, warns per check #29
- `construction_dust_particles_spawn` still not `#[ignore]`d — documented flaky, warned per check #14

## Design Drift

**Pillar 1 — Geography Shapes Everything (NEW FINDING)**
- Slope distribution is 98.4–98.7% flat across all 3 evaluation seeds (seed 42: flat=98.4%, seed 137: flat=98.7%, seed 777: flat=98.6%). Steep=0% and cliff=0% in all seeds. `elevation_std` is 0.09–0.11. The design doc requires terrain that "creates constraints on how you grow" and "mountain ranges as natural walls" — this cannot happen when 98%+ of tiles are flat. Filed as new known_issue in `terrain_generation`.
- Desert biome dominant in 2/3 seeds: seed 42=32%, seed 137=48.7% (near-half the map). Forest near-zero: seed 42=0.3%, seed 137=0.1%. Seed 777 is the exception (Desert=0.1%, Grass=12.5%). Asymmetric map generation remains broken post-WorldState refactor (matches existing `terrain_generation` known_issue, now confirmed persistent across 20+ health checks).

**Pillar 2 — Emergent Complexity (confirmed)**
- `VillagerMemory` still not referenced in `src/ecs/ai.rs` (check #22 fires). Villagers remain omniscient about global stockpile counts. No progress since this was first noted.

**Pillar 3 — Explore→Expand→Exploit→Endure**
- All events: `active_events=0` in all 3 seeds. No drought, no migration, no harvest events in any evaluation. Event system is structurally calibrated out of the 12K-tick evaluation window. Drought requires `grain >= pop*5` — impossible at current grain rates (see check #27).

**Roads never form**
- `ROAD_TRAFFIC_THRESHOLD=300.0` prevents road formation for populations of 4–11. 0% Road coverage in all seeds (check #25 fires). CLAUDE.md design principle "Roads auto-build from traffic" does not occur in practice.

**Fog-of-war trivially 100%**
- `exploration_pct=100.0` in all seeds (check #21 fires). Not a new finding.

## Architecture

No new size threshold violations. Current large files (below 3000-line flag):
| File | Lines | Status |
|------|-------|--------|
| `src/tilemap.rs` | 2515 | Priority 5 open (data-driven TerrainDef refactor) |
| `src/ecs/systems.rs` | 2227 | Approaching |
| `src/terrain_pipeline.rs` | 2232 | Approaching |
| `src/game/build.rs` | 2306 | Approaching |
| `src/ecs/ai.rs` | 2104 | Approaching |
| `src/simulation/moisture.rs` | 1867 | Largest sim module |

ARCHITECTURE.md file map is accurate vs actual directory structure. All split-product files confirmed present (checks #11–13).

`world_state.rs` still undocumented in ARCHITECTURE.md (features.json `world_state` known_issue) — unchanged from previous checks.

## Game Quality Evaluation

### Per-Seed Results
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Rabbits | Events |
|------|-----------|------|----------|-------------|--------|--------|----------|---------|--------|
| 42   | 4         | 12   | 3.0      | 4 (full)    | 30.7%  | 14     | Yes*     | 0       | 0      |
| 137  | 11        | 13   | 1.2      | 20          | 17.9%  | 14     | Yes      | 2       | 0      |
| 777  | 11        | 15   | 1.4      | 12          | 37.9%  | 14     | Yes      | 2       | 0      |

*Seed 42 dies at tick 6668 in default playtest (GAME OVER screen observed). All measurements at tick 6000 (screenshot half_speed_base calibration).

### Report Card Summaries
- Seed 42: `Population: Fragile | Food: Tight (no buffer) | Housing: Overcrowded | Terrain: Diverse | Threats: Low`
- Seed 137: `Population: Healthy | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing (23.75)`
- Seed 777: `Population: Healthy | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing (23.88)`

### Simulation diagnostics
| Seed | pipe_water_total | wind_moisture_total | avg_vegetation | avg_moisture | slope_flat% | Desert% | Forest% |
|------|-----------------|--------------------:|---------------|-------------|------------|---------|---------|
| 42   | 298.19          | 17,810              | 0.44          | 0.46        | 98.4%      | 32.0%   | 0.3%    |
| 137  | 244.62          | 16,603              | 0.42          | 0.32        | 98.7%      | 48.7%   | 0.1%    |
| 777  | 306.08          | 41,231              | 0.42          | 0.65        | 98.6%      | 0.1%    | 0.7%    |

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14 biomes in all seeds (high count), water 18-38% (good range). But: 98%+ flat (0% steep/cliff), Desert dominates 2/3 seeds (32-49%), Forest near-zero. Terrain lacks geographic character. |
| Settlement Viability (30%) | 2 | All 3 survive to tick 6000 but seed 42 dies at tick 6668. Food Critical in 2/3 seeds (food/cap 1.2-1.4). Seed 42 permanently stuck at pop=4, housing full, growth_potential=0 for 20+ consecutive runs. |
| Simulation Health (20%) | 4 | All systems active: pipe_water 244-306, wind_moisture 16K-41K, avg_vegetation 0.42-0.44. Moisture chain documented as broken but numbers non-zero. No regression from yesterday. |
| Emergent Behavior (20%) | 2 | 0 events across all 3 seeds. Identical building type mix ({Farm, Garrison, Hut, Stockpile, Workshop}) across all seeds — no Bakery, Granary, Smithy, or Bridge. No roads form. Seeds differ in terrain layout but not in emergent behavior patterns. |
| **Weighted Average** | **2.7** | 3×0.3 + 2×0.3 + 4×0.2 + 2×0.2 = 2.7 (down from 2.9 yesterday; food worsened in seeds 137/777, emergent scored lower due to identical building variety) |

### Baseline Check
- **3/3 PASS** — seed 42 pop=4 (within tolerance), seed 137 pop=4 (baseline has min/max range), seed 777 pop=11 ✅
- Note: baselines use wide tolerances (+20%) designed pre-non-determinism discovery; tightening when RNG is seeded.

## Harness Updates

**New Check #32 — Flat terrain detection**

Added to `scripts/validate_features.sh`: reads the most recent `docs/metrics_history.json` entry and warns when `slope_flat_pct > 95%` appears in 2+ seeds. Addresses Pillar 1 ("Geography Shapes Everything") — terrain this flat cannot create the strategic constraints the design requires. Fires on current code (today's data: all 3 seeds at 98%+ flat). Exits 0 (warning only).

**metrics_history.json** updated with today's entry including new fields: `slope_flat_pct`, `desert_pct`, `events_fired`, `housing_growth_potential`.

## features.json Updates

- `terrain_generation`: Added known_issue — `slope_flat_pct > 98%` in all 3 eval seeds; terrain is nearly flat despite `elevation_std > 0`; contradicts Pillar 1 (no hills/mountains to create strategic terrain features)
- All systems: `last_verified` updated to `2026-04-30`

## Harness Review (1st-of-month check — April 30 is last day, May 1 is 1st)

Running early since today is the last day of April.

1. **CLAUDE.md rules**: Rule "~190 tests" is 4× stale. Check #29 fires every day. The rule itself should be updated to remove the specific count, but health-check agents can't modify design docs — flagged for human.
2. **docs/workflow.md**: Workflow describes "GitHub issues" stage but no issues have been filed in the visible backlog. The workflow describes the intended flow but GitHub is not actively used. Minor drift.
3. **Checks that never fire**: Check #30 (population swing) fires due to non-determinism. Check #31 (prey extinction) fires intermittently. Check #23 (food crisis) fires. Check #24 (seed 42 stagnation) fires. All active checks appear to have fired at least once. No checks are obviously dead.
4. **Stale checks**: Check #21 warns `sight_range=25.0` — this is the wolf sight_range from spawn.rs line 72. Villager sight_range is 22.0 (line 137). The check uses `sort -g | tail -1` and catches the maximum, which is from wolves not villagers. The warning is arguably correct (wolves also trivially reveal the map), but the message text says "trivially reveals a 70x25 map" which overstates villager coverage. Consider making the check villager-specific.

## Priority Actions

1. **Food system viability** (lowest rubric score contributor): 2/3 seeds have food/cap < 2.0. Farm yield or hunger rate needs diagnosis. Per CLAUDE.md rules: add a diagnostic test showing actual farm yield vs hunger consumption rate BEFORE changing any constants. File: `src/ecs/systems.rs` (hunger) + `src/game/build.rs` (farm yield). [BACKLOG.md should have this as top item]
2. **Slope flatness / terrain variety** (Pillar 1 gap, NEW): All seeds show 98%+ flat terrain. Investigate `terrain_pipeline.rs` erosion and height generation — is the height normalized to a very small range? `elevation_std=0.09-0.11` with `avg_height=0.42-0.47` suggests the landscape is nearly uniform. Debug actual height values before changing constants.
3. **Event calibration** (blocks Emergent score): Drought grain threshold `grain >= pop*5` is impossible in evaluation window. Consider reducing multiplier to `pop*2` after diagnosing actual grain accumulation rates. Other events (harvest, migration, wolf surge) should be diagnosed similarly — 0 events in 20 consecutive health checks indicates systematic miscalibration.
