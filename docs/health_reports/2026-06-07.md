# Daily Health Check — 2026-06-07

## Test Results
- **778 passed, 0 failed, 10 ignored** (193.61s)
- Test count accuracy: features.json counts within tolerance; 818 `#[test]` annotations in src/
- Known flaky tests still NOT marked `#[ignore]`: `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn`

## Design Drift
- **Pillar 2 gap persists**: `VillagerMemory` written in `systems.rs` and `components.rs`, never read in `ai.rs` — villagers still query global stockpile state, not personal memory. Core of Pillar 2 unimplemented.
- **Event modifier chain broken**: `tick_config.rain_rate` multipliers for drought/harvest built in `game/mod.rs:2135-2147` but `step_water_cycle` signature is `(bool, f64)` — `tick_config` is silently discarded. Events fire but have **zero simulation effect**. Check #33 fires.
- **Event system structurally suppressed**: events_fired=0 in 2+ seeds for 5/5 consecutive health checks. Check #36 fires.
- **Fog-of-war trivial**: `exploration_pct=100.0` in all 3 seeds at tick 6000. `sight_range=22` on a 70×25 map. Pillar 1 exploration mechanic non-functional. Checks #21 fires.
- No new design drift beyond known-tracked issues in features.json.

## Architecture
- No non-test source files over 3000 lines — passes.
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` file map (check #38).
- `CLAUDE.md` module structure references `simulation.rs` (split in 2026-03) and omits `world_state.rs` (added in 0D refactor) — check #40 fires.
- `CLAUDE.md` claims ~190 tests; actual is 818 — check #29 fires.

## Game Quality Evaluation

### Per-Seed Results (diagnostics @ tick ≈ 6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4           | 30.7%  | 14     | Yes      |
| 137  | 11        | 14   | 1.27     | 12          | 17.9%  | 14     | Yes      |
| 777  | 13        | 13   | 1.00     | 20          | 38.1%  | 15     | Yes      |

Note: Default seed playtest (seed 42) — GAME OVER at tick 6668, peak pop=4 (consistent with 35+ consecutive diagnostics runs). Seed 137 playtest survived to tick 12101 with pop=6 (food crisis shrinking pop from 11).

### Report Card Summaries
- Seed 42: "Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)"
- Seed 137: "Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)"
- Seed 777: "Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)"

Note: Report cards are separate RNG runs from diagnostics — seed 137 report shows food.per_capita=0.0 (food=0 in that run) vs diagnostics food=14. Within-session non-determinism confirmed again.

### Rubric Scores
| Category | Score (1–5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 14–15 biome types (good), but 98.4–98.7% flat, Desert dominant 32–49%, Forest <1%. No mountains or cliffs. Checks #32 and #42 both fire. |
| Settlement Viability (30%) | 2 | All 3 survive tick 6000 but seed 42 dies at 6668. Food critical in 2/3 seeds (food_per_cap 1.0–1.27 < 2.0 threshold). Seed 42 housing saturated (growth=0). Non-deterministic outcomes obscure true viability. |
| Simulation Health (20%) | 4 | pipe_water_total: 244–313 (positive). wind_moisture_total: 15944–41378 (positive). avg_vegetation: 0.41–0.44 (>0.1). All systems active. |
| Emergent Behavior (20%) | 2 | Seeds diverge: pop 4 vs 11 vs 13, biome distributions differ. Building counts vary (5/8/15). BUT events_fired=0 in all seeds (structurally suppressed). No events = no narrative, no "unscripted 3-step causal chains." Pillar 2 design goal unmet. |
| **Weighted Average** | **2.4** | 0.6 + 0.6 + 0.8 + 0.4 = 2.4. Flat for 2nd consecutive day (2026-06-06: 2.4, 2026-06-05: 2.2). |

### Baseline Check
- Seed 42: **PASS** (pop=4, water=30.7%, biomes=13)
- Seed 137: **PASS** (pop=11, water=17.9%, biomes=14)
- Seed 777: **PASS** (pop=11, water=38.0%, biomes=14)

## Harness Updates
- **Check #54 added** to `scripts/validate_features.sh`: `mining_sparkle_particles_spawn` known-flaky test guard. Warns if the test exists in `src/game/tests.rs` without `#[ignore]`. Fires today (1 new WARN). Consistent pattern with checks #14 and #35 for the other two known-flaky tests. Test verified exits 0 on current code (34 warnings total, 0 errors).

## features.json Updates
- Updated `last_verified` to `2026-06-07` for all 25 systems.
- `ecs_core.known_issues`: noted that `mining_sparkle_particles_spawn` flaky guard now in validate_features.sh (check #54).
- No status changes — all statuses unchanged from yesterday.

## Priority Actions
1. **Fix biome distribution** (`terrain_pipeline.rs`): Forest <1% in all seeds drives downstream failures — wood starvation (check #44), wood skill pure-decay (check #41), Bakery permanently blocked (check #47), grain dead-end (check #53). This is the single highest-leverage fix: check #42 fires. Design doc: Pillar 1 — terrain must create different strategic conditions per seed.
2. **Fix seeded per-entity RNG** (`game/build.rs`, `ecs/systems.rs`): `rand::rng()` in auto-build and breeding paths produces ±11 population variance between sequential same-seed runs. The rubric scores are unreliable until this is fixed. Root cause: `ecs_core` known issue, tracked since 2026-04-24. Mark three flaky tests `#[ignore]` as immediate mitigation.
3. **Fix event modifier chain** (`game/mod.rs`, `game/water_cycle.rs`): drought/harvest events have zero simulation effect. Wiring `tick_config` through to `step_water_cycle` would make events matter, improving Emergent Behavior score. Check #33 and #36 fire.
