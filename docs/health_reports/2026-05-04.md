# Daily Health Check — 2026-05-04

## Test Results
- Total: **778 passed, 0 failed, 10 ignored** (788 running, 265s)
- Test count accuracy: OK — all systems within 30% tolerance per validate_features.sh
- `cargo test --lib` exit 0, no regressions

## Validate Features
- **0 errors, 19 warnings** (same count as 2026-05-02; WorldState check passes, but prey extinction check now fires again: seed 137 rabbits=0 in today's run)
- Persistent warnings (unchanged from May 2):
  - `save_load` needs_tests, test_count=0
  - `construction_dust_particles_spawn` flaky test not marked `#[ignore]`
  - `rand::rng()` in systems.rs (3 occurrences), game/mod.rs (3 occurrences)
  - `world_state` needs_work, test_count=0
  - sight_range=25 + reveal_radius=15 trivially reveals full map (fog-of-war inactive)
  - VillagerMemory not read in ai.rs (Pillar 2 gap)
  - Persistent food crisis (food_per_cap < 2.0 in 4/5 recent checks, 2+ seeds)
  - Seed 42 pop=4/food=12 stagnation for 5/5 recent checks
  - ROAD_TRAFFIC_THRESHOLD=300 prevents road formation
  - fire.rs, water_cycle.rs, particles.rs: 0 tests each
  - Drought threshold (grain >= pop * 5) structurally impossible in evaluation window
  - CLAUDE.md claims ~190 tests; actual is 818 (4× stale)
  - slope_flat_pct > 95% in 3/3 seeds (98.4–98.7%)
  - Event modifier chain dead code (tick_config discarded before step_water_cycle)

## Design Drift

1. **Pillar 1 (Geography Shapes Everything)** — DRIFT (ongoing since 2026-04-30): Terrain is 98%+ flat (0% steep, 0% cliff) in all 3 seeds. Desert dominates in seed 42 (30.2%) and seed 137 (49.0%) with near-zero Forest (0.1–0.3%). Elevation std=0.09–0.12. No hills or mountain passes to create strategic constraints. Contradicts design principle that "two seeds produce fundamentally different settlements." `terrain_pipeline.rs` height generation amplitude is too low.

2. **Pillar 2 (Emergent Complexity)** — DRIFT: `VillagerMemory` is defined in `components.rs` and written in `systems.rs` but never read in `ai.rs`. Villagers still use global stockpile counts (15 occurrences in ai.rs). Phase 2 memory-driven AI not started.

3. **Pillar 3 (Explore→Expand→Exploit→Endure)** — DRIFT: `exploration_pct=100%` in all 3 seeds by tick 6000. Fog-of-war trivially resolved due to `sight_range=25` + initial `reveal_radius=15`. Exploration phase cannot emerge.

4. **Design anti-goal "Roads auto-build from traffic"** — DRIFT: `ROAD_TRAFFIC_THRESHOLD=300.0` (src/game/mod.rs) prevents road formation at pop=4–13. 0% Road coverage in all evaluation seeds. Road emergence was an explicit design goal (CLAUDE.md: "Roads auto-build from traffic. Don't add manual road placement").

5. **Event effects** — CRITICAL DRIFT (since 2026-05-01): `tick_config` applies drought/harvest `rain_rate` multipliers in `game/mod.rs:2135–2147` but `step_water_cycle()` signature is `(bool, f64)` — tick_config is silently discarded. Even if drought fires, it has zero simulation effect.

## Architecture

**Non-test source files over 2000 lines** (ARCHITECTURE.md flags these):
| File | Lines | Note |
|------|-------|------|
| `src/game/mod.rs` | 2592 | Getting big |
| `src/tilemap.rs` | 2515 | Priority 5 — data-driven TerrainDef refactor open |
| `src/game/build.rs` | 2306 | Getting big |
| `src/terrain_pipeline.rs` | 2232 | Approaching split threshold |
| `src/ecs/systems.rs` | 2227 | Getting big |
| `src/ecs/ai.rs` | 2104 | Getting big |

No file exceeds 3000 lines (validate_features.sh threshold). ARCHITECTURE.md file map is accurate. All completed refactors intact (simulation.rs split, render.rs split, ECS tests extracted, WorldState 0D architecture).

## Game Quality Evaluation

### Per-Seed Results
*(Primary source: --report-card runs. Diagnostics run in same session diverged: seed 137 diag=pop4 vs RC=pop11; seed 777 diag=pop7 vs RC=pop13 — non-determinism most extreme within-session divergence observed to date.)*

| Seed | Population | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|----------|-------------|--------|--------|----------|
| 42   | 4         | 3.0      | 4 (cap=4)   | 30.9%  | 13     | Yes      |
| 137  | 11        | 1.1 (Critical) | 12 (cap=12) | 17.9% | 14 | Yes |
| 777  | 13        | 1.2 (Critical) | 16 (cap=16) | 38.0% | 15 | Yes |

Simulation metrics (from diagnostics):
- pipe_water_total: 300.9, 244.6, 310.9 (all active)
- wind_moisture_total: 19939, 16134, 39973 (all active)
- avg_vegetation: 0.43, 0.41, 0.42 (all > 0.1)
- Events fired: 0, 0, 0 (23rd consecutive zero-event health check)

### Report Card Summaries
- Seed 42: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)
- Seed 137: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)
- Seed 777: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13–15 biomes, water 17–38% (good range), but 98%+ flat, 0% steep/cliff, desert 30–49% dominates 2/3 seeds |
| Settlement Viability (30%) | 2 | All 3 survive tick 6K; food Critical in 2/3 seeds (f/cap=1.1–1.2); Seed 42 pop=4 stagnant; chronic food crisis |
| Simulation Health (20%) | 4 | All simulation subsystems active with non-zero values; event modifier chain broken but underlying pipes/wind/vegetation running |
| Emergent Behavior (20%) | 2 | 0 events fired (23rd consecutive); no roads (threshold too high); no outposts; seeds diverge on terrain but behavioral emergence absent |
| **Weighted Average** | **2.7** | 3×0.3 + 2×0.3 + 4×0.2 + 2×0.2 |

### Baseline Check
```
PASS: seed 137 (pop=11, water=17.9%, biomes=14)
PASS: seed 42  (pop=4,  water=30.7%, biomes=13)
PASS: seed 777 (pop=11, water=38.0%, biomes=14)
Results: 3 passed, 0 failed
```

## Harness Updates
- No new checks added today. All 18 warning patterns already have coverage in validate_features.sh checks 1–34.
- **Candidate for next session:** Check 35 — double-run divergence guard: run seed 137 twice sequentially in validate_features.sh and compare populations; warn if |pop1-pop2| > 5. Today's within-session divergence (pop=4 vs pop=11 for seed 137) was the largest observed and not captured by current harness checks.

## features.json Updates
- `ecs_core.known_issues`: Updated with today's within-session non-determinism observation (diag vs RC divergence: seed 137 pop=4 vs 11; seed 777 pop=7 vs 13 — same binary, same session, sequential runs).
- `last_verified` updated to 2026-05-04 for all systems.

## Priority Actions
1. **Fix flat terrain (terrain_quality=3, Pillar 1):** `terrain_pipeline.rs` height/amplitude parameters produce elevation_std=0.09–0.12 and 98%+ flat slopes. Investigate fBm octave amplitudes and SPL erosion intensity — the world is too flat to create geographic constraints. Diagnose with `--diagnostics` before changing constants.
2. **Fix food production (settlement_viability=2, chronic):** food_per_cap < 2.0 in seeds 137/777 for 4/5 consecutive checks. Farm yield vs hunger rate diagnostic needed in `ecs/systems.rs` and `game/build.rs` before changing anything. Check farm assignment logic (how many villagers tend each farm?).
3. **Fix event system (emergent_behavior=2, critical drift):** (a) Reconnect `tick_config` modifiers to `step_water_cycle` so drought/harvest have actual effect. (b) Recalibrate event thresholds — drought grain>=pop*5 never fires, road threshold=300 never fires. Lower conservatively after diagnostic run confirms current production values.
