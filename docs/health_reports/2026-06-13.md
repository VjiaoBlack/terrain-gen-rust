# Daily Health Check — 2026-06-13

## Test Results
- **778 passed, 0 failed, 10 ignored** (322.97s)
- Test count accuracy: within tolerance — all features.json counts match actual source
- Known-flaky: `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn` all unignored (3× CI false-failure risk per check #55)

## Design Drift

**Pillar 2 gap — VillagerMemory not wired into AI (check #22 fires)**
`ecs/components.rs` defines `VillagerMemory` (DangerZone, ThreatReport); `ecs/systems.rs` writes it; `ecs/ai.rs` never reads it. AI still reads global stockpile counts (15 occurrences in `ai.rs`). Design doc requires local-only decision-making. No progress since first flagged.

**Event modifier chain dead code (check #33 fires)**
`game/mod.rs:2135-2147` builds `tick_config` with drought (`rain_rate*=0.4`) and harvest (`rain_rate*=1.5`) but `step_water_cycle()` signature is `(bool, f64)` — `tick_config` is silently discarded. Drought and harvest events have zero effect on simulation even when they fire.

**Fog-of-war trivially complete (checks #21 fires)**
`exploration_pct=100.0` in all 3 seeds. `sight_range=22`, initial reveal radius=15 trivially fill a 70×25 map. Pillar 3 Explore phase (tension: "what kind of map did we get?") is inoperative.

**CLAUDE.md stale module structure (check #40 fires)**
Still lists `simulation.rs` (split 2026-03-01); missing `world_state.rs` (added 0D refactor).

## Architecture

- No files over 3000 lines (threshold met)
- Six files in 2000–2600 line range: `game/mod.rs` (2592), `tilemap.rs` (2515), `game/build.rs` (2306), `terrain_pipeline.rs` (2232), `ecs/systems.rs` (2227), `ecs/ai.rs` (2104) — approaching split threshold per ARCHITECTURE.md
- `analytical_erosion.rs` and `src/scripting/` absent from ARCHITECTURE.md file map (check #38)
- `world_state.rs` (74 lines, 0 tests, status=`needs_work`) has no unit tests and is not documented in ARCHITECTURE.md or design docs

## Game Quality Evaluation

### Per-Seed Results (diagnostics, tick=6000)
| Seed | Pop | Food | Food/Cap | Housing Cap | Growth Pot | Water% | Biomes | Flat% | Survived |
|------|-----|------|----------|-------------|------------|--------|--------|-------|---------|
| 42   | 4   | 12   | 3.00     | 4           | 0          | 30.7%  | 13     | 98.4% | yes     |
| 137  | 11  | 15   | 1.36     | 20          | 9          | 17.8%  | 14     | 98.7% | yes     |
| 777  | 11  | 13   | 1.18     | 16          | 5          | 38.0%  | 14     | 98.6% | yes     |

### Report Card Summaries
- Seed 42: `Fragile (one bad winter away) | Tight (no buffer) fpc=3.8 | Overcrowded (need more huts) | Diverse terrain | Low threat`
- Seed 137: `Healthy (growing) | CRITICAL food fpc=0.7 (starvation imminent) | Tight housing | Diverse terrain | Growing threats`
- Seed 777: `Fragile | CRITICAL food fpc=0.0 (starvation imminent) | Adequate housing | Diverse terrain | Growing threats`

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13-14 biomes (count OK), but 98%+ flat (steep=0%, cliff=0%), Desert 32-49% in 2 seeds, Forest<1% in all — biome distribution severely imbalanced |
| Settlement Viability (30%) | 2 | All 3 survived diagnostics; avg pop=8.7 (ok); food crisis in 2/3 seeds (report cards: 2× Critical, fpc=0.0/0.7); seed 42 stuck at pop=4 for 35+ health checks; housing saturated seed 42 |
| Simulation Health (20%) | 4 | pipe_water_total>0 (298/244/314), wind_moisture_total>0 (17K/16K/41K), avg_vegetation>0.1 (0.41-0.44) — all systems active; moisture chain architectural debt (3 water systems) |
| Emergent Behavior (20%) | 2 | 0 events fired (event modifier chain broken); seed 137/777 pop converged (11/11); no roads (ROAD_TRAFFIC_THRESHOLD=300.0 too high); wood_skill=0.5488 pure-decay in all 3 seeds |
| **Weighted Average** | **2.4** | `2×0.3 + 2×0.3 + 4×0.2 + 2×0.2` — down from 2.7 yesterday (0 events vs WolfSurge fired yesterday; food more critical) |

### Baseline Check
- **3/3 PASS**: seed 42 (pop=4, water=30.7%, biomes=13), seed 137 (pop=11, water=17.9%, biomes=14), seed 777 (pop=11, water=37.9%, biomes=14)

## Harness Updates
- **Check #59 added** to `validate_features.sh`: Bakery-chain-never-produced-bread detector. Checks if `bread=0` in ALL seeds across ALL metrics_history entries with bread data. Confirmed: 5 consecutive health checks with bread data, all bread=0. This proves the Bakery chain has never functioned since tracking began (root cause: Forest<1% → no wood → planks=0 → Bakery blocked). Companion to check #47 (static planks threshold) and check #53 (grain dead-end).

## features.json Updates
- `last_verified` updated to 2026-06-13 for all systems
- `ecs_core.known_issues`: added observation — within-session seed 777 food divergence (report card fpc=0.0 vs diagnostics fpc=1.18, delta=1.18); seed 137 report card fpc=0.7 vs diagnostics fpc=1.36; rand::rng() driving food outcomes as much as building outcomes
- Score: 2.4/5.0 (terrain=2, settlement=2, simulation=4, emergent=2)

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** — Forest<1% in all seeds blocks wood supply → planks=0 → Bakery impossible; Desert 32-49% means no geographic variety; flat terrain (98%+) violates Pillar 1. Single highest-leverage fix: restore Forest to ≥10%, Mountain/Cliff coverage to ≥5% each. Would fix terrain score (2→4), enable emergent production chains, and differentiate seeds. Read terrain_pipeline.rs biome scoring logic before touching anything.
2. **Seeded per-entity RNG (`game/build.rs`, `ecs/systems.rs`)** — rand::rng() causes ±7 population swings and ±11 food swings between consecutive same-seed runs. Makes evaluation unreliable and prevents accurate rubric scoring. Replace `rand::rng()` with a `SmallRng` seeded from entity ID + global seed. Prerequisite for stabilizing settlement score.
3. **FoodToGrain Granary adaptive threshold** — `systems.rs:979`: change `food > 15` to `food > max(15, pop * 2)`. Quick targeted fix (single constant change) for recurring starvation in seed 137 (4× food=0 documented). Does not require biome fix. Write test first: assert that at pop=11 with food=22, Granary does NOT drain below 22.
