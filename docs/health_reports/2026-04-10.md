# Daily Health Check — 2026-04-10

## Test Results
- **Run 1**: 776 passed, 2 failed, 10 ignored (182.7s)
- **Run 2**: 778 passed, 0 failed, 10 ignored (173.2s)
- Failures are **flaky/non-deterministic**: both tests pass individually

### Failing tests (run 1)
- `game::tests::save_load_round_trip` — uses hardcoded `/tmp/test_savegame.json` (src/game/tests.rs:212). Fails intermittently when parallel test threads contend on file I/O. Likely takes >60s under load and triggers timeout pressure.
- `game::tests::build_site_gets_completed_in_game` — timing-sensitive (runs up to 3000 ticks); fails when scheduler contention delays villager AI.

### Test count accuracy (validate_features)
- All within tolerance. Only warning: `save_load` status=`needs_tests`, test_count=0 (persistent, no progress).

---

## Design Drift

### ARCHITECTURE.md is significantly stale
The file still lists pre-refactor sizes for systems that have already been split:

| System | Doc says | Actual |
|--------|----------|--------|
| `game/mod.rs` | 6073 lines (NEEDS SPLIT / BAD) | **2592 lines** |
| `simulation.rs` | 4844 lines (NEEDS SPLIT / BAD) | **Deleted — split into `simulation/`** |
| `game/render.rs` | 4094 lines (NEEDS SPLIT / BAD) | **Split into `game/render/`** (mod=1300, shared=914, overlays=555, normal=415, landscape=398) |
| `ecs/mod.rs` | 7163 lines (NEEDS SPLIT / BAD) | **17 lines** (tests extracted to `ecs/tests.rs`: 7164 lines) |

Priorities 1–4 from the refactor plan have been substantially completed. ARCHITECTURE.md must be updated to reflect current reality. New sizes to document: `simulation/moisture.rs` = 1867 lines (largest simulation module), `game/build.rs` = 2306 lines.

### VillagerMemory never read in AI (Pillar 2 drift, game_design.md)
Design intent: agents respond to local conditions, share information through contact. Implementation: `VillagerMemory` is populated with `DangerZone`/`ThreatReport` in `systems.rs` but `ai.rs` still reads global stockpile counts (15 occurrences). Phase 2 knowledge architecture not started. Known in features.json.

### Fog of War non-functional (Pillar 1/2 drift)
`exploration_pct = 100.0` on all 3 seeds, trivially. Design intent is genuine discovery through exploration. Known in features.json.

### No events firing at tick 6000 (Pillar 2/3 drift)
Zero active events on all 3 seeds. `Drought` event requires `grain >= pop*5` which is structurally unreachable at this tick count. WolfSurge fires only at higher threat_score. The emergent "crisis from system interactions" vision (game_design.md §Pillar 2) is not manifesting in evaluation windows.

---

## Architecture

### Module sizes
No non-test source file exceeds 3000 lines (validate_features OK).

| File | Lines | Status |
|------|-------|--------|
| `src/ecs/tests.rs` | 7164 | Test file — excluded from check |
| `src/game/tests.rs` | 2925 | Test file — excluded |
| `src/game/mod.rs` | 2592 | Improved from 6073, still largest non-test src file |
| `src/tilemap.rs` | 2515 | Getting big (known) |
| `src/game/build.rs` | 2306 | Growing |
| `src/simulation/moisture.rs` | 1867 | Largest simulation module; 9/18 tests are `#[ignore]` |

### Compiler warnings
11 warnings in release build: unused method `wrapping_idx` (moisture.rs), unused fns `map_terrain_glyph`/`draw_map_building_marker` (render/map.rs), unused `run_sediment_steps` (pipe_water.rs), unused const `DIRECTIONS` (chokepoint.rs), several `mut` removals.

---

## Game Quality Evaluation

### Per-Seed Diagnostics (tick 6000 / 12000-equivalent)

| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 14     | Yes      |
| 137  | 11        | 3    | 1.1      | 16          | 17.9%  | 14     | Yes      |
| 777  | 11        | 13   | 1.9      | 20          | 38.0%  | 14     | Yes      |

### Report Card Summaries
- **Seed 42**: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse | Threats: Low
- **Seed 137**: Population: Healthy (growing) | Food: **Critical (starvation imminent)** | Housing: Tight (limited growth room) | Terrain: Diverse | Threats: Growing
- **Seed 777**: Population: Healthy (growing) | Food: **Critical (starvation imminent)** | Housing: Adequate (room to grow) | Terrain: Diverse | Threats: Growing

### Slope distribution (all seeds)
98.4–98.7% flat, 1.3–1.6% gentle, 0% steep/cliff. elevation_std = 0.09–0.12. Terrain is almost entirely flat — a persistent known issue.

### Rubric Scores

| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | Biomes excellent (14 types). Water within range (18–38%). Elevation critically flat (98.5%+ flat, std ≤0.12). |
| Settlement Viability (30%) | 2 | All seeds survive. Food Critical on 2/3 seeds (per-cap 1.1, 1.9). Seed 42 pop=4, housing full, growth=0. |
| Simulation Health (20%) | 4 | All systems active: pipe_water >244, wind_moisture >17K, avg_vegetation 0.42–0.44. Moisture chain still partially unclear. |
| Emergent Behavior (20%) | 3 | Seeds diverge on terrain/pop/skills (farm skill 1.8 vs 20, pop 4 vs 11). No events fire. Buildings limited to 5 types (no Smithy/Bakery/Granary). |
| **Weighted Average** | **2.9** | Regression from 3.0 (2026-04-06) — food security worsened on seed 137 (food 18→3). |

### Baseline Check
```
Running seed 137 for 12000 ticks...  PASS: pop=11, water=17.9%, biomes=14
Running seed 42 for 12000 ticks...   PASS: pop=4,  water=30.7%, biomes=13
Running seed 777 for 12000 ticks...  PASS: pop=13, water=38.1%, biomes=14
Results: 3 passed, 0 failed
```

---

## Harness Updates

### New check added to `validate_features.sh`
**Check #10**: Scans test files for hardcoded `/tmp/` paths without unique suffixes — detects potential parallel test file races like the one causing `save_load_round_trip` to flake.

### New check in `check_baselines.sh`
Added food-per-capita minimum (≥1.0) for surviving settlements.

---

## features.json Updates
- `game_loop`: added known_issue for `save_load_round_trip` parallel flakiness (hardcoded `/tmp/test_savegame.json`)
- `game_loop`: updated `last_verified` to 2026-04-10
- `ecs_core`: updated `last_verified` to 2026-04-10
- `rendering`: updated `last_verified` to 2026-04-10

---

## Priority Actions

1. **Fix `save_load_round_trip` flakiness** [game_loop]: Replace `/tmp/test_savegame.json` with a unique temp path (e.g., `tempfile` crate or `format!("/tmp/test_save_{}.json", std::process::id())`). This is the #1 priority as it's causing real CI failures.

2. **Food security regression** [settlement_viability]: Food per-capita is Critical (≤1.9) on 2/3 seeds. Seed 137 dropped from food=18 to food=3. Investigate whether granary/bakery production chain is reachable via auto-build — if not, add it. Check if farms are keeping pace with population growth.

3. **Update ARCHITECTURE.md** [architecture]: The refactors (Priorities 1–4) are substantially done. Update file sizes, remove "NEEDS SPLIT / BAD" labels, add new modules (`simulation/`, `game/render/`, `ecs/tests.rs`, `world_state.rs`, `hydrology.rs`). This prevents future agents from making incorrect architectural decisions.
