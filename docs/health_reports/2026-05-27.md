# Daily Health Check — 2026-05-27

## Test Results
- **778 passed, 0 failed, 10 ignored** — CLEAN RUN (~295s)
- 818 `#[test]` annotations in src/ (unchanged from 2026-05-23)
- 11 compiler warnings (unchanged — unused_mut, dead variables, DIRECTIONS const)
- Known-flaky tests still not `#[ignore]`d: `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`

## Design Drift

**Pillar 1 — Geography Shapes Everything (violated):**
- Terrain 98%+ flat in all 3 seeds (flat_pct: 98.4%, 98.69%, 98.57%; elevation_std: 0.11, 0.09, 0.11; 0% Mountain/Cliff across all seeds). Topographic constraints on settlement completely absent.
- Forest biome <1% all seeds (42=0.3%, 137=0.1%, 777=0.6%). All buildings cost wood, no tree coverage means no replenishment. Check #42 fires.
- Seeds 137/777 Pillar 1 convergence: 5/5 consecutive health checks with population diff ≤ 1, despite radically different biomes (Desert=48.7% vs Desert=0.1%). Design goal "two different maps → two fundamentally different settlements" unmet.

**Pillar 2 — Emergent Complexity (not started):**
- `VillagerMemory` is written in `ecs/systems.rs` but never read in `ecs/ai.rs` (check #22). Villagers use global stockpile counts for all decisions. Core Pillar 2 requirement unimplemented.

**Event system broken:**
- 0 events fired in all 3 seeds — 6th consecutive zero-event health check (check #36 fires: suppressed 5/5).
- `tick_config` drought/harvest `rain_rate` modifiers built at `game/mod.rs:2135–2147` but silently discarded — never passed to `step_water_cycle` (check #33 fires). Drought and harvest events have zero simulation effect.

**Fog-of-war trivial:** `sight_range=25` + initial reveal radius=15 exposes full 70×25 map within ~200 ticks. `exploration_pct=100.0` in all seeds at tick 6000. "Exploration as discovery" non-functional.

**Roads never form:** `ROAD_TRAFFIC_THRESHOLD=300.0` exceeds what pop=4–11 settlements can accumulate. 0% Road coverage in all eval seeds. "Roads auto-build from traffic" unobservable.

## Architecture

- No non-test source files over 3000 lines (check #5 passes).
- Files approaching split threshold (>2000 lines): `game/mod.rs` (2592), `tilemap.rs` (2515), `build.rs` (2306), `terrain_pipeline.rs` (2232), `ecs/systems.rs` (2227), `ecs/ai.rs` (2104). Unchanged from last check.
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38, known since 2026-05-08).
- `CLAUDE.md` module structure lists `simulation.rs` (split 2026-03) and omits `world_state.rs` (check #40, known since 2026-05-11). Test count claim (~190) vs actual 818 (check #29).
- `ProcessingBuilding` diagnostic conflation (`game/mod.rs:2299`) still counts pre-built Granary as Workshop (check #46).

## Game Quality Evaluation

### Per-Seed Results (--diagnostics run, game.tick≈6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Events | Survived |
|------|-----------|------|----------|-------------|--------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4           | 30.8%  | 14     | 0      | ✓ (dies ~6668) |
| 137  | 11        | 11   | 1.00     | 24          | 17.9%  | 14     | 0      | ✓        |
| 777  | 4         | 14   | 3.50     | 4           | 37.9%  | 14     | 0      | ✓        |

**Note on seed 777 severe non-determinism:** Three sequential runs in today's session produced pop=4 (diagnostics), pop=14 (report-card), pop=8 (baseline check). This is the largest within-session variance observed — three distinct outcomes spanning pop=4–14 in a single health check. Root cause: `rand::rng()` in `game/build.rs` auto-build scoring places 1, 5, or 3 huts depending on RNG state.

**Notable change vs 2026-05-23:** Seed 777 population collapsed from 12→4 in diagnostics run (delta=8, exceeds check #30's threshold of 5 for consecutive metrics entries). Seed 137 food/cap dropped from 1.45→1.0, a new all-time low for this run.

### Report Card Summaries
- Seed 42: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded | Terrain: Diverse (rich landscape) | Threats: Low
- Seed 137: Population: Healthy (growing) | Food: Critical (starvation imminent, 1.3 f/cap) | Housing: Adequate (room to grow) | Terrain: Diverse | Threats: Growing (score=23.77)
- Seed 777: Population: Healthy (growing, pop=14) | Food: Critical (starvation imminent, 1.6 f/cap) | Housing: Adequate (room to grow) | Terrain: Diverse | Threats: Growing (score=31.58)
  - Report card and diagnostics diverge by pop=10 (14 vs 4) — strongest within-session evidence of rand::rng() auto-build non-determinism

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 14 biomes present (≥8 ✓), water 17.9–37.9% (within range ✓), but 98%+ flat (0 steep/cliff), Forest <1%, Desert 31–49% in 2/3 seeds |
| Settlement Viability (30%) | 2 | Seed 42 dies ~tick 6668; food Critical in 2/3 seeds (f/cap 1.0–3.5); housing saturated in seeds 42 and 777 (pop=cap=4); avg pop=6.3 |
| Simulation Health (20%) | 4 | pipe_water (244–310), wind_moisture (16K–40K), avg_vegetation (0.42–0.43) all active and well-bounded |
| Emergent Behavior (20%) | 2 | 0 events fired (6th consecutive zero-event check); 0% roads; seed 137/777 Pillar 1 convergence 5/5; no production chains completing |
| **Weighted Average** | **2.4** | 17th consecutive score of 2.4/5.0. Score plateau persists (check #17 fires: flat at ~2.4 for 5+ entries). |

### Baseline Check
- **3/3 PASS**: seed 137 (pop=11, water=17.9%, biomes=15), seed 42 (pop=4, water=30.7%, biomes=13), seed 777 (pop=8, water=38.1%, biomes=14)

### validate_features.sh
- **0 errors, 35 warnings** (unchanged from 2026-05-23)
- All 48 checks verified against current codebase

## Harness Updates

**New check added: #49 — Metrics history schema completeness**

Many harness checks silently SKIP when required fields are missing from the most recent `metrics_history.json` entry (e.g., checks #31 requires `rabbits`, #32 requires `slope_flat_pct`, #41 requires `wood_skill`). If a health check agent omits these fields from the metrics entry, checks SKIP without warning. New check #49 verifies all required fields are present:

```bash
# Required per-seed fields: population, food, food_per_cap, wood, buildings,
# water_pct, biomes, rabbits, survived, slope_flat_pct, desert_pct, forest_pct,
# events_fired, housing_growth_potential, wood_skill
```

This is a meta-check: it guards the harness itself, not the game.

## features.json Updates
- All systems `last_verified` updated to `2026-05-27`
- `ecs_core` known_issues: seed 777 three-way within-session divergence (pop=4/14/8 across three sequential runs today); seed 42 now 35th+ consecutive stuck pop=4 food=12; seed 137 food/cap at all-time low of 1.0 (previous worst: 0.91 on 2026-05-16 with food=0; this run food=11 but pop=11)

## Priority Actions
1. **Fix biome distribution** (`terrain_pipeline.rs`) — Forest <1% is root cause of: zero woodcutting, pure-decaying wood skill, production chain blockage (Workshop→planks, Bakery), Pillar 1 non-differentiation (checks #32, #41, #42, #44, #48). Fix before any other gameplay change.
2. **Fix event modifier chain** (`game/mod.rs:2135–2147` → `step_water_cycle`) — two-line fix to connect `tick_config` to water cycle; unlocks drought/harvest simulation effects (check #33). Would improve Emergent Behavior rubric score.
3. **Fix `rand::rng()` non-determinism** (`game/build.rs`, `ecs/systems.rs`, `game/mod.rs`) — within-session seed 777 variance of pop=4/14/8 in three runs today is unacceptable for data-driven evaluation. Replace with seeded per-entity RNG as documented in BACKLOG.md.
