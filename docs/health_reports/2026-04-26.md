# Daily Health Check — 2026-04-26

## Test Results
- **778 passed, 0 failed, 10 ignored** (198.5s) — identical to all previous entries
- 818 `#[test]` annotations in src/ (above the ≥700 regression guard)
- `validate_features.sh`: 0 errors, 16 warnings (unchanged from 2026-04-25)
- All listed files exist; all split-product files present; all statuses valid

### validate_features warnings (persistent)
- `save_load` status='needs_tests' with test_count=0
- `construction_dust_particles_spawn` not marked `#[ignore]` (flaky CI risk)
- `rand::rng()` in systems.rs (×3) and game/mod.rs (×3) — non-determinism
- Score plateau at ~2.9/5.0 for 5+ consecutive entries (now 16th)
- `world_state` status='needs_work' with test_count=0
- `sight_range=25.0` trivially reveals map (fog-of-war non-functional)
- VillagerMemory not read in ai.rs (Pillar 2 gap)
- Persistent food crisis: food_per_cap < 2.0 in 2+ seeds for 5/5 recent checks
- Seed 42 stagnation: pop=4, food=12 for 5/5 recent checks (15 total consecutive)
- `ROAD_TRAFFIC_THRESHOLD=300.0` prevents road formation
- fire.rs, water_cycle.rs, particles.rs have 0 tests
- Drought structurally impossible (grain >= pop×5 never met)

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Fog-of-war non-functional: all seeds hit exploration_pct=100% long before tick 6000. sight_range=25 (vs 22 in CLAUDE.md comments) + initial reveal radius=15 trivially covers any map.
- Desert biome dominates 32-49% in seeds 42 and 137, Forest <0.4%. The design calls for forest belts, mountain ranges, resource geography — the actual map is a dry flatland.
- Terrain is 98%+ flat (slope_dist: flat_pct=98.4–98.7%, zero cliff/steep tiles) — mountain passes, river valleys, chokepoints don't form.

**Pillar 2 — Emergent Complexity:**
- VillagerMemory (DangerZone, ThreatReport) is written in systems.rs but never read in ai.rs. Villagers still check global stockpile counts (15 occurrences in ai.rs). Phase 2 memory-driven AI not started.
- Systems do NOT chain through simulation state as designed: drought is `yield × 0.5` not `water → soil → crop`.

**Pillar 3 — Explore → Expand → Exploit → Endure:**
- No arc visible. Default seed (42) dies at game tick 6668 with peak pop 4. No expansion phase, no exploitation, no endurance.
- Event system non-functional in practice: 0 events across all 3 seeds at tick 6000 (16th consecutive zero-event eval). Drought structurally impossible; WolfSurge threshold never reached in this run.

**Roads (Anti-goal drift):** Design says "Roads auto-build from traffic." ROAD_TRAFFIC_THRESHOLD=300.0 means 0% Road coverage in all eval seeds. The feature exists in code but is calibrated out of reach.

## Architecture

Module sizes (no change from yesterday):
| File | Lines | Status |
|------|-------|--------|
| game/mod.rs | 2592 | Getting big |
| game/build.rs | 2306 | Getting big |
| terrain_pipeline.rs | 2232 | Getting big |
| tilemap.rs | 2515 | Getting big |
| ecs/systems.rs | 2227 | Getting big |
| ecs/ai.rs | 2104 | Getting big |

No files exceed 3000 lines (split threshold for validate_features). Closest: tilemap.rs at 2515.

ARCHITECTURE.md is accurate — all major refactors are reflected. world_state.rs is not documented there (known issue in features.json).

CLAUDE.md lists ~190 lib tests; actual is 778. Comments stale by 4×.

## Game Quality Evaluation

### Per-Seed Results (tick 6000, --screenshot --ticks 12000 with half_speed_base)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4 (full)    | 30.7%  | 13     | yes (dies ~6668) |
| 137  | 11        | 12   | 1.09     | 12          | 17.9%  | 14     | yes |
| 777  | 4*        | 14   | 3.5      | 4 (full)*   | 37.9%  | 14     | yes |

*Seed 777 non-deterministic: diagnostics pop=4, report card pop=11 in same session (rand::rng() variance — 16th confirmed instance of same-session divergence).

### Report Card Summaries
- Seed 42: `Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low`
- Seed 137: `Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing`
- Seed 777: `Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing`

### Simulation Health (all systems active)
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|---------|
| pipe_water_total | 298.19 | 244.78 | 310.25 |
| wind_moisture_total | 17810 | 15944 | 40196 |
| avg_vegetation | 0.44 | 0.41 | 0.42 |
| avg_moisture | 0.46 | 0.32 | 0.64 |

All simulation systems active (values > 0). Note wind_moisture_total varies 2.5× across seeds despite similar moisture.

### Baseline Check
All 3 seeds: **PASS** (check_baselines.sh: 3 passed, 0 failed).

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13-14 biomes ✓, water 15-40% ✓, slopes 98%+ flat ✗, Desert 32-49% dominates |
| Settlement Viability (30%) | 2 | Seed 42 dies at tick 6668; food Critical 2/3 seeds; seed 42 at capacity with 0 growth |
| Simulation Health (20%) | 4 | All systems active with above-zero values; moisture chain still has known issues |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4 vs 11, wood 43 vs 4); 0 events; settlement patterns similar |
| **Weighted Average** | **2.9** | 16th consecutive plateau at 2.9 |

Calculation: 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 0.9 + 0.6 + 0.8 + 0.6 = **2.9**

## Harness Updates

Added **Check #28: rand::rng() count growth guard** to validate_features.sh.
- Warns if rand::rng() calls in hot-path files exceed baseline counts (systems.rs≤3, mod.rs≤3, ai.rs=0).
- Purpose: catch if the non-determinism problem is worsening without raising alarm on the already-documented baseline.
- Result on current code: **OK** (counts at baseline).

## features.json Updates

Updated `last_verified` for all systems to 2026-04-26.

Updated `ecs_core` known_issues: seed 42 stagnation now at 16 consecutive health checks (was 15).

## Priority Actions

1. **Fix seed 42 auto-build failure** (`ecs_core` known_issue): pop=4, food=12 for 16 consecutive checks. Housing full (cap=4) but auto-build never places second Hut despite wood=43 surplus. Diagnose `game/build.rs` hut-placement scoring — this is the longest-running single-seed stagnation and blocks Settlement Viability score from improving.

2. **Fix `construction_dust_particles_spawn` flaky test** (`game_loop` known_issue): has fired twice (Apr 13, Apr 15), not marked `#[ignore]`. One line fix: add `#[ignore]` annotation. Eliminates non-deterministic CI failures.

3. **Lower `ROAD_TRAFFIC_THRESHOLD`** (`events` known_issue): 300.0 is unreachable for pop=4–13 settlements. Diagnose what traffic accumulates at high-frequency tiles first (use `--diagnostics` traffic overlay), then pick a threshold ≤100 that fires. Roads are an anti-goal for manual placement but auto-build from traffic is core to the design.

4. **Diagnose biome distribution skew** (`terrain_generation` known_issue): Desert 32-49% in 2/3 seeds, Forest <0.4%. This prevents Geography-Shapes-Everything from feeling different across seeds. Check `terrain_pipeline.rs` biome assignment thresholds — compare against WorldState refactor to see if the 0D changes shifted biome boundaries.
