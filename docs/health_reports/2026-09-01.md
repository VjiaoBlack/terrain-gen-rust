# Daily Health Check — 2026-09-01

## Test Results
- Total: **777 passed, 1 failed, 10 ignored** (finished in 308s)
- Failure: `construction_dust_particles_spawn` — known flaky test (race on particle timing), documented in features.json but not `#[ignore]`d. 3 flaky tests in this family remain unignored.
- Test count accuracy: features.json `ecs_core.test_count=230` but actual `#[test]` annotations across ecs/ files is lower (tests extracted to `ecs/tests.rs` which is not listed in ecs_core.files[]). Overall 818 `#[test]` annotations in src/ — above 700 threshold per check #45.

## Critical Fix: features.json Invalid JSON
- **FIXED**: Line 261 in `ecs_core.known_issues` was missing a trailing comma, making the entire file invalid JSON.
- `./scripts/validate_features.sh` was reporting `FAIL: features.json is not valid JSON` before the fix.
- Added the missing comma between the `2026-08-31` entry and the `2026-08-10` entry.

## Design Drift
- **Pillar 1 (Geography Shapes Everything) still violated**: Seeds 137 (Desert=48.7%) and 777 (Desert=0.1%) produce identical report card summaries: "Population: Healthy | Food: Critical | Housing: Adequate | Terrain: Diverse | Threats: Growing". Different terrain, same outcome. Check #48 fires consistently.
- **Pillar 2 (Emergent Complexity) weak**: `VillagerMemory` is defined and written but never read in `ai.rs` for AI decisions. Villagers still use global stockpile counts (15 occurrences in ai.rs). No fix in sight.
- **All events_fired=0** in all 3 diagnostic seeds today (zero-event diagnostic run). WolfSurge is non-reproducible day-to-day due to rand::rng() non-determinism.
- **wood_skill=0.5487951708942683** (pure decay `1.0 * 0.9999^6000`) in all 3 seeds — zero woodcutting occurs at tick 6000 because Forest<1% in all evaluation seeds. Code is correct; terrain/biome imbalance is the root cause.
- **Fog-of-war non-functional**: `sight_range=22.0` + initial `reveal_radius=15` trivially reveals the full 70×25 map in ~200 ticks. Exploration mechanic has no game impact.
- **Road formation blocked**: `ROAD_TRAFFIC_THRESHOLD=300.0` prevents road formation at current population scales (pop=4–13). 0% Road coverage in all eval seeds at tick 6000.

## Architecture
- **No files over 3000 lines** (validate_features.sh check passes).
- **Files approaching threshold** (flagged in ARCHITECTURE.md, not yet split):
  - `terrain_pipeline.rs`: 2232 lines
  - `tilemap.rs`: 2515 lines
  - `game/build.rs`: 2306 lines
  - `ecs/systems.rs`: 2227 lines
  - `ecs/ai.rs`: 2104 lines
- `src/analytical_erosion.rs` still not documented in ARCHITECTURE.md (known issue).
- `src/game/fire.rs`, `water_cycle.rs`, `particles.rs` have 0 unit tests each.
- Three water systems still coexist: WaterMap, PipeWater, Terrain::Water. Cleanup not complete.

## Game Quality Evaluation

### Per-Seed Results (tick ~6000, --ticks 12000 with half_speed_base)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4           | 30.7%  | 13     | Yes      |
| 137  | 11        | 12   | 1.09     | 12          | 17.9%  | 14     | Yes      |
| 777  | 11        | 16   | 1.45     | 20          | 37.9%  | 14     | Yes      |

Simulation values (all seeds): pipe_water > 244, wind_moisture > 16K, avg_vegetation ~0.42–0.44. All simulation systems active.

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight | Housing: Overcrowded | Terrain: Diverse | Threats: Low
- Seed 137: Population: Healthy | Food: Critical | Housing: Tight | Terrain: Diverse | Threats: Growing
- Seed 777: Population: Healthy | Food: Critical | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13–14 biomes ✓; water 18–38% ✓; but slope 98.4–98.7% flat (cliff=0%, steep=0%) — "Geography Shapes Everything" requires elevation variety |
| Settlement Viability (30%) | 2 | Seed 42 stuck at pop=4 (diagnostics; baseline=6); seeds 137/777 Critical food (fpc=1.09–1.45); seed 42 growth_potential=0; only seed 777 has room to grow |
| Simulation Health (20%) | 5 | All systems active: pipe_water=244–305, wind_moisture=16K–41K, avg_veg=0.42–0.44 |
| Emergent Behavior (20%) | 1 | All 3 seeds events_fired=0 today; seeds 137/777 fully convergent outcomes despite radically different terrain; no events, no divergence |
| **Weighted Average** | **2.7** | terrain×0.3=0.9 + settlement×0.3=0.6 + simulation×0.2=1.0 + emergent×0.2=0.2 |

### Baseline Check
- **3/3 PASS**: seed 42 pop=6, seed 137 pop=11, seed 777 pop=11 at tick 12000

## Harness Updates
- No new checks added. validate_features.sh: 0 errors, 43 warnings.
- CRITICAL FIX: features.json missing comma repaired — validate_features.sh now passes JSON parse check.

## features.json Updates
- Fixed invalid JSON (missing comma at line 261 in ecs_core.known_issues).
- Added today's diagnostic entry to ecs_core.known_issues (see below).
- Updated `last_verified` for ecs_core to 2026-09-01.

## Harness Review (Monthly — 2026-09-01)

**CLAUDE.md staleness**: CLAUDE.md claims `~190 lib tests` but `cargo test --lib` shows 777–778 passed. This is 4× stale. Known issue in features.json but CLAUDE.md has not been updated. Low priority (does not affect tooling), but misleads new contributors.

**workflow.md vs reality**: Not audited this pass (no new divergence detected from prior month).

**validate_features.sh checks that may not fire**: Check #33 (tick_config chain), check #27 (drought threshold), checks for biome imbalance — these fire consistently. Check #60 (seek_pct > 80%) fires in ~50% of runs depending on RNG. Check #55 (flaky test alert) should fire since all 3 flaky tests are unignored — worth verifying it fires consistently.

**No obsolete rules in CLAUDE.md** detected — all rules remain applicable.

## Priority Actions
1. **Fix rand::rng() non-determinism** (game/build.rs:631, ecs/systems.rs, game/mod.rs) — root cause of score plateau at 2.4–2.9. Replace `rand::rng()` with seeded per-entity RNG. This single fix would stabilize settlement viability and emergent behavior scores.
2. **Balance biome distribution** — Desert at 49% in seed 137 with Forest <1% across all seeds. Woodcutting never occurs; wood skill permanently at pure-decay. Fix biome thresholds in terrain_pipeline.rs to ensure Forest ≥ 5% in at least 2 evaluation seeds.
3. **Fix Granary food-depletion interaction** — FoodToGrain threshold `food > 15` (systems.rs:979, 1391) too low for pop ≥ 10. Raises to `max(15, population * 2)` per prior analysis. Multiple total-starvation events documented.
