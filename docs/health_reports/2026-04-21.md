# Daily Health Check — 2026-04-21

## Test Results
- **778 passed, 0 failed, 10 ignored** — `cargo test --lib` passes cleanly
- Test count accuracy: all 25 systems within tolerance per `validate_features.sh`
- Build: 11 compiler warnings (dead_code, unused_variables), no errors
- 818 total `#[test]` annotations in src/ — CLAUDE.md estimate of ~190 is 4× stale (known)

## Design Drift

No new drift detected today. All previously identified drifts persist (unchanged since 2026-04-17):

1. **Pillar 2 — VillagerMemory not connected to AI.** `VillagerMemory` written in `ecs/systems.rs` (24 refs), 0 references in `ecs/ai.rs`. Villagers still query global stockpile in ~15 places. Check #22 guards this.
2. **Pillar 1 — Road auto-build never triggers.** Zero Road coverage in all 3 seeds. `ROAD_TRAFFIC_THRESHOLD=300.0` unreachable by 4–11 villager populations.
3. **Pillar 3 — Fog-of-war trivially complete.** `sight_range=25.0` (`spawn.rs`) + reveal_radius=15 (`game/mod.rs:1369`) fully reveal 70×25 map in ~200 ticks. `exploration_pct=100.0` all seeds.
4. **Drought event structurally impossible.** Grain threshold (`grain >= pop*5`) never met. 0/3 seeds trigger Drought. WolfSurge fires on 1 seed (non-deterministic which seed).
5. **Desert biome dominance post-WorldState refactor.** Seeds 42/137: Desert 32%/48.7%. Mountain 0% all seeds. Forest < 1% all seeds.

## Architecture

No files exceed 3000 lines (non-test). All file sizes stable vs 2026-04-20:

| File | Lines | Trend |
|------|-------|-------|
| `src/game/mod.rs` | 2592 | Stable |
| `src/tilemap.rs` | 2515 | Stable |
| `src/game/build.rs` | 2306 | Stable |
| `src/terrain_pipeline.rs` | 2232 | Stable |
| `src/ecs/systems.rs` | 2227 | Stable |
| `src/ecs/ai.rs` | 2104 | Stable |
| `src/simulation/moisture.rs` | 1867 | Stable |

`src/hydrology.rs` still absent from ARCHITECTURE.md file map (known). Three water systems still coexist (WaterMap, PipeWater, Terrain::Water — known, WorldState refactor ongoing).

## Game Quality Evaluation

### Per-Seed Results (tick 6000 via `--diagnostics`)
| Seed | Pop | Food | Food/Cap | Hut Cap | Water% | Biomes | Survived to 6K |
|------|-----|------|----------|---------|--------|--------|----------------|
| 42   | 4   | 12   | 3.0      | 4       | 30.7%  | 13     | Yes (dies ~6668 in `--play` run) |
| 137  | 11  | 14   | 1.3      | 20      | 17.9%  | 14     | Yes |
| 777  | 11  | 15   | 1.3      | 20      | 38.0%  | 15     | Yes (WolfSurge active) |

**Non-determinism observation:** WolfSurge fired on seed **777** today vs seed **137** yesterday. Seeds 137/777 food_per_cap varied 1.0–1.4 across runs. Report card shows seed 137 housing capacity=12 while diagnostics (separate run) shows hut_capacity=20 — both are `Hut:5` runs but different build outcomes due to `rand::rng()`. Check #15 guards this.

**Seed 42 playtest confirms death:** `--play` run ends at tick 6668 — "All villagers have perished. Peak population: 4." Stockpile had 15 food, 43 wood, 9 stone. Death mechanism unclear (plague? starvation spike? overcrowding disease?). Diagnosing this is Priority 1.

### Report Card Summaries
- **Seed 42:** Population: Fragile | Food: Tight (3.0/cap) | Housing: Overcrowded (growth_potential=0) | Terrain: Diverse | Threats: Low
- **Seed 137:** Population: Healthy | Food: Critical (1.3/cap) | Housing: Tight | Terrain: Diverse | Threats: Growing
- **Seed 777:** Population: Healthy | Food: Critical (1.3/cap) | Housing: Adequate | Terrain: Diverse | Threats: Growing (WolfSurge)

### Simulation Vitals
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|----------|
| pipe_water_total | 298.19 | 244.62 | 313.41 |
| wind_moisture_total | 17,810 | 16,602 | 41,502 |
| avg_vegetation | 0.44 | 0.42 | 0.42 |
| flat_pct | 98.4% | 98.7% | 98.6% |
| Desert biome | 32.0% | 48.7% | 0.1% |
| Forest biome | 0.3% | 0.1% | 0.6% |
| Mountain biome | 0.0% | 0.0% | 0.0% |

### Rubric Scores
| Category | Score (1–5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | **3** | 13–15 biome types ✓; water 17.9–38.0% ✓; but flat_pct 98.4–98.7% (near-zero slopes); Mountain 0% all seeds; Forest < 1%; Desert 32–49% in 2/3 seeds |
| Settlement Viability (30%) | **2** | All survive tick 6000, seed 42 dies at 6668. Food Critical (1.3/cap) in 2/3 seeds — 4/5 recent checks show this. Seed 42 growth_potential=0. |
| Simulation Health (20%) | **4** | All systems active: pipe_water > 0, wind_moisture > 0, avg_vegetation > 0.1. Wind moisture varies 2.5× between seeds (likely moisture chain incomplete). |
| Emergent Behavior (20%) | **3** | Seeds diverge meaningfully (pop 4 vs 11, Desert 32% vs 0.1%). WolfSurge on 1/3 seeds. Road/Bakery/Smithy never built. 0 Drought. |
| **Weighted Average** | **2.9** | 10th consecutive plateau — no improvement since 2026-04-11 |

### Baseline Check
```
PASS: seed 137 (pop=11, water=17.9%, biomes=14)
PASS: seed 42  (pop=4,  water=30.9%, biomes=13)
PASS: seed 777 (pop=11, water=37.9%, biomes=14)
Results: 3 passed, 0 failed
```

## Harness Updates

**Added check #23: Persistent food crisis detector (`validate_features.sh`)**

Counts how many of the last 5 `metrics_history.json` entries show `food_per_cap < 2.0` in 2+ of 3 seeds. Fires if ≥ 3 of 5 entries match. Uses majority-based detection rather than strictly consecutive to be robust against non-deterministic single-run outliers. Fires immediately on current data (4/5 recent entries in crisis).

`validate_features.sh` now has **0 errors, 10 warnings** (checks 1–23):
- Persistent: save_load no tests, flaky test not #[ignore]d, rand::rng() in 2 hot paths, plateau at 2.9, world_state no tests, FoW trivial, VillagerMemory not in ai.rs
- **New**: food_per_cap chronic crisis (4/5 recent entries)

## features.json Updates
- Updated all `last_verified` dates to `2026-04-21`

## Priority Actions

1. **[HIGHEST — breaks plateau] Diagnose seed 42 death mechanism.** Seed 42 dies at tick 6668 despite 15 food, 43 wood, 9 stone in stockpile. Before changing anything: add a diagnostic that prints tick-by-tick population + food + hunger_events + death_cause for seed 42, ticks 5000–7000. Check `game/events.rs` for plague (`grain < pop`), `ecs/systems.rs` for death threshold. Settlement Viability score stuck at 2 until this is fixed.

2. **[HIGH — breaks plateau] Food per capita chronically Critical in 2/3 seeds.** 4/5 recent health checks show food_per_cap < 2.0 for seeds 137/777 (new check #23). Write a diagnostic showing actual farm harvest ticks vs villager eat ticks for seed 137 over ticks 3000–6000 before touching any constant.

3. **[MEDIUM] Mark `construction_dust_particles_spawn` as `#[ignore]`.** Documented as flaky since 2026-04-13 (8 days). 5-minute fix that eliminates false CI failures. `src/game/tests.rs` — add `#[ignore]` above `fn construction_dust_particles_spawn`.

4. **[MEDIUM] Road auto-build threshold calibration.** ROAD_TRAFFIC_THRESHOLD=300.0 never triggers for 4–11 villagers. Add one-line diagnostic printing peak traffic values at common paths (stockpile↔farm, stockpile↔forest) for any seed, then set threshold to 80% of that observed peak.

5. **[LOW] Replace rand::rng() in hot paths with seeded per-entity RNG.** `src/ecs/systems.rs` (3 occurrences) and `src/game/mod.rs` (3 occurrences) cause non-determinism. Manifests as WolfSurge target shifting between runs, food_per_cap varying 1.0–3.5 for the same seed. Blocks reproducible debugging of priority actions 1 and 2.
