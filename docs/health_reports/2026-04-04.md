# Daily Health Check — 2026-04-04

## Test Results
- **Total: 768 passed, 0 failed, 9 ignored** (finished in 28.31s)
- All systems green. `cargo test --lib` exits 0.

### Test Count Accuracy vs features.json
| System | features.json | Actual grep |
|--------|--------------|-------------|
| ecs_core | 230 | 230 (src/ecs/tests.rs) — matches |
| game_loop | 107 | 107 (src/game/tests.rs) — matches |
| simulation_moisture | 17 | 18 — off by 1 |
| simulation_maps | 20 | 20 — matches |
| rendering | 62 | 61 (render/mod.rs only has 61) — off by 1 |
| terrain_generation | 25 | 6 (terrain_gen.rs) + 17 (terrain_pipeline.rs) = 23 — off by 2 |
| pathfinding | 40 | 12+10+18 = 40 — matches |
| fire/water_cycle/particles | — | 0 each — no tests for these new modules |

## Design Drift

- **Pillar 4 render split is complete** — `src/game/render/` submodules exist (normal, map, landscape, overlays, debug, query, shared). ARCHITECTURE.md still lists `render.rs` as a 4094-line monolith — it has been split. Update needed.
- **game/mod.rs split is partially done** — fire.rs (231), water_cycle.rs (204), particles.rs (227), input.rs (258) exist as separate files. ARCHITECTURE.md Priority 2 split is largely implemented. game/mod.rs is 2379 lines (was expected ~6073). Update needed.
- **simulation.rs split is complete** — `src/simulation/` submodule with 10 files exists. ARCHITECTURE.md Priority 1 showing simulation.rs at 4844 lines is stale. Update needed.
- **Seed 137 playtest shows mostly water** — settlement spawned in near-ocean terrain. Pop 12 at tick 12101 with 44 grain but 5 wood and 0 planks. No buildings visible near camera. May be camera placement or genuine placement issue. Seed 42 shows healthy settlement at pop 8 with stone discovered. Both seeds differ visually, partially satisfying Pillar 1 goal.
- **Anti-goal check**: No manual road placement, no manual villager assignment, no tech tree UI found in build.rs or input.rs — design pillars respected.

## Architecture

- **ecs/tests.rs at 7164 lines** — flagged in features.json, not yet split. Priority 3 from ARCHITECTURE.md.
- **simulation/moisture.rs at 1867 lines** — largest simulation module, largest in simulation/ dir. Not flagged in ARCHITECTURE.md.
- **game/render/mod.rs at 1300 lines + shared.rs at 864 lines** — render split happened but mod.rs and shared.rs are still substantial. render/overlays.rs at 384, render/normal.rs at 457 — reasonable.
- **Render total: 5074 lines across 8 files** — split done but mod.rs is still large.
- **fire.rs, water_cycle.rs, particles.rs have 0 tests** — new modules from the game/mod.rs split lack dedicated tests.
- **ARCHITECTURE.md is significantly stale** — 4 of 5 refactor priorities have been partially or fully executed but docs still show old line counts and structure.

## features.json Updates
- `game_loop.known_issues[0]`: game/mod.rs split has been done — mod.rs is 2379 lines, not 6073. Issue text should reflect current reality (mod.rs could still be trimmed but split is done).
- `rendering.files`: references `src/game/render/` (array entry) which is now a directory — this is fine.
- All `last_verified` fields set to "2026-04-04" for checked systems.
- New known_issue for `game_loop`: fire.rs, water_cycle.rs, particles.rs have 0 tests.
- New known_issue for `simulation_moisture`: moisture.rs at 1867 lines — candidate for split.

## Priority Actions
1. **Update ARCHITECTURE.md** — file map is significantly stale (simulation split done, game split done, render split done). Wrong line counts mislead future agents.
2. **Add tests for fire.rs, water_cycle.rs, particles.rs** — three new modules with 0 tests each. Any change to these is unverified.
3. **Split ecs/tests.rs** (7164 lines) — Priority 3 from ARCHITECTURE.md, still pending. Most impactful remaining structural issue.
