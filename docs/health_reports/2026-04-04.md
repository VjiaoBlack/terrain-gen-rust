# Daily Health Check — 2026-04-04

## Test Results
- **768 passed, 0 failed, 9 ignored** (finished in ~19s)
- All 9 ignored tests are intentional diagnostic tests in `simulation/moisture.rs` (lines 708–1754), documented with run instructions — not dead tests
- CLAUDE.md says "~190 tests" but actual count is 768 — documentation significantly outdated

## Test Count Breakdown (actual #[test] markers)
| File | Tests |
|------|-------|
| src/ecs/tests.rs | 230 |
| src/game/tests.rs | 107 |
| src/tilemap.rs | 74 |
| src/game/render/mod.rs | 61 |
| src/ecs/groups.rs | 32 |
| src/simulation/wind.rs | 22 |
| src/simulation/day_night.rs | 21 |
| src/pipe_water.rs | 21 |
| src/simulation/maps.rs | 20 |
| src/main.rs | 20 |
| src/simulation/moisture.rs | 18 (9 active + 9 ignored) |
| *(and ~120 more spread across 18 other files)* | |

## Design Drift
- **Seed 137 is a water world, not a desert.** `game_design.md:104` uses seed 137 as the "desert" example opposite seed 42 (grassland). Playtest at tick 12101 shows ~90% water tiles with pop=12 in a tiny land patch. Design doc example no longer matches actual terrain gen output.
- **Population seems low for 12K ticks.** Default seed pop=9 at tick 12101. Design doc Phase 1 targets describe a "living" settlement but no explicit pop benchmark is defined. This may be expected with auto-build on a small starting footprint.
- No drift found in building costs, movement speeds, or A* cost values vs CLAUDE.md tables.
- Anti-goals hold: no manual road placement exposed, no per-villager task UI visible in play output.

## Architecture

### Features already implemented that ARCHITECTURE.md still marks as TODO:
ARCHITECTURE.md's four refactor priorities are largely **complete**. The file is significantly stale:

| ARCHITECTURE.md Priority | Actual State |
|--------------------------|-------------|
| Priority 1: Split simulation.rs (4844 lines) | **Done.** `src/simulation/` with 10 files (mod.rs, water_map.rs, moisture.rs, vegetation.rs, day_night.rs, wind.rs, scent.rs, traffic.rs, soil_fertility.rs, maps.rs) |
| Priority 2: Split game/mod.rs (6073 lines) | **Done.** Extracted to input.rs (258L), water_cycle.rs (204L), fire.rs (231L), particles.rs (227L), tests.rs (2913L). game/mod.rs now 2379 lines |
| Priority 3: Split ecs/mod.rs tests | **Done.** `src/ecs/tests.rs` (7164 lines) now holds the 230 tests |
| Priority 4: Split game/render.rs (4094 lines) | **Done.** `src/game/render/` with 8 files: mod.rs (1300L), normal.rs (457L), map.rs (301L), landscape.rs (406L), overlays.rs (384L), query.rs (327L), debug.rs (115L), shared.rs (864L) |

### Files over 2000 lines (remaining concerns):
| File | Lines | Status |
|------|-------|--------|
| src/ecs/tests.rs | 7164 | Tests-only file, coherent. Consider splitting to tests/ dir. |
| src/game/tests.rs | 2913 | Tests-only file, coherent. |
| src/game/mod.rs | 2379 | Down from 6073, still getting big |
| src/tilemap.rs | 2515 | Unchanged from ARCHITECTURE.md's "getting big" warning |
| src/game/build.rs | 2306 | Unchanged from ARCHITECTURE.md |
| src/ecs/systems.rs | 2227 | Unchanged |
| src/ecs/ai.rs | 2104 | Unchanged |
| src/simulation/moisture.rs | 1867 | New concern, not in ARCHITECTURE.md |

### New files not documented in ARCHITECTURE.md:
- `src/analytical_erosion.rs` (412 lines) — SPL (stream power law) erosion, 6 tests, used in terrain pipeline
- `src/scripting/mod.rs` (158 lines) — Lua scripting via `mlua` crate, feature-gated, 5 tests
- `src/game/input.rs`, `src/game/fire.rs`, `src/game/water_cycle.rs`, `src/game/particles.rs` — from Priority 2 split, not yet added to ARCHITECTURE.md's file table
- `src/game/chokepoint.rs` — already listed correctly in ARCHITECTURE.md

### Data flow issues (still valid from ARCHITECTURE.md):
- Three water systems (WaterMap, PipeWater, Terrain::Water) still coexist — `src/simulation/water_map.rs` (460L) is still present
- Moisture chain correctness unclear — 9 diagnostic tests are `#[ignore]`d suggesting the moisture system is under active investigation

## features.json Updates
- **features.json does not exist.** The health check prompt references it but the file is absent from the repository. Should be created.

## Priority Actions
1. **Update ARCHITECTURE.md** — the four "refactor priorities" are done. Replace with accurate current file map. Add new files (analytical_erosion.rs, scripting/mod.rs). Update line counts. This is misleading for agents reading it.
2. **Create features.json** — the health check system expects it; its absence means system status is untracked across sessions.
3. **Investigate seed 137 water coverage** — if the "desert seed 137" example in game_design.md is now a water world, either update the example seed in the docs or investigate whether the terrain pipeline regression changed how that seed generates.
4. **Resolve three-water-system confusion** — WaterMap (water_map.rs) is still running; ARCHITECTURE.md says it should be removed once PipeWater takes over. Verify whether it's still needed for moisture seeding.
5. **Update CLAUDE.md test count** — "~190 tests" is wrong; actual is 768. Minor but causes confusion.
