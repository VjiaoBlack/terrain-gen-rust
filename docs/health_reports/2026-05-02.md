# Daily Health Check — 2026-05-02

## Test Results
- **778 passed, 0 failed, 10 ignored** (`cargo test --lib`, 474s)
- 818 `#[test]` annotations total in `src/`
- Test count accuracy: features.json values within tolerance — no discrepancies found
- CLAUDE.md still claims ~190 tests (4× stale); check #29 tracks this

## Design Drift

**Pillar 1 — Geography Shapes Everything (ACTIVE VIOLATION)**
- slope_flat_pct = 98.4–98.7% across all 3 eval seeds; elevation_std = 0.09–0.11
- Zero cliff, zero steep slope in any seed at tick 6000
- Two-thirds of seeds (42, 137) are desert-dominated (32%, 47.5%); forest ≤ 0.7% everywhere
- Design doc requires "hills, mountain passes, varied topography to create strategic constraints"; current terrain is geologically indistinct (`terrain_pipeline.rs`, `src/terrain_gen.rs`)

**Pillar 2 — Emergent Complexity (ACTIVE VIOLATION)**
- `VillagerMemory` defined in `ecs/components.rs`, written in `ecs/systems.rs`, never read in `ecs/ai.rs` (check #22)
- Villagers still query global stockpile counts (~15 occurrences in `ai.rs`); per-villager knowledge architecture not started
- `tick_config` event modifier chain is dead code: drought/harvest `rain_rate` multipliers built in `game/mod.rs:2135–2148` but `step_water_cycle` signature is `(bool, f64)` and never receives `tick_config` (check #33)

**Pillar 3 — Explore → Expand → Exploit → Endure (PARTIAL VIOLATION)**
- 0 events in seeds 42/137 this run; seed 777 fired WolfSurge (non-deterministic — prior run showed 0)
- Drought/Harvest structurally impossible: grain threshold `>= pop * 5` never met at tick 6000 (check #27)
- No outposts, no roads, building set identical across all 3 seeds — no phase arc visible

**Fog-of-War (ACTIVE VIOLATION)**
- sight_range=25.0 + initial reveal radius=15 → exploration_pct=100.0 in all 3 seeds (checks #21, simulation_maps.known_issues)
- Exploration mechanic is non-functional as a game constraint

No new drift found beyond prior entries.

## Architecture

- **No non-test files exceed 3000 lines** (validate_features.sh check #5 passes)
- Files approaching split threshold: `tilemap.rs` (2515 lines, Priority 5 open), `game/build.rs` (2306), `terrain_pipeline.rs` (2232), `ecs/systems.rs` (2227), `ecs/ai.rs` (2104)
- `moisture.rs` (1867) and `wind.rs` (1540) flagged "Getting big" in ARCHITECTURE.md; not at split threshold
- **CLAUDE.md module structure is significantly stale**: does not list `world_state.rs`, `game/input.rs`, `game/water_cycle.rs`, `game/fire.rs`, `game/particles.rs`, or `src/simulation/` subdirectory. Misleads new contributors.
- ARCHITECTURE.md itself matches actual file structure.
- 11 compiler warnings (game/mod.rs known_issues logged 12 — marginal improvement or different count scope)

## Game Quality Evaluation

### Per-Seed Results
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4 (capped)  | 30.7%  | 14     | Yes      |
| 137  | 11        | 15   | 1.36     | 24          | 17.8%  | 14     | Yes      |
| 777  | 11        | 13   | 1.18     | 16          | 37.9%  | 14     | Yes      |

### Report Card Summaries
- Seed 42: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse | Threats: Low
- Seed 137: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing
- Seed 777: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14 biomes ✓, water 18–38% ✓; but 98%+ flat terrain, desert-dominant in 2/3 seeds, forest ≤ 0.7% everywhere |
| Settlement Viability (30%) | 2 | All 3 survive; avg pop 8.7; food critical in 2/3 seeds (f/cap < 1.4); seed 42 stuck at pop=4 for 22 consecutive runs |
| Simulation Health (20%) | 4 | pipe_water_total 245–305 ✓, wind_moisture 17K–41K ✓, avg_vegetation 0.42–0.44 ✓, avg_moisture 0.34–0.64 ✓ |
| Emergent Behavior (20%) | 2 | Terrain/pop diverges meaningfully; 1 WolfSurge (seed 777, non-deterministic); no roads, no outposts, no drought/harvest, identical building types all seeds |
| **Weighted Average** | **2.7** | `3×0.30 + 2×0.30 + 4×0.20 + 2×0.20 = 2.7` |

### Baseline Check
- Seed 42: **PASS** (pop=4, water=30.7%, biomes=14)
- Seed 137: **PASS** (pop=11, water=17.9%, biomes=14)
- Seed 777: **PASS** (pop=11, water=37.9%, biomes=14)
- All 3 baselines pass (`scripts/check_baselines.sh`)

### Ecosystem Change from Yesterday
- Seed 42: rabbits=0 (unchanged, extinction)
- Seed 137: rabbits=2 (recovered from 0 on 2026-05-01 — non-deterministic)
- Seed 777: rabbits=7 (down from 15 on 2026-05-01, still viable)
- Prey extinction check (#31): **does not fire today** (only 1/3 seeds extinct, threshold is 2)

## Harness Updates

Added **check #34**: WorldState 0D architecture regression guard.
- Verifies that `WorldState` is still referenced in `src/game/mod.rs`
- Catches accidental revert of the 0D architecture refactor (Stages 1–4)
- Fast, deterministic, zero build cost
- Confirmed fires correctly on current code (OK: WorldState referenced)

## Harness Hygiene (2026-05 monthly review — 2nd of month)

**CLAUDE.md:**
- Module structure section is significantly out of date: missing `world_state.rs`, `game/input.rs`, `game/water_cycle.rs`, `game/fire.rs`, `game/particles.rs`, and the `src/simulation/` directory split. Claims "~190 tests" (actual: 818).
- Should be updated by a human or harness agent — it's codebase documentation, not design docs.

**docs/workflow.md:**
- Agent model references (Opus/Sonnet) may be outdated but workflow steps are accurate.
- No obsolete rules found.

**validate_features.sh:**
- All 33 checks firing correctly or SKIPping when data is absent
- No checks that "never fire" — all are useful given observed issues
- Check #31 (prey extinction) correctly did NOT fire today (1/3 seeds, threshold is 2) — sensitivity appropriate
- Score plateau check (#17) not firing: recent scores span 2.7–2.9 (range 0.20 > threshold 0.10)

## features.json Updates
- Updated `last_verified` to `2026-05-02` for all 25 systems
- Updated `ecs_core` known_issue: seed 42 pop=4 food=12 count updated to 22 consecutive runs
- Updated `events` known_issue: seed 777 WolfSurge fired in 2026-05-02 run (non-deterministic)
- No status changes — all system statuses remain accurate

## Priority Actions
1. **Flat terrain (Terrain Quality: 3 → target 4)** — slope_flat_pct=98%+ across all seeds violates Pillar 1. Diagnose `terrain_pipeline.rs` fBm amplitude and erosion output. Write diagnostic test showing elevation_std before/after each pipeline stage before touching constants.
2. **Food system (Settlement Viability: 2 → target 3)** — food_per_cap < 2.0 in 2/3 seeds for 4 of last 5 health checks. Farm yield vs hunger rate needs diagnostic test (see BACKLOG.md). Root cause may be farm count insufficient for pop=11 with only 1–3 farms.
3. **Seed 42 structural failure (Settlement Viability)** — pop=4, housing_growth_potential=0 for 22 consecutive runs. Auto-build never places second Hut despite wood surplus (wood=43). Diagnose Hut placement scoring in `game/build.rs`. Non-determinism in `rand::rng()` means some runs DO place it (pop=6 seen on 2026-04-28) — investigate RNG dependency in scoring.
4. **Event calibration (Emergent Behavior: 2)** — fix tick_config event modifier chain so drought/harvest actually affect simulation (game/mod.rs:2135–2148 vs water_cycle.rs:9). Also: drought threshold `grain >= pop * 5` is unreachable in evaluation window; reduce to `grain >= pop * 2`.
