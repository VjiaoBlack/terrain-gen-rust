# Daily Health Check — 2026-04-20

## Test Results
- **778 passed, 0 failed, 10 ignored** — `cargo test --lib` passes cleanly (237s)
- Test count accuracy: features.json within tolerance for all systems
- Build: 11 compiler warnings (dead_code, unused_variables), no errors
- 818 total `#[test]` annotations in src/ — CLAUDE.md estimate of ~190 is 4× stale (known, unfixed)

## Design Drift

All drifts from prior checks persist (unchanged since 2026-04-17):

1. **Pillar 2 — VillagerMemory not connected to AI decisions.** `VillagerMemory` defined in `ecs/components.rs` (6 refs), written in `ecs/systems.rs` (24 refs), but has **0 references in `ecs/ai.rs`**. Villagers still query global stockpile counts in ~15 places in ai.rs. New: check #22 now detects this gap automatically.

2. **Pillar 1 — Road auto-build never triggers.** Zero Road coverage in all 3 seeds at tick 6000. `ROAD_TRAFFIC_THRESHOLD=300.0` is unreachable by 4–11 villager populations. Design anti-goal "no manual road placement" is not met.

3. **Pillar 3 — Fog-of-war trivially complete.** `sight_range=25.0` (`spawn.rs:137`, up from 22.0 noted in prior reports) + initial reveal radius=15 (`game/mod.rs:1369`) fully reveal a 70×25 map in ~200 ticks. `exploration_pct=100.0` in all seeds. Checks #21 and #22 now warn on these values.

4. **Drought event structurally impossible.** Grain threshold (`grain >= pop*5`) never met at tick 6000. 0/3 seeds trigger Drought. WolfSurge fires in seed 137 only.

5. **Desert biome dominance.** Seeds 42 and 137: Desert 32% and 49%. Mountain 0% in all seeds. Forest < 1% in all seeds. Known issue since 2026-04-17 post-WorldState refactor.

## Architecture

No files exceed 3000 lines (non-test). Files approaching threshold (unchanged from 2026-04-19):

| File | Lines | Trend |
|------|-------|-------|
| `src/game/mod.rs` | 2592 | Stable |
| `src/tilemap.rs` | 2515 | Stable |
| `src/game/build.rs` | 2306 | Stable |
| `src/terrain_pipeline.rs` | 2232 | Stable |
| `src/ecs/systems.rs` | 2227 | Stable |
| `src/ecs/ai.rs` | 2104 | Stable |

`src/hydrology.rs` still absent from ARCHITECTURE.md file map (known). Three water systems still coexist (known).

## Game Quality Evaluation

### Per-Seed Results (tick 6000 via `--diagnostics`)
| Seed | Pop | Food | Food/Cap | Hut Cap | Water% | Biomes | Survived to 6K |
|------|-----|------|----------|---------|--------|--------|----------------|
| 42   | 4   | 12   | 3.0      | 4       | 30.7%  | 13     | Yes (dies ~6668 in playtest) |
| 137  | 11  | 11   | 1.0      | 12      | 17.9%  | 13     | Yes |
| 777  | 11  | 15   | 1.4      | 12      | 37.9%  | 15     | Yes |

**New observation:** Seed 42 shows GAME OVER at tick 6668 in a live `--play` run with auto-build enabled. "All villagers have perished. Survived to Y1 Spring D6. Peak population: 4." The tick-6000 diagnostic passes baseline check (pop>0) but the settlement cannot sustain itself. This is a qualitative regression not detected by baselines.

**Non-determinism confirmed:** Seed 777 population varied 4–13 across three runs on 2026-04-19 (rand::rng() in systems.rs + game/mod.rs). Today's diagnostics run shows 11, report-card run shows 13. Check #15 (RNG determinism) fires for both files.

### Report Card Summaries
- **Seed 42:** Fragile | Food: Tight (3.8/cap) | Housing: Overcrowded | Terrain: Diverse | Threats: Low
- **Seed 137:** Healthy | Food: Critical (1.4/cap) | Housing: Tight | Terrain: Diverse | Threats: Growing (WolfSurge active)
- **Seed 777:** Healthy | Food: Critical (1.4/cap) | Housing: Tight | Terrain: Diverse | Threats: Growing

### Simulation Vitals
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|----------|
| pipe_water_total | 298.19 | 244.78 | 306.16 |
| wind_moisture_total | 17,810 | 17,344 | 41,159 |
| avg_vegetation | 0.44 | 0.42 | 0.42 |
| flat_pct | 98.4% | 98.7% | 98.6% |
| Desert biome | 32.0% | 49.1% | 0.1% |
| Forest biome | 0.3% | 0.1% | 0.7% |
| Mountain biome | 0.0% | 0.0% | 0.0% |

### Rubric Scores
| Category | Score (1–5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | **3** | 13–15 biome types ✓; water 18–38% ✓; flat_pct 98.4–98.7% (near-zero slope); no Mountains; Forest < 1%; Desert 32–49% in 2/3 seeds |
| Settlement Viability (30%) | **2** | All survive tick 6000, but seed 42 dies at ~6668. Food Critical in 2/3 seeds (per_cap 1.0–1.4). Seed 42 growth_potential=0, stuck at pop=4. |
| Simulation Health (20%) | **4** | All simulation systems active (water, wind, vegetation). Wind moisture seed-variant (777 = 2.4× others) but not broken. |
| Emergent Behavior (20%) | **3** | Seed 42 vs 777 diverge (pop 4 vs 11, Desert 32% vs 0.1%). WolfSurge in seed 137 only. Road/Bakery/Smithy never built. |
| **Weighted Average** | **2.9** | 9th consecutive plateau — no improvement since 2026-04-11 |

### Baseline Check
```
PASS: seed 137 (pop=11, water=17.9%, biomes=14)
PASS: seed 42  (pop=4,  water=30.7%, biomes=13)
PASS: seed 777 (pop=11, water=38.0%, biomes=14)
Results: 3 passed, 0 failed
```

## Harness Updates

**Added check #22: Pillar 2 gap — VillagerMemory in AI (`validate_features.sh`)**

Detects if `VillagerMemory` is absent from `src/ecs/ai.rs`, flagging that per-villager memory (Pillar 2 core feature) is defined and written but not used for AI decisions. Fires as WARN (does not fail CI). Passes on current code.

`validate_features.sh` now has **0 errors, 9 warnings** (checks 1–22):
- Persistent: save_load no tests, flaky test not #[ignore]d, rand::rng() in 2 hot paths, plateau at 2.9, world_state no tests, FoW trivial (sight=25, reveal=15), VillagerMemory not in ai.rs
- Score plateau now at 9 consecutive entries — highest priority action

## features.json Updates
- Updated all `last_verified` to `2026-04-20`
- Added known_issue to `ecs_core`: "Seed 42 dies at tick ~6668 in playtest (pop=4 at tick 6000 is Fragile and does not survive to 12K ticks)"

## Priority Actions

1. **[HIGHEST] Diagnose seed 42 early death — Settlement Viability = 2.** Seed 42 dies at tick ~6668 with pop=4 despite 12 food, 43 wood, 9 stone in stockpile. Before changing anything, write a diagnostic test tracing population + food at ticks 3000/6000/9000 for seed 42 to find whether death is from starvation, predators, housing, or disease. Check `game/events.rs` plague triggers.

2. **[HIGH] Food per-capita Critical in 2/3 seeds.** Seeds 137/777 at 1.0–1.4 food/cap (starvation threshold). Farms are built (2 each) but yield is insufficient. Write a diagnostic showing actual farm harvest rate per tick vs villager hunger rate before adjusting any constant.

3. **[MEDIUM] Mark `construction_dust_particles_spawn` as `#[ignore]`.** This flaky test has been flagged in every health check since 2026-04-13 (7 days). It takes 5 minutes to fix and stops false CI failures. No diagnosis needed — the cause is documented (`game_loop.known_issues`).

4. **[MEDIUM] Road auto-build calibration.** ROAD_TRAFFIC_THRESHOLD=300.0 never reached by 4–11 villagers. Add a diagnostic showing actual peak traffic values for common paths, then set threshold to 70–80% of that value.

5. **[LOW] Replace rand::rng() in hot paths.** `systems.rs` (3 occurrences) and `game/mod.rs` (3 occurrences) cause non-determinism — seed 777 population varied from 4 to 13 in different runs on 2026-04-19. Blocking reproducible debugging.
