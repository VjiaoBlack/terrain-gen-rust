# Daily Health Check — 2026-04-28

## Test Results
- **778 passed, 0 failed, 10 ignored** (201.5s) — 18th consecutive identical result
- 818 `#[test]` annotations in src/ (above the ≥700 regression guard)
- `validate_features.sh`: 0 errors, 17 warnings (no change from 2026-04-27)
- New check added: **#30 — Population swing detection** (see Harness Updates)

### validate_features warnings (persistent, unchanged)
- `save_load` status='needs_tests' with test_count=0
- `construction_dust_particles_spawn` not marked `#[ignore]` (flaky CI risk, fired Apr 13/15)
- `rand::rng()` in systems.rs (×3) and game/mod.rs (×3) — non-determinism
- Score plateau at ~2.9/5.0 for 5+ consecutive entries (now 18th)
- `world_state` status='needs_work' with test_count=0
- `sight_range=25.0` trivially reveals map (fog-of-war non-functional)
- VillagerMemory not read in ai.rs (Pillar 2 gap)
- Persistent food crisis: food_per_cap < 2.0 in 2+ seeds for 4/5 recent checks
- Seed 42 stagnation: pop=4, food=12 for 5/5 recent checks (18 total consecutive in diagnostic)
- `ROAD_TRAFFIC_THRESHOLD=300.0` prevents road formation
- fire.rs, water_cycle.rs, particles.rs have 0 tests
- Drought structurally impossible (grain >= pop×5 never met)
- CLAUDE.md claims ~190 lib tests; actual count is 818 (4× stale)

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Fog-of-war non-functional: all seeds hit exploration_pct=100% in diagnostic run. sight_range=25 + initial reveal radius=15 trivially covers any 70×25 map.
- Desert dominates seed 42 (32%) and seed 137 (49%); Forest absent from top biomes in those seeds. Seed 777 is healthier (Scrubland=18.8%, Sand=18.7%, Grass=12.5%).
- Terrain 98%+ flat (flat_pct=98.4–98.7%, cliff_pct=0%, steep_pct≤0.01%). No mountain passes or valleys forming meaningful geography.

**Pillar 2 — Emergent Complexity:**
- VillagerMemory (DangerZone, ThreatReport) written in systems.rs but never read in ai.rs — 18th consecutive day.
- Drought multiplier hardcoded (events.rs), not chained through water→soil→crop.

**Pillar 3 — Explore → Expand → Exploit → Endure:**
- Seed 42: pop=4 (Fragile, Overcrowded) at tick 6000; dies around tick 6668 in playtest.
- Events: 0 fired in diagnostic runs. WolfSurge fires non-deterministically. Drought structurally impossible.
- Roads: 0% coverage across all seeds. ROAD_TRAFFIC_THRESHOLD=300.0 unreachable for pop=4-12.

**New finding today:** Seed 42 showed pop=6 in the baseline run (vs pop=4 in diagnostic and RC runs). This confirms that the second Hut IS being placed by auto-build in favorable rand::rng() outcomes — the bug is purely non-determinism in `game/build.rs` scoring, not a fundamental logic error. Fixing rand::rng() in the auto-build path would likely resolve seed 42 stagnation.

## Architecture

Module sizes (no change from yesterday):
| File | Lines | Status |
|------|-------|--------|
| game/mod.rs | 2592 | Getting big |
| game/build.rs | 2306 | Getting big |
| terrain_pipeline.rs | 2232 | Getting big |
| tilemap.rs | 2515 | Getting big |
| ecs/systems.rs | 2227 | Getting big |
| ecs/ai.rs | 2104 | Getting big |

No files exceed 3000 lines (split threshold). All split-product files present. ARCHITECTURE.md accurate.
CLAUDE.md lists ~190 lib tests; actual is 818 — stale by 4×.

## Game Quality Evaluation

### Per-Seed Results (tick ~6000, --screenshot --ticks 12000 with half_speed_base)

Diagnostic run values:
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4 (full)    | 30.7%  | 14     | yes (dies ~6668) |
| 137  | 4†        | 29   | 7.2      | —           | 17.9%  | 14     | yes |
| 777  | 12        | 18   | 1.5      | —           | 38.0%  | 14     | yes |

†Seed 137 non-determinism: diagnostic pop=4 (food=29/Surplus), report-card pop=11 (food_per_cap=1.3/Critical), baseline pop=11. All same binary, sequential runs. Seed 42 diagnostic pop=4, baseline pop=6 — confirms auto-build Hut placement is rand::rng()-gated, not logically blocked.

### Report Card Summaries
- Seed 42: `Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)`
- Seed 137: `Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)` [threat_score=19.78]
- Seed 777: `Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)` [threat_score=26.18]

Food is Critical (starvation imminent) for 2/3 seeds in report-card runs.

### Simulation Health
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|---------|
| pipe_water_total | 298.19 | 244.63 | 313.91 |
| wind_moisture_total | 17810.57 | 16134.06 | 41498.49 |
| avg_vegetation | 0.44 | 0.41 | 0.42 |
| avg_moisture | 0.46 | 0.33 | 0.65 |

All simulation systems active (values > 0). Seed 777 consistently higher wind_moisture (41K vs 16-18K in other seeds).

### Baseline Check
All 3 seeds: **PASS** (check_baselines.sh: 3 passed, 0 failed).
Baseline run populations: seed 42 pop=6, seed 137 pop=11, seed 777 pop=11.

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14 biomes ✓, water 17-38% ✓; slopes 98%+ flat ✗, Desert 32-49% in 2/3 seeds ✗, elevation_std 0.09-0.12 (minimal variation) ✗ |
| Settlement Viability (30%) | 2 | Seed 42 Fragile/Overcrowded/dies ~6668; food Critical (RC) in 2/3 seeds; pop diverges 4-12 across seeds due to non-determinism |
| Simulation Health (20%) | 4 | All systems active with reasonable values; moisture chain has known design issues but functionally produces non-zero output |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4-12, footprint 556-903, threat 10-26); 0 events in diagnostic runs; no roads formed; meaningful divergence visible |
| **Weighted Average** | **2.9** | 18th consecutive plateau at 2.9/5.0 |

Calculation: 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 0.9 + 0.6 + 0.8 + 0.6 = **2.9**

### Baseline Check
- check_baselines.sh: **3 passed, 0 failed** — all seeds within expected ranges

## Harness Updates

Added **Check #30: Population swing detection** to `validate_features.sh`.
- Reads the last two entries in `docs/metrics_history.json`.
- Warns if any seed's population changes by more than 5 between consecutive health check entries.
- Captures the observable *impact* of rand::rng() non-determinism on game outcomes (complements check #15 which catches the code-level calls).
- Today's data would trigger this: seed 137 went from pop=11 (2026-04-27) to pop=4 (today's diagnostic) — delta of 7.
- Check passes on the 2026-04-27 data (last two entries: seed 42 ±0, seed 137 ±0, seed 777 ±0 between Apr 26 and Apr 27).

## features.json Updates
- Updated `last_verified` for all systems to 2026-04-28.
- Updated `ecs_core` known_issues: stagnation at 18 consecutive entries (was 17); added new insight that seed 42 pop=6 in baseline run confirms auto-build Hut placement IS rand::rng()-gated (not a logic error).

## Priority Actions
1. **Fix rand::rng() in auto-build path** (`ecs_core` + `building_system` known issue): seed 42 showed pop=6 in the baseline run today (vs pop=4 in diagnostic/RC runs) — the second Hut IS placed when rand::rng() is favorable. Replacing rand::rng() with seeded per-entity or per-tick RNG in `game/build.rs` auto-build scoring would make this deterministic. This is the single highest-impact fix: resolves Settlement Viability score, resolves seed 42 stagnation, resolves score plateau.

2. **Fix `construction_dust_particles_spawn` flaky test** (`game_loop` known issue): add `#[ignore]` annotation in `src/game/tests.rs`. One-line fix, eliminates false CI failures. Has fired twice (Apr 13, Apr 15) over 18-day tracking window.

3. **Lower `ROAD_TRAFFIC_THRESHOLD`** (`events` known issue): current value 300.0 unreachable for pop=4-13 settlements. Diagnose actual traffic accumulation via `--diagnostics` overlay to determine feasible threshold. Emergent roads are a core design pillar.

4. **Fix biome distribution skew** (`terrain_generation` known issue): Desert 32-49% in seeds 42/137, Forest absent. Check `terrain_pipeline.rs` biome assignment thresholds. Root cause blocking Terrain Quality from scoring above 3.
