# Daily Health Check — 2026-05-14

## Test Results
- **777 passed, 1 failed, 10 ignored** (220s, `cargo test --lib`)
- Failing: `game::tests::build_site_gets_completed_in_game` — known flaky, not marked `#[ignore]` (check #35 fires)
- Compiler: 11 warnings in lib (unused_mut, dead code — same as previous runs)
- features.json test_count accuracy: within tolerance per validate_features.sh

## Design Drift
- **CLAUDE.md stale** (check #40): lists `simulation.rs` (split 2026-03-01), missing `world_state.rs` — misleads new contributors
- **CLAUDE.md test count stale** (check #29): claims ~190 tests, actual 818 (4×)
- **Pillar 2 gap** (check #22): `VillagerMemory` written in `systems.rs` but never read in `ai.rs` — villagers still use global state, not per-agent memory
- **Event modifier chain dead** (check #33): `tick_config` drought/harvest modifiers built at `game/mod.rs:2135–2147` but never passed to `step_water_cycle()` — drought/harvest events have zero effect on simulation
- **Fog-of-war trivial** (check #21): `sight_range=25`, reveal_radius=15 → 100% exploration in all seeds; not a functional game mechanic
- **No roads** (check #25): `ROAD_TRAFFIC_THRESHOLD=300.0` never reached in small settlements (pop 4–11)
- `analytical_erosion.rs` and `scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38, known since 2026-05-08)

## Architecture
- No files over 3000 lines (non-test) — all within limits
- Files approaching split threshold: `game/mod.rs` 2592, `game/build.rs` 2306, `tilemap.rs` 2515, `ecs/ai.rs` 2104, `ecs/systems.rs` 2227, `terrain_pipeline.rs` 2232
- `world_state` system: `test_count=0`, `status=needs_work` — no unit tests for canonical sim state (check #18)
- `save_load` system: `test_count=0`, `status=needs_tests` (check #9, ongoing)

## Game Quality Evaluation

### Per-Seed Results (tick=6000; diagnostics run)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 14     | Yes      |
| 137  | 11        | 16   | 1.45     | 20          | 17.9%  | 15     | Yes      |
| 777  | 11        | 14   | 1.27     | 16 (diag) / 24 (rc) | 37.9% | 14 | Yes |

Non-determinism note: Seed 777 housing cap differs by 8 slots (3 Huts) between diagnostic and report-card runs in the same session — `rand::rng()` in `game/build.rs`.

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight (3.0/cap) | Housing: Overcrowded | Terrain: Diverse | Threats: Low — **WolfSurge active** (active_events=1, first event in seed 42 this session; non-deterministic)
- Seed 137: Population: Healthy | Food: Critical (1.45/cap — starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing
- Seed 777: Population: Healthy | Food: Critical (1.27/cap — starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 14-15 biome types present, but Desert=32-49% in 2/3 seeds, Forest=0.1-0.7% in all seeds, slope 98.4-98.7% flat, elevation_std=0.09-0.11, 0% steep/cliff — Pillar 1 violated |
| Settlement Viability (30%) | 2 | All 3 survive; but 2/3 critical food_per_cap≤1.45, seed 42 fragile/stagnant (27th+ consecutive pop=4 run), wood stockpile nearly exhausted in seeds 137 (4w) and 777 (3w) |
| Simulation Health (20%) | 4 | pipe_water_total=244–306, wind_moisture_total=16K–41K, avg_vegetation=0.42–0.44 — all systems active; deducted 1 for broken event modifier chain (drought/harvest modifiers discarded) |
| Emergent Behavior (20%) | 2 | WolfSurge in 1/3 seeds (non-deterministic), 0 roads in all seeds, prey extinct in 2/3 seeds, same 5 building types across all seeds, no outposts |
| **Weighted Average** | **2.4** | 7th consecutive 2.4 — score plateau (check #17 fires) |

### Baseline Check
- 3/3 PASS (`scripts/check_baselines.sh`) — seed 42 pop=4, seed 137/777 pop=11, water within range

## Harness Updates
- Added **check #42**: Forest biome near-zero detection — fires when `forest_pct < 1.0%` in 2+ evaluation seeds in metrics_history. Complements check #41 (wood skill pure-decay) with a direct biome coverage signal. Fires on current code: seeds 42=0.3%, 137=0.1%, 777=0.7% (all 3 below threshold).
- Validate result: **0 errors, 30 warnings** (was 29)

## features.json Updates
- `last_verified` updated to `2026-05-14` for all systems

## Priority Actions
1. **Biome distribution** (`terrain_quality=2`): Desert=32–49% in 2/3 seeds, Forest≈0% — diagnose `terrain_pipeline.rs` biome scoring post-WorldState refactor; fix biome distribution before any skill/resource tuning (check #41 wood decay, check #42 forest, check #32 flat terrain all have same root cause)
2. **Food system** (`settlement_viability=2`): food_per_cap≤1.45 in 2/3 seeds is Critical/starvation-level; run farm yield vs hunger diagnostics before changing constants (chronic crisis 5/5 checks: check #23)
3. **Event modifier chain** (`emergent_behavior=2`): `tick_config` at `game/mod.rs:2135-2147` builds drought/harvest modifiers that are silently discarded — fix the pipeline to pass modifiers to `step_water_cycle()` (check #33); this also unblocks Pillar 2 emergence (drought → water → soil → crops chain)
4. **Flaky tests**: mark `build_site_gets_completed_in_game` and `construction_dust_particles_spawn` as `#[ignore]` to stop false CI failures (checks #14, #35 both fire)
