# Daily Health Check — 2026-04-05

## Test Results
- Total: **778 passed, 0 failed, 10 ignored** (finished in ~56s)
- Ignored tests: 9 diagnostic (`diag_*`) tests in `simulation/moisture.rs` + 1 in `terrain_pipeline.rs` — all intentionally `#[ignore]`, run manually with `--nocapture`
- Test count accuracy: all systems within tolerance per `validate_features.sh` (0 errors, 0 warnings)

## Design Drift

**Drift 1: VillagerMemory implemented but AI still reads global stockpile (per_villager_memory.md Phase 1 incomplete)**
- `src/ecs/components.rs:203` — `VillagerMemory` struct exists with `MemoryKind`, decay rates, entries
- `src/ecs/ai.rs` — **0 reads from `VillagerMemory`**; AI has 15 `stockpile_wood/stone/food` global reads
- `src/ecs/systems.rs` — memory is written (DangerZone, ThreatReport, etc.) but never read for decisions
- Design doc `docs/design/pillar2_emergence/per_villager_memory.md` says "Phase 1: additive (AI still reads globals). Phase 2 will switch AI to read from memory." — we are stuck in Phase 1.
- **Pillar 2 (Emergent Complexity) is the #2 design pillar. Memory exists but has zero decision influence.**

**Drift 2: Default seed produces rapid game over (tick ~8257, pop peak 9)**
- Seed 42 and 137 both survive to tick 12100+ with pop 3 — the default (random) seed hit a bad map
- Not a code bug, but worth noting that AutoBuild mode survives to ~12K ticks before GAME OVER on tested seeds
- No design drift per se, but settlements never grow past ~9 villagers in 12K ticks — population ceiling is very low

**Drift 3: ARCHITECTURE.md file map is stale (multiple splits completed)**
- `simulation.rs` (listed as 4844 lines, "BAD") — **already split** into `src/simulation/` (10 files, 6796 total lines)
- `ecs/mod.rs` (listed as 7163 lines, "BAD") — **already split**: now 17 lines; tests moved to `ecs/tests.rs` (7164 lines)
- `game/mod.rs` (listed as 6073 lines, "BAD") — **already split**: now 2393 lines; `input.rs`, `water_cycle.rs`, `fire.rs`, `particles.rs`, `tests.rs` extracted
- `game/render.rs` (listed as 4094 lines, "BAD") — **already split** into `src/game/render/` (8 files, 4291 total lines)
- `hydrology.rs` (504 lines) — **not mentioned at all** in ARCHITECTURE.md; particle-based hydraulic erosion port of Nick McDonald's SimpleHydrology

## Architecture

**Module size warnings (non-test files over 2000 lines):**
| File | Lines | Status |
|------|-------|--------|
| `src/tilemap.rs` | 2515 | "Getting big" per ARCH |
| `src/game/build.rs` | 2306 | "Getting big" per ARCH |
| `src/ecs/systems.rs` | 2227 | "Big but coherent" per ARCH |
| `src/terrain_pipeline.rs` | 2230 | OK per ARCH |
| `src/ecs/ai.rs` | 2104 | "Big but coherent" per ARCH |
| `src/game/mod.rs` | 2393 | Improved from 6073 — still needs watch |

No files over 3000 lines (excluding test files). No new critical size violations.

**Test files over 2000 lines (acceptable, but noted):**
- `src/ecs/tests.rs`: 7164 lines (known issue — split recommended)
- `src/game/tests.rs`: 2916 lines (not previously flagged)

**Undocumented file:**
- `src/hydrology.rs` (504 lines, 5 tests) — particle-based hydraulic erosion (Nick McDonald port). Not in ARCHITECTURE.md File Map. Should be added.

**Zero-test modules (known issues, unchanged):**
- `src/game/fire.rs` (0 tests)
- `src/game/water_cycle.rs` (0 tests)
- `src/game/particles.rs` (0 tests)
- `src/game/save.rs` + `src/ecs/serialize.rs` (0 dedicated unit tests)

**ARCHITECTURE.md accuracy:** File map is significantly stale — the four major refactors (simulation split, ecs split, game split, render split) are all done but not reflected. The "Refactor Priorities" section still lists them as future work. Total lines is documented as "43K" but actual is ~47.6K.

## features.json Updates

- `terrain_generation`: `last_verified` → "2026-04-05"
- `ecs_core`: `last_verified` → "2026-04-05"
- `game_loop`: `last_verified` → "2026-04-05", update `known_issues` to reflect `game/mod.rs` is now 2393 lines (not 2379)
- `rendering`: `last_verified` → "2026-04-05"
- `simulation_moisture`: `last_verified` → "2026-04-05"
- `save_load`: `last_verified` → "2026-04-05"
- All other systems: `last_verified` → "2026-04-05"
- New known issue added to `ecs_core`: "VillagerMemory written but never read in AI decisions — Phase 2 (memory-driven AI) not started"

## Harness Updates

Added **Check 5: Large non-test source files** to `scripts/validate_features.sh`.
- Warns on any `.rs` file over 3000 lines that isn't named `tests.rs`
- Catches future bloat before it reaches the "BAD" threshold
- Passes cleanly on current codebase (no files currently exceed 3000 lines outside test files)

## Priority Actions

1. **Update ARCHITECTURE.md file map** — the four completed splits (simulation, ecs, game, render) are done but documented as future work. Add `hydrology.rs`. Update total line count (43K → 47.6K). Low risk, high clarity value.
2. **Start Phase 2 of per_villager_memory** — `VillagerMemory` is written but never read for AI decisions. This is the foundation of Pillar 2 (Emergent Complexity). Even one AI decision (e.g., "go to the resource I remember, not the global closest one") would activate the system.
3. **Add tests for fire.rs / water_cycle.rs / particles.rs** — these three extracted modules (game_loop system) have 0 tests each. They're real gameplay systems with observable effects but no test coverage.
