# Daily Health Check — 2026-05-30

## Test Results
- **778 passed, 0 failed, 10 ignored** (cargo test --lib, ~216s)
- Test count in CLAUDE.md stale: claims ~190, actual 818 (check #29 fires, 4× off)
- Known-flaky tests `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` remain not marked `#[ignore]` (checks #14, #35 fire). CI false-failure risk persists.

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Terrain 98.4–98.7% flat all 3 seeds (elevation_std=0.09–0.12, steep=0%, cliff=0%). No strategic terrain.
- Desert=30–48% in 2/3 seeds; Forest=0.1–0.6% in all 3. Wood skill at pure decay (0.5488) in all seeds. No trees → no wood replenishment → construction chain stalls.
- Seeds 137/777 converge to pop=11 and pop=12 today (diff=1) despite Desert=49% vs Desert=0.1% terrain. Pillar 1 prescribes different settlements from different terrain; they converge.
- Roads never form (ROAD_TRAFFIC_THRESHOLD=300.0 unreachable at pop=4–12; check #25 fires).

**Pillar 2 — Emergent Complexity:**
- `VillagerMemory` written in `ecs/systems.rs` but never read in `ecs/ai.rs`. Villagers use global stockpile counts (check #22 fires). Core Pillar 2 behavior unimplemented.

**Event System:**
- `tick_config` drought/harvest modifiers at `game/mod.rs:2135–2147` never passed to `step_water_cycle` (check #33). Drought/harvest have zero effect. Check #36 fires: events_fired=0 in 2+ seeds for 5/5 consecutive checks (WolfSurge path is independent, but structurally broken drought/harvest dominates).
- Drought grain threshold (`grain >= pop*5`) structurally impossible in eval window (check #27).

No new drift detected beyond the above (all pre-existing, documented issues).

## Architecture
- No non-test source file exceeds 3000 lines ✓
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38; ongoing)
- `CLAUDE.md` module structure lists removed `simulation.rs`, omits `world_state.rs` (check #40; ongoing)
- `save_load` at `needs_tests` with 0 tests; `world_state` at `needs_work` with 0 tests (checks #9, #18)
- 11 compiler warnings in `game/mod.rs` (unchanged)

## Game Quality Evaluation

### Per-Seed Results (Diagnostics, game.tick=6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Events |
|------|-----------|------|----------|-------------|--------|--------|---------|--------|
| 42   | 4         | 15   | 3.75     | 4           | 30.9%  | 13     | Yes (dies 6668) | 0 |
| 137  | 11        | 13   | 1.18     | 20          | 17.9%  | 14     | Yes      | 0      |
| 777  | 12        | 15   | 1.25     | 20          | 38.0%  | 14     | Yes      | 0      |

**Non-determinism:** Report-card run (second process, same binary/seed): seed 42 pop=4/food=12, seed 137 pop=11/food=13 (approx match), seed 777 pop=11/food=15. Seed 777 diagnostics → report-card: pop=12→11 (delta=1, mild). Compared to yesterday where seed 777 pop=4 in diagnostics, today's pop=12 represents a +8 swing — confirming rand::rng() auto-build variance continues.

**Playtests (--play mode):**
- Seed 42: dies at tick 6668 (38th consecutive confirmation at same tick).
- Seed 137: alive at tick 12101 with new villager born, stone deposit discovered.

### Report Card Summaries
- Seed 42: `Fragile` (pop=4) / `Tight` (food, 3.0/cap, no buffer) / `Overcrowded` (cap=4) / `Diverse` terrain / `Low` threat
- Seed 137: `Healthy` (pop=11, growing) / `Critical` (food, starvation imminent, 1.3/cap) / `Tight` housing / `Diverse` terrain / `Growing` threat
- Seed 777: `Healthy` (pop=11 report-run) / `Critical` (food, starvation imminent, 1.4/cap) / `Adequate` housing / `Diverse` terrain / `Growing` threat

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes by count, but Desert 30–49% in 2/3 seeds, Forest <1% in all, Mountain=0%, slopes 98%+ flat; geography cannot shape settlement |
| Settlement Viability (30%) | 2 | Seed 42 dies before 12K ticks; seeds 137/777 survive but food Critical (1.18–1.25/cap); seed 42 housing saturated 38+ consecutive runs |
| Simulation Health (20%) | 4 | pipe_water=244–314 (active), wind_moisture=16K–41K (active), avg_vegetation=0.42–0.43; all subsystems functional |
| Emergent Behavior (20%) | 2 | Events=0 all seeds today; broken tick_config chain; seeds 137/777 converge (diff=1); seed 42 always dies same tick; roads never form |
| **Weighted Average** | **2.4** | 0.6+0.6+0.8+0.4=2.4; plateau at 2.4 for 7th consecutive health check (check #17 fires) |

### Baseline Check
All 3 seeds **PASS** (seed 42: pop=4, water=30.7%, biomes=14; seed 137: pop=4, water=17.9%, biomes=14; seed 777: pop=12, water=38.0%, biomes=15).

### Validate Features
**0 errors, 37 warnings** (same count as yesterday — no new checks added, no checks resolved).

Key firing checks: #14/#35 (flaky tests unignored), #15/#28 (rand::rng() non-determinism), #17 (score plateau), #22 (VillagerMemory gap), #23 (chronic food crisis), #24 (seed 42 stagnation), #25 (road threshold), #27 (drought impossible), #29 (CLAUDE.md stale), #30 (pop swing: seed 777 delta=8 will fire on next check), #31 (prey extinction 2/3 seeds), #32 (flat terrain), #33/#36 (event suppression), #37/#43 (housing saturation), #39 (acute food crisis seed 137), #40 (CLAUDE.md stale modules), #41/#42 (wood skill decay / forest near-zero), #44 (wood stall seeds 137/777), #45 (Granary food threshold unreachable), #46 (ProcessingBuilding conflation), #47 (Bakery planks blocked), #51 (FoodToGrain threshold unsafe).

## Harness Updates
No new checks added. Existing 51 checks cover all known issue patterns. The plateau in score (7 consecutive 2.4) indicates the needed fixes are in application code, not harness coverage.

## features.json Updates
- All 25 systems: `last_verified` updated to `2026-05-30`
- No new known_issues: today's observations follow known patterns (rand::rng() auto-build variance, wood starvation, event suppression). Seed 777 pop=12 today confirms +8 within-session swing, within previously documented range.

## Priority Actions
1. **Fix seeded per-entity RNG** (`ecs_core`, `game_loop`): until seeded, all rubric scores are unreliable noise. Replace `rand::rng()` in `game/build.rs` and `ecs/systems.rs` with seeded per-entity RNG (BACKLOG top priority).
2. **Fix biome distribution in `terrain_pipeline.rs`** (`terrain_generation`): Desert 30–49%, Forest <1%, Mountain=0% in all seeds. This single fix unblocks wood skill, Workshop/Bakery chain, road formation, and Pillar 1 geography differentiation.
3. **Fix event modifier chain** (`events`): `tick_config` at `game/mod.rs:2135–2147` never passed to `step_water_cycle`. One-line fix: thread `tick_config.rain_rate` through `step_water_cycle`. Unlocks drought/harvest events.
4. **Fix Granary food threshold** (`game_loop`): raise `FoodToGrain` trigger from `food > 15` to `food > max(15, population * 2)` in `src/ecs/systems.rs:979,1391`. Prevents food depletion at pop ≥ 10.
