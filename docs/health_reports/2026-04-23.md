# Daily Health Check — 2026-04-23

## Test Results
- **778 passed, 0 failed, 10 ignored** — finished in 158s (lib tests only)
- Test suite size: 818 `#[test]` annotations in src/ (above 700 threshold)
- save_load_round_trip and event_system_serialization ran to completion (2+ minutes each under parallel load) but passed — confirms the dynamic-path fix is working.

### Test Count Accuracy
features.json claims are within tolerance. No discrepancies detected by validate_features.sh.

---

## Design Drift

### Pillar 2: VillagerMemory still unused in AI (unchanged)
`VillagerMemory` is defined in `components.rs` and written in `systems.rs` but is not read in `src/ecs/ai.rs`. AI still uses global stockpile counts (~15 occurrences in `ai.rs`). Pillar 2 (local decision-making from personal memory) is architecturally blocked. No change since 2026-04-22.

### Pillar 3: No game arc visible at tick 6000
All 3 seeds produce 0 events at tick 6000. Road auto-build never triggers (`ROAD_TRAFFIC_THRESHOLD=300.0` in `game/mod.rs:530`; evaluation seeds have pop=4–13, which cannot generate 300 traffic at any tile in 6000 ticks). The "Endure" arc requires events; without them the game is static after initial build-up.

### Anti-goal violation: Fog-of-war trivially 100%
`sight_range=25.0` (spawn.rs) + initial reveal radius=15 (game/mod.rs) combine to reveal a 70×25 map in ~200 ticks. `exploration_pct=100.0` in all 3 seeds. The "Exploration as discovery" deliverable from Phase 1 is not functional. No change since 2026-04-22.

### Resolved: save_load and event_serialization race conditions
`save_load_round_trip` (tests.rs:212) and `event_system_serialization` (tests.rs:704) now use dynamic paths:
`/tmp/test_savegame_{thread_id}_{pid}.json` — the race condition documented in features.json is resolved.
→ Removing these from `game_loop.known_issues` in features.json.

---

## Architecture

### File sizes (no new violations)
| File | Lines | Status |
|------|-------|--------|
| src/ecs/tests.rs | 7164 | OK (test file, exempted) |
| src/game/tests.rs | 2927 | OK (test file, exempted) |
| src/tilemap.rs | 2515 | Approaching threshold (Priority 5 in ARCHITECTURE.md) |
| src/game/mod.rs | 2592 | OK (split done) |
| src/game/build.rs | 2306 | OK (approaching) |
| src/ecs/systems.rs | 2227 | OK (approaching) |
| src/terrain_pipeline.rs | 2232 | OK (approaching) |
| src/ecs/ai.rs | 2104 | OK |
| src/simulation/moisture.rs | 1867 | OK |

No file exceeds 3000 lines (non-test). No new violations. No structural regressions.

### validate_features.sh results: 0 errors, 12 warnings (was 11)
New warning from Check 25 (ROAD_TRAFFIC_THRESHOLD — added this run):
- save_load: needs_tests, test_count=0 (unchanged)
- construction_dust_particles_spawn: not `#[ignore]`d (unchanged)
- rand::rng() in systems.rs and game/mod.rs: 3+3 occurrences (unchanged)
- Score plateau at 2.9 for 5+ consecutive entries
- world_state: needs_work with 0 tests
- sight_range=25.0 trivially reveals map
- VillagerMemory not in ai.rs
- food_per_cap < 2.0 in 2+ seeds for 4/5 recent checks
- Seed 42 stuck pop=4, food=12 for 5/5 recent checks
- **NEW** Check 25: ROAD_TRAFFIC_THRESHOLD=300.0 — too high for small settlements

---

## Game Quality Evaluation

### Per-Seed Results (tick 6000, --screenshot --auto-build --diagnostics)
| Seed | Population | Food | Food/Cap | Housing Cap | Growth Pot. | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 0           | 30.7%  | 14     | Yes      |
| 137  | 11        | 13   | 1.18     | 20          | 9           | 17.9%  | 14     | Yes      |
| 777  | 13        | 17   | 1.31     | 16          | 3           | 38.0%  | 15     | Yes      |

Note: Non-determinism confirmed — report card (separate process) shows seed 42 pop=6 vs diagnostics pop=4. Same RNG seed produces different population due to `rand::rng()` in breeding/death path (check 15 fires on systems.rs and game/mod.rs, 3+3 occurrences each).

### Report Card Summaries
- **Seed 42**: Population: Fragile (one bad winter away) | Food: Critical (starvation imminent) | Housing: Tight | Terrain: Diverse | Threats: Low
- **Seed 137**: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight | Terrain: Diverse | Threats: Growing
- **Seed 777**: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14-15 biomes (≥8 ✓), water 18–38% ✓, but slope 98.4–98.7% flat (varied slopes criterion ✗) and Desert dominates 2/3 seeds (32–49%) with Forest near-zero (0.1–0.6%) |
| Settlement Viability (30%) | 2 | All 3 survive ✓; pop 4/11/13 (seed 42 struggling, <5 threshold); food/cap 3.0/1.18/1.31 — 2/3 seeds Critical (<2.0 threshold); seed 42 growth_potential=0 (housing block) |
| Simulation Health (20%) | 4 | pipe_water_total: 298/244/313 ✓; wind_moisture_total: 17810/16053/41498 ✓; avg_vegetation: 0.44/0.41/0.42 ✓; all systems active. Minor: wind_moisture 2.6× variance across seeds |
| Emergent Behavior (20%) | 3 | Seeds diverge in outcomes (pop 4 vs 13, Desert 32–49% vs 0.1%, farm skill 1.8 vs 37.4); seed 777 shows active production chains (planks=6, grain=32); 0 events fired across all seeds ✗ |
| **Weighted Average** | **2.9** | 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 0.9+0.6+0.8+0.6 |

**13th consecutive entry at 2.9/5.0** — score plateau confirmed.

### Baseline Check
```
PASS: seed 137 (pop=11, water=17.9%, biomes=14)
PASS: seed 42  (pop=4,  water=30.7%, biomes=14)
PASS: seed 777 (pop=11, water=37.9%, biomes=14)
Results: 3 passed, 0 failed
```

---

## Harness Updates

### Added Check 25: ROAD_TRAFFIC_THRESHOLD too high for small settlements
Added to `scripts/validate_features.sh`:
- Reads `ROAD_TRAFFIC_THRESHOLD` from `src/game/mod.rs`
- Warns if value > 200.0 (current value: 300.0)
- Rationale: threshold of 300.0 means zero roads ever form in evaluation seeds (pop=4–13 cannot generate 300 accumulated traffic at any tile within 6000 ticks). This is the mechanism that blocks road emergent behavior.
- New warning fires on current code (expected — documents known issue).

---

## features.json Updates
- **Removed** from `game_loop.known_issues`: save_load_round_trip and event_system_serialization race conditions — both tests now use dynamic `/tmp/test_{name}_{thread_id}_{pid}.json` paths. Race condition resolved.
- **Updated** `last_verified` to 2026-04-23 for all systems.

---

## Priority Actions
1. **Fix Seed 42 Hut placement** (`game/build.rs`): Auto-build never places a second Hut despite wood=43 surplus and housing_capacity=4 at full. This is the root cause of seed 42 stagnation (pop=4 for 13 consecutive entries) and depresses Settlement Viability to 2/5. Diagnose Hut scoring in `score_building_need()` before changing constants.
2. **Fix food/cap crisis** (farm yield vs. hunger rate): food/cap=1.18–1.31 in seeds 137/777 is consistently Critical. Seeds need either higher farm throughput or lower hunger rate. Write a diagnostic test first: measure ticks_per_food_unit from a farm vs. ticks_per_hunger_decrement per villager.
3. **Lower ROAD_TRAFFIC_THRESHOLD to ~75–100** (`game/mod.rs:530`): 300.0 prevents any roads from forming. Roads would improve Emergent Behavior score and match design intent. Add a test: verify road tiles appear after N ticks with M villagers.
