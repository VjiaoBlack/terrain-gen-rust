# Daily Health Check — 2026-04-16

## Test Results
- **778 passed, 0 failed, 10 ignored** (lib tests, 214s)
- Test count accuracy: within tolerance per `validate_features.sh`

## Validate Features (0 errors, 5 warnings — unchanged)
1. `save_load` status='needs_tests' with test_count=0 — no progress
2. `construction_dust_particles_spawn` documented as flaky, not marked `#[ignore]` — CI false-failures expected
3. `src/ecs/systems.rs` — 3× `rand::rng()` (non-determinism risk)
4. `src/game/mod.rs` — 3× `rand::rng()` (non-determinism risk)
5. Score plateau: 2.9/5.0 for 6 consecutive entries (check #17 fires)

## Design Drift

**1. Fog-of-war non-functional** — `game_design.md` pillar 1 lists FoW as an exploration mechanic. `exploration_pct=100.0` trivially in all 3 seeds. Root cause: initial reveal radius=15 + sight_range=22 covers 70×25 map in ~200 ticks (`simulation_maps` known_issues).

**2. Roads never auto-build** — Design anti-goal explicitly says "Roads auto-build from traffic." Zero Road coverage at tick 6000 all seeds. `ROAD_TRAFFIC_THRESHOLD=300.0` (`game/mod.rs:530`) is unreachable with 4-11 villagers. Contradicts design pillar.

**3. Events not firing** — `game_design.md` pillar 2: "A drought + wolf raid + stone shortage should create a crisis organically." 0 `active_events` across all 3 seeds at tick 6000. Event calibration for small populations not working.

**4. Desert biome monoculture** — Design success criteria: "Two seeds produce visibly different settlement shapes." Seeds 42 (Desert=32%) and 137 (Desert=49%) are both dominated by the same biome — not "two fundamentally different settlements." Forest coverage 0.1-0.7% across all seeds.

**5. VillagerMemory unused** — Design pillar 2 (agent knowledge architecture) specifies per-villager memory driving AI decisions. `VillagerMemory` is written in systems.rs but never read for decisions in ai.rs (`ecs_core` known_issues). Still Phase 2 / not started.

## Architecture

All file sizes match `ARCHITECTURE.md`. No new files over 3000 lines (excluding tests.rs). Files approaching or at split threshold (2000+):

| File | Lines | Status |
|------|-------|--------|
| `game/mod.rs` | 2592 | needs_refactor |
| `tilemap.rs` | 2515 | Priority 5 open |
| `game/build.rs` | 2306 | known big |
| `terrain_pipeline.rs` | 2232 | known big |
| `ecs/systems.rs` | 2227 | known big |
| `ecs/ai.rs` | 2104 | known big |

No regressions on split guards (simulation.rs absent, game/render.rs absent, all split products present).

`world_state.rs` still has 0 tests and is not documented in ARCHITECTURE.md's data flow section — only the overview table. This is a known gap.

## Game Quality Evaluation

### Per-Seed Results (tick 6000, year 1, Spring)

| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4 | 12 | 3.0 | 4 (Overcrowded) | 30.7% | 13 | Yes |
| 137  | 11 | 8 | 0.7 | 16 (Tight) | 17.9% | 13 | Yes |
| 777  | 11 | 18 | 1.6 | 20 (Adequate) | 37.9% | 14 | Yes |

### Report Card Summaries
- Seed 42: Population Fragile | Food Tight | Housing Overcrowded | Terrain Diverse | Threats Low (score 10.7)
- Seed 137: Population Healthy | Food Critical | Housing Tight | Terrain Diverse | Threats Growing (score 19.9)
- Seed 777: Population Healthy | Food Critical | Housing Adequate | Terrain Diverse | Threats Growing (score 28.0)

Additional diagnostics:
- All seeds: 0 active events, 0 wolves, 0 fire tiles, 0 roads
- pipe_water_total: 298/245/306 (all active), wind_moisture: 17.8K/16.1K/41.2K (active), avg_veg: 0.44/0.41/0.42

### Rubric Scores

| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13-14 biome types, but Desert dominates 2/3 seeds (32-49%). Forest 0.1-0.7%. Terrain 98%+ flat (elevation_std 0.09-0.11). Biome count acceptable, distribution skewed. |
| Settlement Viability (30%) | 2 | All 3 survive, but seed 42 pop=4 Fragile with 0 growth potential. Food Critical in 2/3 seeds (food/cap 0.7 and 1.6). No events, no roads, no production-chain buildings active. |
| Simulation Health (20%) | 4 | pipe_water_total >0 all seeds. wind_moisture_total >0. avg_vegetation 0.41-0.44 (>0.1). All simulation systems active. |
| Emergent Behavior (20%) | 3 | Seed divergence: seed 42 (pop=4) vs 137/777 (pop=11). 0 events all seeds. Road coverage 0%. Building mixes similar across seeds. No surprising outcomes. |
| **Weighted Average** | **2.9** | 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 2.9 |

### Baseline Check
- Seed 42: PASS (pop=4, water=30.7%, biomes=13)
- Seed 137: PASS (pop=11, water=17.9%, biomes=15)
- Seed 777: PASS (pop=11, water=38.0%, biomes=14)

## Harness Updates
- Added check #18 to `validate_features.sh`: warns when a system has status='needs_work' and test_count=0 (catches `world_state` which has known design issues but zero test coverage — check #9 only catches `needs_tests` status).

## features.json Updates
- `last_verified` updated to 2026-04-16 for all systems
- No status changes (no code changes since 2026-04-13)

## Priority Actions

1. **Fix `construction_dust_particles_spawn` flaky test** — mark `#[ignore]` immediately. Fired twice in April. One-line fix, unblocks CI reliability. (`src/game/tests.rs`)

2. **Lower `ROAD_TRAFFIC_THRESHOLD`** — currently 300.0 (`game/mod.rs:530`), unreachable with ≤11 villagers. Design explicitly requires roads from traffic. Target: ~50.0 for small settlements. Requires diagnostic test first (check traffic values at tick 6000 for current seeds).

3. **Seed 42 population stuck at 4** — pop=4 for 10+ consecutive entries, housing at capacity (Overcrowded), 0 growth_potential. Auto-build not placing additional huts. Investigate auto-build hut scoring in `game/build.rs` for low-population settlements. This drives the Settlement Viability score down to 2.

4. **Biome distribution skew** — Desert 32-49% on 2/3 seeds with Forest near-zero violates the "geography shapes everything" pillar (each seed should suggest a different strategy). Investigate biome assignment logic in `terrain_pipeline.rs`.
