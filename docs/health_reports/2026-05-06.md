# Daily Health Check — 2026-05-06

## Test Results
- **778 passed, 0 failed, 10 ignored** (166s, `cargo test --lib`)
- `build_site_gets_completed_in_game` passed today (flaky — fails intermittently due to `rand::rng()` non-determinism, still not marked `#[ignore]`). `construction_dust_particles_spawn` also not `#[ignore]`d.
- Validate features: **0 errors, 19 warnings** (unchanged from 2026-05-05; same categories)
- Test count: 818 `#[test]` annotations in `src/`; CLAUDE.md still claims ~190 (stale, ~4× off)

## Design Drift

**Pillar 1 — Geography Shapes Everything (VIOLATED):** 98.4–98.7% flat terrain across all 3 seeds; elevation_std = 0.09–0.12; 0% steep, 0% cliff in all seeds. Desert dominates seeds 42 (32%) and 137 (49.4%) while Forest is near-absent (0.1–0.7%). Terrain is not creating strategic constraints on settlement growth as required by `docs/game_design.md` Pillar 1. Status unchanged since 2026-04-30 when first measured.

**Pillar 2 — Emergent Complexity (NOT STARTED):** `VillagerMemory` defined in `components.rs`, written in `systems.rs`, but never read in `ai.rs` (0 references). Villagers still read global stockpile counts (15 call sites in `ai.rs`). Phase 2 (memory-driven AI) has not begun.

**Pillar 3 — Explore→Expand→Exploit→Endure (STALLED):** 25th consecutive zero-event health check. The event modifier chain is broken: `tick_config` drought/harvest modifiers built in `game/mod.rs:2135–2147` are never passed to `step_water_cycle()` (see check #33). `exploration_pct=100%` in all seeds. Roads never form (ROAD_TRAFFIC_THRESHOLD=300.0 unreachable by pop=4–11 in eval window).

**Non-Determinism (NEW DATA POINT):** Within-session housing divergence reached new severity today. Seed 137: diagnostics run (first) shows 3 Huts (capacity=12); report-card run (second, sequential) shows capacity=24 (6 Huts). Seed 777: diagnostics=5 Huts (capacity=20); report-card=3 Huts (capacity=12). Same binary, same seed, ~90 seconds apart — auto-build output diverges by ±2–3 Huts. Root cause: `rand::rng()` in `game/build.rs` auto-build scoring.

## Architecture

No new files exceed 3000 lines. Files approaching threshold (unchanged):

| File | Lines | Status |
|------|-------|--------|
| `src/game/mod.rs` | 2592 | OK (documented) |
| `src/tilemap.rs` | 2515 | Priority 5 candidate for split |
| `src/game/build.rs` | 2306 | OK (documented) |
| `src/ecs/systems.rs` | 2227 | OK (documented) |
| `src/ecs/ai.rs` | 2104 | OK (documented) |
| `src/simulation/moisture.rs` | 1867 | OK |

11 compiler warnings in `game/mod.rs` (unchanged: unused_mut, dead tick_config chain, unused constants).

## Game Quality Evaluation

### Per-Seed Results (tick ≈ 6000 via `--screenshot --ticks 12000` with `half_speed_base=true`)

| Seed | Population | Food | Food/Cap | Housing Cap | Growth Pot | Water% | Biomes | Rabbits | Survived |
|------|-----------|------|----------|-------------|------------|--------|--------|---------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 0          | 30.7%  | 13     | 0       | yes      |
| 137  | 11        | 12   | 1.1      | 12          | 1          | 17.9%  | 14     | 0       | yes      |
| 777  | 11        | 13   | 1.2      | 20          | 9          | 37.9%  | 14     | 8       | yes      |

Terrain (all seeds): slope_flat 98.4–98.7%, 0% steep, 0% cliff; elevation_std 0.09–0.12; pipe_water_total 245–303; wind_moisture_total 15,945–40,786.

### Report Card Summaries
- **Seed 42:** Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)
- **Seed 137:** Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)
- **Seed 777:** Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)

_Note: Seed 137 report-card shows `capacity: 24` but diagnostics show `hut_capacity: 12` (3 Huts); second run placed 6 Huts. This within-session divergence confirms `rand::rng()` severity in auto-build path._

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes (excellent biome variety), but 98%+ flat (Pillar 1 violation), Desert dominant in 2/3 seeds, 0% cliff/steep |
| Settlement Viability (30%) | 2 | All 3 survive; seed 42 Fragile (pop=4, growth_pot=0); food Critical (food/cap=1.1–1.2) in 2/3 seeds; 6th consecutive check with food crisis in 2+ seeds |
| Simulation Health (20%) | 4 | pipe_water > 0 ✓, wind_moisture > 0 ✓, avg_vegetation 0.41–0.44 ✓. All subsystems active. Known broken: tick_config event modifiers silently dropped |
| Emergent Behavior (20%) | 2 | Seeds diverge meaningfully (pop=4 vs 11), but 0 events fired (25th consecutive zero-event check), no roads, rabbits extinct in 2/3 seeds |
| **Weighted Average** | **2.4** | 2×0.3 + 2×0.3 + 4×0.2 + 2×0.2 = 0.6+0.6+0.8+0.4 |

Score plateau: 2.4/5.0 (2nd check at this score; plateau check threshold is 5 entries, not yet triggered).

### Baseline Check
All 3 baselines PASS (3/0): seed 42 pop=4 ✓, seed 137 pop=11 ✓, seed 777 pop=11 ✓

## Harness Updates
- **New check #36 added:** Event suppression detector — fires WARN when `events_fired=0` in 2+ seeds across last 5 consecutive metrics_history entries. Root cause is the broken tick_config chain (check #33). New check fires immediately on current data (25 consecutive zero-event checks). Passes with exit 0 (warns, does not error).
- Validate features: 19 warnings total (was 19; +1 new check offset by the check firing a warning that was previously uncounted).

## features.json Updates
- All `last_verified` dates updated to 2026-05-06
- `ecs_core.known_issues`: Added new data point — within-session housing divergence for seeds 137 and 777 (diagnostics vs report-card runs in same session show ±2–3 Hut difference)
- `events.known_issues`: Updated "0/3 seeds" entry to note 25th consecutive zero-event check as of 2026-05-06

## Priority Actions
1. **Flat terrain (Terrain score: 2)** — 98%+ flat contradicts Pillar 1 core requirements. Write a failing test (`slope_flat_pct < 85%`) then investigate `src/terrain_pipeline.rs` height amplitude and erosion parameters. Do not change constants without the test.
2. **Event modifier chain broken (Emergent score: 2)** — `tick_config` drought/harvest modifiers silently dropped (`game/mod.rs:2135–2147`). Fix parameter pass-through to `step_water_cycle()`. Confirmed by check #33. After fix, events may still not fire (separate threshold/calibration issue) but at least the pipeline will be wired.
3. **Food crisis (Settlement score: 2)** — food/cap < 2.0 in 2/3 seeds for 6 consecutive checks. Add diagnostic test printing farm yield per tick, food consumed per tick, before any constant changes.
4. **Mark flaky tests `#[ignore]`** — `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` both pass today but fail intermittently. Mark `#[ignore]` or fix with seeded RNG to eliminate false CI failures. (`src/game/tests.rs`)
5. **Seed 42 auto-build stagnation** — pop=cap=4, growth_potential=0 in 4/5 recent checks. Hut placement priority in `game/build.rs` needs diagnosis. Root cause: `rand::rng()` in auto-build scoring may be the same fix as #4.
