# Daily Health Check — 2026-05-05

## Test Results
- **777 passed, 1 failed, 10 ignored** (293s, `cargo test --lib`)
- **NEW FLAKY TEST**: `game::tests::build_site_gets_completed_in_game` — fails in full suite, passes in isolation. Root cause: `rand::rng()` in `ecs/systems.rs` and `game/mod.rs` causes villagers to not complete the build site within 3000 ticks on some non-deterministic runs. Same mechanism as `construction_dust_particles_spawn`. Should be marked `#[ignore]` or given a seeded RNG.
- Test count accuracy: features.json figures within tolerance. CLAUDE.md still claims ~190 tests; actual count is 818.

## Design Drift

**Pillar 1 — Geography Shapes Everything (VIOLATED):** All 3 evaluation seeds have slope_flat_pct ≥ 98.4%; elevation_std = 0.09–0.12. Terrain is nearly uniformly flat. No hills, mountain passes, or elevation-driven constraints. Desert biome dominates seeds 42 (32%) and 137 (48.7%). Settlement shape is not constrained by geography — it forms a radial blob regardless of seed.

**Pillar 2 — Emergent Complexity (NOT STARTED):** `VillagerMemory` is defined in `components.rs`, written in `systems.rs`, but never read in `ai.rs`. Villagers still consult global stockpile counts at 15 call sites in `ai.rs`. Per-villager local decision-making is absent. `docs/game_design.md` Pillar 2 core requirements are unimplemented.

**Pillar 3 — Explore→Expand→Exploit→Endure (STALLED):** 0 events fired in any seed (24 consecutive zero-event health checks). Drought/harvest event modifiers are built in `game/mod.rs:2135–2147` but `step_water_cycle()` never receives them — silently discarded. Event modifier chain is structurally broken. Roads never form (ROAD_TRAFFIC_THRESHOLD=300 vs max traffic achievable by pop=4–11).

**Fog-of-war (NOT FUNCTIONAL):** `sight_range=25.0` (spawn.rs) + `reveal_radius=15` (game/mod.rs) trivially reveals the full 70×25 map within ~200 ticks. `exploration_pct=100.0` in all evaluation seeds. Exploration as discovery (Phase 1 deliverable per `game_design.md`) is not functioning.

## Architecture

No files exceed 3000 lines (excluding test files). Files approaching threshold (unchanged from previous checks):

| File | Lines | Status |
|------|-------|--------|
| `src/game/mod.rs` | 2592 | OK (documented) |
| `src/tilemap.rs` | 2515 | Priority 5 candidate for split |
| `src/game/build.rs` | 2306 | OK (documented) |
| `src/ecs/systems.rs` | 2227 | OK (documented) |
| `src/ecs/ai.rs` | 2104 | OK (documented) |
| `src/simulation/moisture.rs` | 1867 | OK |

11 compiler warnings in `game/mod.rs` (unchanged: unused_mut, dead tick_config, unused variables).

## Game Quality Evaluation

### Per-Seed Results (tick ≈ 6000 via --ticks 12000 with half_speed_base=true)

| Seed | Population | Food | Food/Cap | Housing Cap | Growth Pot | Water% | Biomes | Rabbits |
|------|-----------|------|----------|-------------|------------|--------|--------|---------|
| 42   | 4         | 12   | 3.0      | 4           | 0          | 30.7%  | 13     | 0       |
| 137  | 11        | 16   | 1.1      | 24          | 5          | 17.9%  | 14     | 3       |
| 777  | 11        | 17   | 1.4      | 20          | 9          | 38.0%  | 14     | 6       |

Slope distribution all seeds: 98.4–98.7% flat, 0% steep, 0% cliff. Elevation std: 0.09–0.12. pipe_water_total: 244–314. wind_moisture_total: 16,602–41,498.

### Report Card Summaries
- Seed 42: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)
- Seed 137: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)
- Seed 777: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes (excellent), but 98%+ flat across all 3 seeds (Pillar 1 violation), Desert dominant in 2/3 seeds |
| Settlement Viability (30%) | 2 | Seed 42 Fragile (dies ~tick 6668, growth_potential=0); seeds 137/777 Healthy but food Critical (food/cap 1.1–1.4); persistent food crisis 5/5 consecutive checks |
| Simulation Health (20%) | 4 | pipe_water > 0 ✓, wind_moisture > 0 ✓, avg_vegetation 0.42–0.44 ✓. All systems active. Known broken: moisture chain, tick_config event modifiers silently dropped |
| Emergent Behavior (20%) | 2 | Seeds produce meaningfully different worlds (good), but 0 events fired in all seeds (24th consecutive zero-event check), no roads (threshold too high), prey extinct in 1/3 seeds (up from 2/3 yesterday — rand::rng() variation) |
| **Weighted Average** | **2.4** | terrain×0.3 + settlement×0.3 + simulation×0.2 + emergent×0.2 = 0.6+0.6+0.8+0.4 |

_Note: Previous health checks scored terrain at 3 rather than 2. Re-scored to 2 this check because 98%+ flat terrain is a direct contradiction of Pillar 1 core requirements. Score drop from 2.7→2.4 reflects this correction, not a regression._

### Baseline Check
All 3 baselines PASS (3/0): seed 137 pop=11 ✓, seed 42 pop=4 ✓, seed 777 pop=11 ✓

## Harness Updates
- **New check added (#35):** `build_site_gets_completed_in_game` flaky test guard — warns when not marked `#[ignore]`. Mirrors existing check #14 pattern for `construction_dust_particles_spawn`.
- Validate features: 0 errors, 19 warnings (all known, no new categories).

## Harness Review (1st of May already done; next review June 1)
Not applicable today.

## features.json Updates
- `ecs_core.known_issues`: Added `build_site_gets_completed_in_game` as new flaky test (fails in full suite, passes in isolation — same mechanism as construction_dust_particles_spawn).
- All `last_verified` dates updated to 2026-05-05.

## Priority Actions
1. **Flat terrain (Terrain score: 2)** — Investigate terrain_pipeline.rs height generation and erosion amplitude. Slope is 98%+ flat across all seeds; Pillar 1 requires varied topography. Write a failing test that asserts slope_flat_pct < 80% before touching constants. (`src/terrain_pipeline.rs`)
2. **Event modifier chain is broken (Emergent score: 2)** — `tick_config` drought/harvest modifiers built in `game/mod.rs:2135–2147` are silently discarded; `step_water_cycle()` never receives them. Fix the parameter pass-through or restructure the event→simulation bridge. Confirmed by harness check #33.
3. **Food crisis (Settlement score: 2)** — food_per_cap < 2.0 in 2+ seeds for 5 consecutive checks. Diagnose farm yield vs hunger rate with diagnostics before changing any constants.
4. **Mark new flaky test `#[ignore]`** — `game::tests::build_site_gets_completed_in_game` fails in full suite (~same mechanism as construction_dust_particles_spawn). Either add `#[ignore]` or fix with seeded RNG. (`src/game/tests.rs:252`)
5. **Seed 42 auto-build stagnation** — growth_potential=0, pop=cap=4 in 4/5 recent checks. Auto-build never places second Hut. Diagnose Hut placement priority in `game/build.rs` before changing scoring.
