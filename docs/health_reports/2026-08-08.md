# Daily Health Report — 2026-08-08

**Commit:** 797730a  
**Rubric Score:** 2.2 / 5.0  
**Tests:** 778 passed, 0 failed, 10 ignored (`cargo test --lib`)  
**Harness:** 0 errors, 42 warnings (`validate_features.sh`, 69 checks)  
**Baselines:** 3/3 PASS (`check_baselines.sh`)

---

## Test Results

`cargo test --lib` completed in ~298s: **778 passed, 0 failed, 10 ignored**.  
No regressions from prior run. Build (`cargo build --release`) succeeded in 54.84s.

Known flaky tests (not newly failing today):
- `mining_sparkle_particles_spawn` — intermittent, depends on stone-particle RNG
- `day_night_cycle` — harmless tick-count variance  
- One integration test known to fail under certain RNG seeds

All three remain unresolved; none failed today.

---

## Design Drift

| Pillar | Status | Evidence |
|--------|--------|----------|
| P1: Geography Shapes Everything | **VIOLATED** | slope_flat_pct=98.4–98.7% all seeds; elevation_std=0.09–0.11; steep=0%, cliff=0%. Two seeds produce near-identical settlement footprints. |
| P2: Emergent Complexity (local knowledge) | **NOT IMPLEMENTED** | `VillagerMemory` field in ai.rs never read (check #22). Villagers use global stockpile reads. |
| P3: 4E Arc (explore→expand→exploit→exterminate) | Partial — stalls at expand | Seed 42 pop=4 indefinitely; cap reached via rand::rng() luck only |
| P4: Observable Simulation | Partial | Overlays work; event log empty (0 events fired); tick_config chain broken |
| P5: Scale Over Fidelity | OK | ~1600 tiles render at 60fps |

**CLAUDE.md stale references (not to fix, just noting):**
- Module map lists `simulation.rs` (monolith) — already split to `src/simulation/`
- Test count claims "~190 tests" — actual is 778
- `world_state.rs` not in module map

---

## Architecture

| File | Lines | Status |
|------|-------|--------|
| `src/tilemap.rs` | 2515 | Priority 5 refactor: data-driven TerrainDef (not yet started) |
| `src/simulation/moisture.rs` | 1867 | 9/18 tests are `#[ignore]`; split candidate |
| `src/terrain_pipeline.rs` | 2232 | Approaching 2500-line split threshold |
| `src/ecs/tests.rs` | 7164 | Test file, expected large |

Three water systems still coexist: `WaterMap` (legacy), `PipeWater` (primary), `Terrain::Water` (derived). Cleanup scheduled as Priority 0C.

`src/world_state.rs` (74 lines) not yet in ARCHITECTURE.md file map.

---

## Game Quality Evaluation

### Per-Seed Diagnostics (12000 ticks, `--auto-build`)

| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|---------|
| Population | 4 | 11 | 11 |
| Food | 12 | 12 | 13 |
| Food/cap | 3.00 | 1.09 | 1.18 |
| Buildings | 5 | 8 | 11 |
| Housing cap | 4 | 12 | 16 |
| Housing growth potential | 0 | 1 | 5 |
| Wood | 43 | 4 | 4 |
| Grain | 0 | 0 | 10 |
| Planks | 0 | 0 | 0 |
| Bread | 0 | 0 | 0 |
| Rabbits | 2 | 0 | 6 |
| Events fired | 0 | 0 | 0 |
| Water % | 30.7% | 17.9% | 37.9% |
| Desert % | 32.0% | 49.4% | 0.1% |
| Forest % | 0.3% | 0.1% | 0.7% |
| Slope flat % | 98.4% | 98.7% | 98.6% |
| Wood skill | 0.549 | 0.549 | 0.549 |
| Seek pct | 75.0% | 81.8% | 81.8% |
| Build sites | 2 | 6 | 4 |
| Biomes | 13 | 14 | 14 |

### Report Card Summaries

**Seed 42:** Population 4, stuck. Ample wood (43) but rand::rng() prevents second Hut placement. No Workshop/Bakery chain. Zero events. Seek time 75% (high idle). Desert 32%, Forest 0.3%.

**Seed 137:** Population 11, food-constrained (fpc=1.09). Housing growth potential 1 Hut. Wood exhausted (4). Zero events. Highest desert % (49.4%), lowest forest (0.1%). Seek 81.8%.

**Seed 777:** Population 11, moderate food security (fpc=1.18). Best case: 5 potential growth Huts, grain accumulating (10), 6 rabbits. Wood exhausted (4). Zero events. 0.1% desert. Seek 81.8%.

### Rubric Scores

| Category | Weight | Score | Justification |
|----------|--------|-------|---------------|
| Terrain | 30% | 2/5 | 7-stage pipeline works; biome distribution imbalanced (desert 0–49%, forest <1%); terrain 98%+ flat violates Pillar 1 |
| Settlement | 30% | 2/5 | Basic survival (2/3 seeds >10 pop); no production chains reach planks/bread; seek_pct 75–82% (high idle); seed 42 permanently stalled |
| Simulation | 20% | 4/5 | Water (pipe model) and moisture functional; seasons/day-night cycle working; event system wired but tick_config chain broken (0 events across all seeds) |
| Emergent | 20% | 1/5 | No VillagerMemory reads; no events; rand::rng() non-determinism means outcomes vary by RNG luck not strategic placement |
| **Weighted avg** | | **2.2/5.0** | Below 3.5 ship threshold |

### Baseline Check

```
check_baselines.sh: 3/3 PASS
  seed 42: pop=6 (range: 3–8)   ✓
  seed 137: pop=4 (range: 3–9)  ✓
  seed 777: pop=12 (range: 8–15) ✓
```

Note: Baseline runs show seed 42 pop=6 while diagnostics show pop=4 — same-seed non-determinism (rand::rng()). Both are within the baseline range (3–8), so both PASS.

---

## Harness Updates

**Check #69 added** — Full schema staleness detector. Warns when the most recent `metrics_history.json` entry with all 20 required fields is >7 days old. Prevents silent SKIP chains caused by `record_metrics.sh` writing only 5-field sparse entries while downstream checks (#31, #32, #41, #42, #44, #49, #60, #62, etc.) require the full 20-field schema. Passes today (entry is 0 days old).

Harness result: **0 errors, 42 warnings** (43 with #69 counted — but #69 is OK, so warning count unchanged).

Active harness warnings (unchanged from yesterday except check counts):
- #15: rand::rng() in 3 hot paths
- #22: VillagerMemory not read (Pillar 2 unimplemented)
- #25: Road threshold >200 (effectively disabled)
- #27: Drought threshold impossible (0.0 ≤ -1.0)
- #33: tick_config event chain broken
- #36: Event suppression via impossible thresholds
- #41: Wood skill decay never fires
- #42: Forest <1% in all 3 seeds
- #47: Bakery planks path blocked
- #48: Seeds 137/777 desert convergence (Pillar 1)
- #60: Seek pct >80% in 2 seeds
- #62: Seek pct >65% in all 3 seeds
- #65: Seed 42 population stagnation (82nd consecutive)
- #66: Zero-event streak (20th consecutive)
- #68: Early grain stall (seed 777 grain=10, planks=0, bread=0)

---

## features.json Updates

| System | Change |
|--------|--------|
| `ecs_core` | Added known_issue: seed 42 reverted to stuck (pop=4) day after 2026-08-07 favorable run; last_verified → 2026-08-08 |
| `events` | Added known_issue: 20th consecutive zero-event run; last_verified → 2026-08-08 |
| `terrain_generation` | last_verified → 2026-08-08 |
| `analytical_erosion` | last_verified → 2026-08-08 |
| `pipe_water` | last_verified → 2026-08-08 |

---

## Priority Actions

These are documented blockers, not new discoveries. Prioritized for dev attention:

1. **P0: rand::rng() non-determinism** (`game/build.rs:631`, `ecs/systems.rs`, `game/mod.rs`) — Hut placement depends on RNG state, not strategy. Seed per-entity RNG or use a deterministic placement algorithm. Blocks meaningful evaluation (check_baselines.sh sees variance ±7 pop).

2. **P0: Forest biome near-zero** (forest_pct <1% all seeds) — Wood supply chain exhausted by tick ~3000. Blocks Workshop→Bakery chain and all mid-game production. Root cause: biome distribution post-WorldState refactor. Fix terrain pipeline forest threshold.

3. **P1: tick_config event chain broken** — `drought_chance`/`harvest_bonus` modifiers never reach simulation. Zero events across 20 consecutive diagnostic runs. Threading fix in `Game::step()`.

4. **P1: VillagerMemory not read** — Pillar 2 (Emergent Complexity) requires local knowledge, not global stockpile access. `VillagerMemory` struct exists but is never queried in `src/ecs/ai.rs`.

5. **P2: Terrain flatness** (98%+ flat, slope_std=0.09–0.11) — Pillar 1 ("Geography Shapes Everything") violated. Hills, ridges, and passes required before Pillar 1 success metric (<30% footprint overlap across 10 seeds) can be met.
