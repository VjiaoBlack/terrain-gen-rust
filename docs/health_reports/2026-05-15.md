# Daily Health Check — 2026-05-15

## Test Results
- **778 passed, 0 failed, 10 ignored** (219s, `cargo test --lib`) — CLEAN RUN
- Previous run (2026-05-14): 777 passed / 1 failed (known flaky `build_site_gets_completed_in_game`)
- Compiler: 11 warnings in lib (unused_mut, dead code — same as prior runs)
- features.json accuracy: within tolerance (validate_features.sh: **0 errors, 30 warnings**)

## Design Drift
- **CLAUDE.md stale** (check #40): still lists `simulation.rs` (split 2026-03-01), still missing `world_state.rs` — misleads new contributors (priority: low but persistent)
- **CLAUDE.md test count stale** (check #29): claims ~190 tests, actual 818 (4×)
- **Pillar 2 gap** (check #22): `VillagerMemory` written in `systems.rs`, never read in `ai.rs` — villagers still use global state, not per-agent memory. Pillar 2 core feature not started.
- **Event modifier chain dead** (check #33): `tick_config` drought/harvest modifiers built at `game/mod.rs:2135–2147`, never passed to `step_water_cycle()` — drought/harvest have zero effect on simulation. CRITICAL: blocks Pillar 2 "systems chain through simulation state."
- **Fog-of-war trivial** (check #21): `sight_range=25`, reveal_radius=15 → 100% exploration by tick 200; not a functional mechanic
- **No roads** (check #25): `ROAD_TRAFFIC_THRESHOLD=300.0` never reached in small settlements (0% Road in all seeds)
- `analytical_erosion.rs` and `scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38)

## Architecture
- No non-test files over 3000 lines — all within limits
- Files approaching split threshold (unchanged from prior): `game/mod.rs` 2592, `game/build.rs` 2306, `tilemap.rs` 2515, `ecs/ai.rs` 2104, `ecs/systems.rs` 2227, `terrain_pipeline.rs` 2232
- `world_state` system: `test_count=0`, `status=needs_work` — canonical sim state has zero unit tests
- `save_load` system: `test_count=0`, `status=needs_tests` (ongoing)

## Game Quality Evaluation

### Per-Seed Results (tick=6000; diagnostics run)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Wolves | Rabbits | Survived |
|------|-----------|------|----------|-------------|--------|--------|--------|---------|----------|
| 42   | 4         | 12   | 3.0      | 4 (1 Hut)   | 30.7%  | 13     | 0      | 0       | Yes      |
| 137  | 11        | 14   | 1.27     | 20 (5 Huts) | 17.9%  | 14     | 0      | 0       | Yes      |
| 777  | 13        | 15   | 1.15     | 20 (5 Huts) | 38.1%  | 15     | 0      | 9       | Yes      |

Non-determinism note: baselines check ran seed 777 independently and returned pop=11 (vs diagnostic=13). Same session, same binary. `rand::rng()` in `game/build.rs` produces ±2-3 Hut variance per run, affecting housing capacity and downstream population capacity.

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight (3.0/cap) | Housing: Overcrowded (need more huts) | Terrain: Diverse | Threats: Low
- Seed 137: Population: Healthy (growing) | Food: **Critical** (1.27/cap, starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing
- Seed 777: Population: Healthy (growing) | Food: **Critical** (1.15/cap, starvation imminent) | Housing: Tight | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | Desert=32%/49% in 2/3 seeds; Forest=0.1-0.7% in all; slope 98.4-98.7% flat (0% steep, 0% cliff); elevation_std=0.09-0.12. Pillar 1 violated — no hills, no mountain constraints, no forest belt |
| Settlement Viability (30%) | 2 | Seed 42: 28th consecutive pop=4/food=12 stagnation; 2/3 seeds Critical food/cap (1.15-1.27); wood nearly exhausted (seed 137=4w, 777=1w) |
| Simulation Health (20%) | 4 | pipe_water_total=244-314, wind_moisture_total=16K-41K, avg_vegetation=0.42-0.44 — all systems active; deducted 1 for broken event modifier chain |
| Emergent Behavior (20%) | 2 | 0 events fired in all 3 seeds (27th+ consecutive zero-event check); 0 roads; prey extinct in 2/3 seeds; same 5 building types across seeds; no outposts |
| **Weighted Average** | **2.4** | 8th consecutive 2.4 — score plateau confirmed |

### Baseline Check
- 3/3 PASS (`scripts/check_baselines.sh`) — seed 42 pop=4, seed 137 pop=11, seed 777 pop=11 (non-deterministic vs diagnostic=13), water within range

## Harness Updates
- **check #42 actually added to `scripts/validate_features.sh`** — was claimed in 2026-05-14 health report and commit message, but the script was never modified (that commit only touched `health_reports/` and `features.json`). Added today: detects when Forest biome < 1.0% in 2+ evaluation seeds in metrics_history. Fires immediately: 3/3 seeds (42=0.3%, 137=0.1%, 777=0.7%). Validate: **0 errors, 30 warnings** (was 29 before fix).
- 2026-05-14 metrics entry was missing from `docs/metrics_history.json` (commit cbd6389 omitted it). Added 2026-05-15 entry today; 2026-05-14 remains absent — note the gap in trend data.

## features.json Updates
- `last_verified` updated to `2026-05-15` for all 25 systems

## Priority Actions
1. **Biome distribution** (`terrain_quality=2`): Desert=32-49% in 2/3 seeds, Forest≈0% in all — diagnose `terrain_pipeline.rs` biome scoring post-WorldState refactor. Root cause of check #41 (wood skill decay), check #42 (forest coverage), check #32 (flat terrain). No woodcutting possible without forest tiles. Fix before any skill/resource tuning.
2. **Food system** (`settlement_viability=2`): food/cap ≤ 1.27 in 2/3 seeds is Critical for 8+ consecutive checks — run farm yield vs hunger diagnostics in `ecs/systems.rs` and `ecs/ai.rs`. Chronic crisis: check #23 fires 8/8 consecutive. Do NOT change hunger constants without data.
3. **Event modifier chain** (`emergent_behavior=2`): `tick_config` at `game/mod.rs:2135-2147` silently discards drought/harvest rain modifiers — fix the pipeline to pass modifiers to `step_water_cycle()` (check #33). Unblocks Pillar 2 "systems chain through simulation state" and drought→water→soil→crops chain.
4. **Flaky tests**: mark `build_site_gets_completed_in_game` and `construction_dust_particles_spawn` as `#[ignore]` to stop false CI failures (checks #14, #35 both fire).
