# Daily Health Check — 2026-05-29

## Test Results
- **778 passed, 0 failed, 10 ignored** (cargo test --lib, ~230s)
- Test count in CLAUDE.md is stale: claims ~190, actual 818 (check #29 fires, 4× off)
- Known-flaky tests `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` remain not marked `#[ignore]` (checks #14, #35 fire). CI false-failure risk persists.

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Terrain is 98.4–98.7% flat across all 3 seeds (steep=0.0%, cliff=0.0%, elevation_std=0.09–0.12). No mountains, no chokepoints. A settlement on seed 137 (48.7% Desert) and seed 777 (38% Water, 19.6% Scrubland) plays out identically in outcome space: both end at pop=11 in most runs.
- Desert=32–49% in 2/3 seeds, Forest=0.1–0.6% in all 3. Wood skill at pure decay (wood_skill=0.5488=1.0×0.9999^6000). No trees → no wood replenishment → construction chain depends on a depleting resource.
- Roads never form (ROAD_TRAFFIC_THRESHOLD=300.0 unreachable at pop=4–11; check #25 fires).

**Pillar 2 — Emergent Complexity:**
- `VillagerMemory` written in `ecs/systems.rs` but never read in `ecs/ai.rs`. Villagers use global stockpile counts (check #22 fires). Core Pillar 2 behavior unimplemented.

**Event System:**
- `tick_config` drought/harvest modifiers at `game/mod.rs:2135–2147` never passed to `step_water_cycle` (check #33). Events have zero effect on simulation. Check #36 fires: events_fired=0 in 2+ seeds for 5/5 recent checks (though seed 777 diagnostics fired WolfSurge today, the structural suppression of drought/harvest remains).
- Drought grain threshold (`grain >= pop*5`) structurally impossible in eval window (check #27).

**Pre-built Granary food trap (new observation):**
- `FoodToGrain` processing triggers at `food > 15` (systems.rs:979, 1391). With pop=11–15, villagers consume ~1–1.5 food/tick. The `food=15` buffer is marginal at high population — seed 137 bounces between food=0 (yesterday) and food=15 (today) depending on rand::rng() task assignment. The Granary fires immediately upon any food spike above 15, preventing buffer accumulation. Check #51 added (see below).

## Architecture
- No non-test source file exceeds 3000 lines ✓
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38)
- `CLAUDE.md` module structure lists removed `simulation.rs`, omits `world_state.rs` (check #40)
- `save_load` at `needs_tests` with 0 tests; `world_state` at `needs_work` with 0 tests (checks #9, #18)
- 11 compiler warnings in game/mod.rs (unchanged from last check)

## Game Quality Evaluation

### Per-Seed Results (Diagnostics, tick=6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Events |
|------|-----------|------|----------|-------------|--------|--------|---------|--------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 13     | Yes      | 0      |
| 137  | 11        | 15   | 1.4      | 12          | 17.9%  | 14     | Yes      | 0      |
| 777  | 4         | 13   | 3.25     | 4           | 38.0%  | 14     | Yes      | 1 (WolfSurge) |

**Non-determinism in this session:** Report-card runs (second process) showed seed 137 pop=11/food/cap=1.1 (Critical) and seed 777 pop=11/food/cap=1.5 (Critical); baseline check (third process) showed seed 777 pop=11. Three sequential runs of seed 777: pop=4, pop=11, pop=11. The diagnostic run is used as primary.

**Notable:** Seed 777 WolfSurge fired in diagnostics (threat_score=10.48, 1 wolf, 6 rabbits). This is the first WolfSurge detected in a diagnostics run for seed 777 — confirms event system partially functional for WolfSurge, while drought/harvest remain structurally suppressed.

Seed 137 food=15 this run (vs food=0 yesterday) — rand::rng() task assignment produced a slightly better farmer allocation; however food/cap=1.4 is still Critical and the food>15 Granary threshold means the buffer can't build beyond this level.

### Report Card Summaries
- Seed 42: `Fragile` (pop=4) / `Tight` (food, no buffer) / `Overcrowded` (housing, cap=4) / `Diverse` (terrain) / `Low` (threat)
- Seed 137: `Healthy` (pop=11, growing) / `Critical` (food, starvation imminent) / `Adequate` (housing) / `Diverse` / `Growing` (threat)
- Seed 777: `Healthy` (pop=11 in report run) / `Critical` (food, starvation imminent) / `Adequate` (housing) / `Diverse` / `Growing` (threat)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes by count, but Desert 32–49% in 2/3, Forest <1% in all, Mountain=0%, slope 98%+ flat; geography cannot shape settlement |
| Settlement Viability (30%) | 2 | All seeds survive; seed 42 structurally stagnant (pop=4 for 35+ consecutive runs); seed 137 food=15 (Critical); housing saturated in 2/3 seeds; rand::rng() makes scores unreliable between runs |
| Simulation Health (20%) | 4 | pipe_water=244–311 (active), wind_moisture=16K–40K (active, varies between seeds), avg_vegetation=0.42–0.44 (healthy); all subsystems functioning |
| Emergent Behavior (20%) | 2 | WolfSurge fired on seed 777 diagnostics today (partial credit); drought/harvest never fire (tick_config chain broken, check #33); roads never form (check #25); seeds 42/777 convergent pop=4 this run |
| **Weighted Average** | **2.4** | 2×0.3 + 2×0.3 + 4×0.2 + 2×0.2 = 2.4; score plateau confirmed (check #17 fires: 6+ consecutive checks at 2.4) |

### Baseline Check
All 3 seeds **PASS** (seed 42: pop=4, water=30.7%, biomes=13; seed 137: pop=11, water=17.9%, biomes=14; seed 777: pop=11, water=37.9%, biomes=14).

### Validate Features
**0 errors, 37 warnings** (up from 35 yesterday: check #51 added (+1), multi-seed housing saturation check #43 fires for seeds 42+777 (+1), prey extinction check #31 fires again with seed 42 rabbits=0 (+1), total starvation check #50 stops firing as food=15 today (−1)).

## Harness Updates
- **Added check #51** (`validate_features.sh`): "FoodToGrain Granary threshold safety" — fires when the `food > N` threshold in `FoodToGrain` recipe in `systems.rs` is below 20. The `food=15` threshold is insufficient for populations ≥ 10 (consumption ~1–1.5 food/tick). Causes observed food=0 crash (seed 137, 2026-05-28) and near-starvation bouncing (food=15 today). Fix: adaptive threshold `max(N, population * 2)`. Fires on current code (threshold=15).

## features.json Updates
- All 25 systems: `last_verified` updated to `2026-05-29`
- `ecs_core` known_issues: added note about FoodToGrain threshold at `systems.rs:979,1391` (`food > 15`) — insufficient buffer at pop ≥ 10, Granary fires immediately upon any spike above threshold, preventing safe food accumulation (check #51 added 2026-05-29)
- `events` known_issues: noted today's WolfSurge on seed 777 in diagnostics run — confirms WolfSurge CAN fire from diagnostics path (previously observed only in report-card/baseline runs)

## Priority Actions
1. **Fix seeded per-entity RNG** (`ecs_core`): seed 777 showed pop=4, 11, 11 across three sequential same-binary runs today. Until RNG is deterministic, every other metric is noise. Replace `rand::rng()` in `game/build.rs` and `ecs/systems.rs` breeding/auto-build paths with seeded per-entity RNG.
2. **Fix biome distribution in `terrain_pipeline.rs`** (`terrain_generation`): Desert 32–49%, Forest <1%, Mountain=0% in all seeds. No trees = no wood = no construction chain beyond starting wood. This single fix unblocks wood skill, Workshop/Bakery production, and road formation.
3. **Fix event modifier chain** (`events`): `tick_config` modifiers at `game/mod.rs:2135–2147` built but never passed to `step_water_cycle`. Drought/harvest have zero effect; only WolfSurge fires (from a separate code path). One-line fix: pass `tick_config.rain_rate` to `step_water_cycle`.
4. **Fix Granary food threshold** (`game_loop`): raise `FoodToGrain` trigger from `food > 15` to `food > max(15, population * 2)` in `src/ecs/systems.rs:979,1391`. Prevents food buffer depletion spiral at pop ≥ 10.
