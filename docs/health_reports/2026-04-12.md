# Daily Health Check — 2026-04-12

## Test Results
- **778 passed, 0 failed, 10 ignored** (252s — slower than expected ~7s; `configurable_map_size_512`, `event_system_serialization`, `save_load_round_trip` each triggered the >60s warning but completed OK)
- No flaky failures today (lucky draw — race condition NOT fixed; hardcoded `/tmp/` paths remain)
- Total `#[test]` annotations in `src/`: **818** — CLAUDE.md says "~190 tests" for lib, significantly stale

### Test count accuracy
All within tolerance per `validate_features.sh`. No discrepancies.

---

## Design Drift

### 1. ARCHITECTURE.md stale — 4th consecutive day (ESCALATE)
Still documents pre-split file sizes: `simulation.rs` (4844 lines, deleted), `game/mod.rs` (6073 vs actual 2592), `game/render.rs` (4094, deleted), `ecs/mod.rs` (7163 vs actual 17). Refactor Priorities 1–4 are complete but the doc still presents them as future work. This actively misleads agents picking up tasks.

### 2. VillagerMemory never read in AI (Pillar 2)
Unchanged: `VillagerMemory` written in `systems.rs` but not consulted in `ai.rs` (`grep` finds 1 mention in ai.rs — a type import, not a read). AI still uses 27 global stockpile reads. Phase 2 knowledge architecture not started.

### 3. Fog-of-war trivially 100% (Pillar 1/2)
`exploration_pct = 100.0` on all 3 seeds at tick 6000. Genuine discovery not functioning. `simulation_maps.known_issues` already documents this.

### 4. Road auto-build never fires in evaluation window
`ROAD_TRAFFIC_THRESHOLD = 300.0` (game/mod.rs:530). With 4-11 villagers and traffic decay every 10 ticks, the threshold is unreachable before tick 6000. All seeds show `"Road": 0.0` in biome distribution. Design principle: "Roads auto-build from traffic" — currently not observable.

### 5. No production-chain buildings in auto-build window
Seeds show Farm/Hut/Workshop/Garrison/Stockpile only. Granary, Bakery, Smithy never constructed at tick 6000 across any seed. Production chains (food→grain→bread, stone→masonry) unreachable in the evaluation window.

### 6. Seed 42 dies at tick 6668 in play mode
`--play --seed 42 --ticks 12000` → GAME OVER at tick 6668 (peak pop=4). The baseline (`--screenshot --ticks 12000`) captures tick 6000 state where pop=4 is alive, masking the eventual death. `survived=true` baseline passes but is misleading.

---

## Architecture

### Module sizes (validate_features.sh: 0 errors, 3 warnings)
| File | Lines | Status |
|------|-------|--------|
| `src/game/mod.rs` | 2592 | OK (was 6073, split done) |
| `src/tilemap.rs` | 2515 | Getting big (26-variant Terrain enum) |
| `src/game/build.rs` | 2306 | Getting big |
| `src/simulation/moisture.rs` | 1867 | Largest sim module |
| `src/simulation/wind.rs` | 1540 | OK |
| `src/game/render/mod.rs` | 1300 | OK post-split |

No non-test files exceed 3000 lines. `src/ecs/tests.rs` is 7164 lines (test file, excluded from check).

### Compiler warnings
11 warnings (release build), unchanged from yesterday.

### Persistent validate_features.sh warnings (3)
1. `save_load` status='needs_tests', test_count=0 — no dedicated unit tests added
2. `src/game/tests.rs:212` — hardcoded `/tmp/test_savegame.json`
3. `src/game/tests.rs:703` — hardcoded `/tmp/test_events_save.json`

---

## Game Quality Evaluation

### Per-Seed Diagnostics (all at game tick 6000; `--screenshot --ticks 12000` uses `half_speed_base=true` → 12000 loop iterations = 6000 game ticks)

| Seed | Population | Food | Food/Cap | Hut Cap | Water% | Biomes | pipe_water | wind_moisture | Events |
|------|-----------|------|----------|---------|--------|--------|-----------|--------------|--------|
| 42   | 4         | 12   | 3.0      | 4       | 30.7%  | 13     | 298.19    | 17,811       | none   |
| 137  | 11        | 14   | 1.3      | 12      | 17.9%  | 14     | 244.78    | 15,945       | none   |
| 777  | 11        | 15   | 1.4      | 20      | 38.0%  | 14     | 313.91    | 41,498       | none   |

**Non-determinism confirmed:** Seed 777 population was 13 on 2026-04-11, 11 today (-2). Food was 18→15 (-3). Same seed, same tick, different outcome across runs.

Slope distribution: 98.4–98.7% flat, ≤1.6% gentle, 0% steep/cliff across all seeds. Persistently flat terrain.

### Report Card Summaries
- **Seed 42**: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded | Terrain: Diverse | Threats: Low — *dies at tick 6668 in play mode*
- **Seed 137**: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing
- **Seed 777**: Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight | Terrain: Diverse | Threats: Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13–14 biomes (excellent). Water 17.9–38.0% (good). Elevation std 0.09–0.12, 98.4%+ flat tiles — persistent critical issue. |
| Settlement Viability (30%) | 2 | All 3 survive at tick 6000. Seed 42 dies at 6668 in play mode. Food Critical on 2/3 seeds (1.3–1.4/cap). Avg pop 8.7. No production-chain buildings. |
| Simulation Health (20%) | 4 | All systems active (pipe_water 245–314, wind_moisture 16K–41K, avg_vegetation 0.41–0.44). Moisture chain not fully connected per design (needs_work status). |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4/11/11, Desert 0.1%/32%/49%, water 18–38%). No events at tick 6000 on any seed. 0% road coverage. Building variety limited (5 types). |
| **Weighted Average** | **2.90** | 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = **2.90** — unchanged for 3rd consecutive day |

### Baseline Check
```
Running seed 137 for 12000 ticks...  PASS: pop=11, water=17.9%, biomes=14
Running seed 42 for 12000 ticks...   PASS: pop=4, water=30.7%, biomes=13
Running seed 777 for 12000 ticks...  PASS: pop=11, water=38.0%, biomes=14
Results: 3 passed, 0 failed
```

**Baseline note:** Seed 42 `water_coverage_pct` was 61.6% in the 2026-04-05 initial baseline notes, now 30.7% — a ~50% drop since the Stage 4 (WorldState) refactor. Baseline range (min 5%, max 80%) is too wide to catch this. Seeds 137/777 also dropped (48.5%→17.9%, 68.8%→38.0%). Water coverage regression post-refactor warrants investigation.

---

## Harness Updates

### New check #13 added to `validate_features.sh`
**Check #13: Split product files exist.** Verifies that `src/ecs/tests.rs`, `src/game/tests.rs`, `src/game/input.rs`, `src/game/water_cycle.rs`, `src/game/fire.rs`, `src/game/particles.rs` all exist. Complements checks #11–12 (which verify old monoliths are gone) — now both directions are guarded. Passes on current code (0 errors).

---

## features.json Updates
- `terrain_generation.last_verified`: 2026-04-12
- `simulation_wind.last_verified`: 2026-04-12
- `game_loop.last_verified`: 2026-04-12
- `events.last_verified`: 2026-04-12
- `ecs_core.last_verified`: 2026-04-12
- `events.known_issues`: added road auto-build threshold note (ROAD_TRAFFIC_THRESHOLD=300 never reached at small pop)
- `ecs_core.known_issues`: added simulation non-determinism note (seed 777 pop 13→11 across days)

---

## Score Trend
| Date | Score | Notes |
|------|-------|-------|
| 2026-04-07 | 3.5 | |
| 2026-04-08 | 3.7 | |
| 2026-04-09 | 3.7 | Stage 4 (WorldState) merged |
| 2026-04-10 | 2.9 | Score drop; water coverage regression visible |
| 2026-04-11 | 2.9 | |
| 2026-04-12 | 2.9 | Plateau — no code changes, same issues |

Score has plateaued at 2.9 for 3 days. No application code changes since 2026-04-10. The drop from 3.7→2.9 coincides with the WorldState (Stage 4) refactor merge.

---

## Priority Actions

1. **Fix `/tmp/` flaky tests** [game_loop — 4th day unresolved]: `save_load_round_trip` (tests.rs:212) and `event_system_serialization` (tests.rs:703) use hardcoded `/tmp/` paths. Replace both with `format!("/tmp/test_{}_savegame.json", std::process::id())`. Low risk, high impact — eliminates intermittent CI failures.

2. **Update ARCHITECTURE.md** [architecture — 4th day unresolved]: Correct stale file sizes/paths. `simulation.rs` → deleted (split to `src/simulation/`). `game/mod.rs` 6073 → 2592. `game/render.rs` → deleted (split to `src/game/render/`). `ecs/mod.rs` 7163 → 17. Refactor Priorities 1–4 are done — mark as complete. Add `world_state.rs`, `hydrology.rs`, `src/game/input.rs`, etc. Blocking accurate agent orientation.

3. **Investigate water coverage drop post-WorldState refactor** [new]: Baseline notes show seeds 42/137/777 water coverage at 61.6%/48.5%/68.8% pre-Stage-4, now 30.7%/17.9%/38.0%. This ~50% drop may affect vegetation and moisture chains. Add a diagnostic test that compares `pipe_water_total` before/after a step to verify volume conservation.
