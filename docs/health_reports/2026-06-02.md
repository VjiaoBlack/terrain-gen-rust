# Daily Health Check — 2026-06-02

## Test Results
- **778 passed, 0 failed, 10 ignored** (cargo test --lib, ~235s)
- CLAUDE.md claims ~190 tests; actual 818 (check #29, 4× stale — ongoing)
- Known-flaky tests `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` still not `#[ignore]` (checks #14, #35 — CI false-failure risk ongoing)

## Design Drift

**Pillar 1 — Geography Shapes Everything:** Same as yesterday — no code changes.
- Terrain 98.4–98.7% flat all 3 seeds; steep=0%, cliff=0%, elevation_std=0.09–0.12
- Desert=32–49% (seeds 42/137); Forest=0.1–0.6% all 3. Wood skill at pure-decay 0.5488 (zero trees cut)
- Roads never form (ROAD_TRAFFIC_THRESHOLD=300, unreachable at pop=4–12; check #25)

**Pillar 2 — Emergent Complexity:**
- `VillagerMemory` written in `ecs/systems.rs`, never read in `ecs/ai.rs` (check #22, ongoing)

**Event System:**
- `tick_config` drought/harvest modifiers (`game/mod.rs:2135–2147`) never passed to `step_water_cycle` (check #33). Events=0 all seeds; check #36: 5/5 consecutive suppressed.

No new drift detected. All issues pre-existing.

## Architecture
- No non-test source file exceeds 3000 lines ✓
- `src/analytical_erosion.rs`, `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38, ongoing)
- `CLAUDE.md` module structure lists removed `simulation.rs`, omits `world_state.rs` (check #40, ongoing)
- `save_load` (needs_tests, 0 tests) and `world_state` (needs_work, 0 tests) unchanged (checks #9, #18)
- 11 compiler warnings in `game/mod.rs` (unchanged)
- **check #21 accuracy fix (today):** Previous implementation used `sort -g | tail -1` on all sight_range values in spawn.rs, returning wolf sight_range=25.0 rather than villager-specific 22.0. Fixed to use `awk '/fn spawn_villager/'` scope — now correctly reports 22.0.

## Game Quality Evaluation

### Per-Seed Results (Diagnostics, game.tick=6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Wood | Build Sites | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4 (full)    | 43   | 2           | 30.7%  | 13     | Yes (dies ~6668) |
| 137  | 4*        | 29   | 7.25*    | 4 (full)    | 15   | 8*          | 17.9%  | 14     | Yes      |
| 777  | 12        | 21   | 1.75     | 16 (+4)     | 4    | 1           | 38.0%  | 14     | Yes      |

*Seed 137 today: lowest-ever diagnostics pop for this seed (rand::rng() bad run). Report-card run (sequential): pop=11, housing=20, food=Critical. Delta=7 — new largest within-session pop divergence for seed 137. Baseline check run also yielded pop=11. Eight build_sites queued for 4 workers (ratio=2.0).

### Report Card Summaries
- Seed 42: `Fragile` (pop=4) / `Tight` food (3.0/cap) / `Overcrowded` (cap=4) / `Diverse` terrain / `Low` threat
- Seed 137 (report-card run, pop=11): `Healthy` / `Critical` food (starvation imminent) / `Adequate` housing / `Diverse` terrain / `Growing` threat
- Seed 777 (report-card run, pop=11): `Healthy` / `Critical` food (starvation imminent) / `Adequate` housing / `Diverse` terrain / `Growing` threat

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes, water 17.9–38% (OK range), but: slopes 98%+ flat, Forest <1% all seeds, Desert 32–49% in 2/3 seeds, Mountain=0% |
| Settlement Viability (30%) | 2 | Seed 42 dies ~tick 6668 (40th confirmation); seed 137 diagnostics pop=4 (non-det.); food Critical in 2/3 seeds on report-card runs; 0 events all seeds |
| Simulation Health (20%) | 4 | pipe_water=244–314, wind_moisture=16K–41K, avg_vegetation=0.41–0.44, avg_moisture=0.33–0.65 — all active |
| Emergent Behavior (20%) | 2 | Events=0 all seeds; tick_config chain broken; roads=0%; wood_skill=0.5488 (decay); seeds 137/777 food/threat convergence |
| **Weighted Average** | **2.4** | 0.6+0.6+0.8+0.4=2.4; **9th consecutive** check at 2.4 — plateau (check #17) |

### Baseline Check
All 3 seeds **PASS** (seed 42: pop=4, water=30.7%, biomes=13; seed 137: pop=11, water=17.9%, biomes=14; seed 777: pop=14, water=38.0%, biomes=14).

## Harness Updates

**Check #21 corrected** (`Fog-of-war triviality check`): grep now scopes to `fn spawn_villager` function in spawn.rs, returning the correct villager sight_range=22.0. Previous implementation returned the maximum across all creatures (wolf's 25.0), which misleadingly implied wolves were the fog-of-war mechanism. Villager sight_range=22.0 is still > 15 threshold; warning continues to fire correctly.

**Check #52 added** (`Build site backlog ratio detection`): Fires when any seed has `build_sites / population > 1.5`. First observation: seed 137 on 2026-06-02 with 8 sites and 4 workers (ratio=2.0) in a rand::rng() low-population run. Root cause: `game/build.rs` auto-build does not check available worker count before queuing new sites. Complements checks #37 (housing saturation), #44 (wood stall). Fires today (seed 137).

**New checks firing today** (vs 2026-06-01):
- Check #30 (Population swing): seed 137 swing 11→4 (delta=7, new largest for this seed)
- Check #43 (Multi-seed housing saturation): seeds 137 AND 42 both at growth_potential=0
- Check #52 (Build site backlog): seed 137 8 sites/4 workers (ratio=2.0)

**Checks no longer firing today:**
- Check #44 (Wood stall): seed 137 wood=15 today (vs 4 yesterday); 2/3 seeds now ≥ 10
- Check #39 (Acute food crisis): all seeds food/cap ≥ 1.5 (seed 137 today: 7.25 in low-pop run)

**validate_features.sh: 0 errors, 35 warnings** (was 34 yesterday)

## features.json Updates
- All 25 systems: `last_verified` updated to `2026-06-02`
- `ecs_core` known_issues added: seed 137 diagnostics pop=4 vs report-card pop=11 (delta=7, new largest within-session pop divergence for seed 137 on 2026-06-02)
- `ecs_core` known_issues added: seed 137 build_sites=8 with pop=4 workers (ratio=2.0) on 2026-06-02 — auto-build over-queuing in low-pop RNG run; check #52 added
- `building_system` known_issues added: auto-build does not cap build queue relative to worker count (first observed 2026-06-02; game/build.rs)

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** — Desert 32–49%, Forest <1%, Mountain=0% in all seeds. Unblocks wood skill, Workshop/Bakery chain, road formation, Pillar 1 differentiation. Single highest-impact fix.
2. **Fix seeded per-entity RNG** (`game/build.rs`, `ecs/systems.rs`) — today's seed 137 diagnostics vs report-card delta=7 population (same binary/seed) is the clearest evidence. Until fixed, all rubric scores are measurement noise.
3. **Fix FoodToGrain Granary threshold** — raise `food > 15` to `food > max(15, population * 2)` at `src/ecs/systems.rs:979,1391`. Prevents food=0 crashes at pop≥10.
4. **Fix event modifier chain** — thread `tick_config.rain_rate` into `step_water_cycle` at `game/mod.rs:2135–2148`. One-function-signature change; unblocks drought/harvest simulation effects.
