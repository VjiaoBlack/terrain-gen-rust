# Daily Health Check — 2026-04-08

## Test Results
- **Run 1: 777 passed, 1 failed, 10 ignored** — `construction_dust_particles_spawn` failed
- **Run 2: 778 passed, 0 failed, 10 ignored** — all pass
- **Flaky test confirmed**: `construction_dust_particles_spawn` (src/game/tests.rs:2840) fails ~0.3% of runs. Cause: particle spawns with `rng.random_range(0..4) == 0` (25% chance per tick); over 20 ticks the probability of zero spawns is ~0.3%. Non-deterministic RNG dependency. This was noted as "fixed" in the April 7 report but was not fixed — only the specific run happened to pass.
- Test count accuracy: all counts within tolerance per `validate_features.sh`

## Design Drift

### threat_score known_issue is RESOLVED
features.json listed "threat_score not exposed in diagnostics JSON — event readiness unobservable" as an open known_issue. Today's diagnostics clearly show `"threats": {"threat_score": X, ...}`. This was fixed in commit `8af4abd` (Wire --diagnostics and --report-card CLI flags). **Removed from features.json today.**

### Events: WolfSurge fires for seed 137
Unlike April 7 (0/3 seeds had events), seed 137 today produced a `WolfSurge` at tick 6000. The wolf event system is functional. Drought remains structurally impossible: seed 137 has grain=0 with pop=11 (needs grain≥55 during Summer). At tick 6000 (effective Spring), the condition will never be met.

### half_speed_base halves evaluation tick count
`--screenshot` mode sets `game.half_speed_base = true` (main.rs:206), which causes `game.step()` to skip every other frame at speed 1. Result: `--ticks 12000` produces `"tick": 6000` in diagnostics output. All evaluation runs are at 6000 effective simulation ticks, not 12000. Baselines were created with this same behavior so checks pass, but evaluation commands in the agent prompt imply 12K ticks of simulation. **Undocumented behavior.**

### Villager omniscience (Pillar 2 gap)
VillagerMemory is written (DangerZone, ThreatReport) but never read in ai.rs decisions (still 15 occurrences of global stockpile reads). Pillar 2 core features unimplemented. Known and tracked.

### ARCHITECTURE.md still out of date
Identified as priority action on April 7. Not updated. Still shows pre-refactor file sizes and single-file structure for systems that have been split. File map needs full rewrite against actual `src/` tree.

## Architecture

### File sizes (current)
| File | Lines | Status |
|------|-------|--------|
| `src/ecs/tests.rs` | 7164 | Test file — OK |
| `src/game/tests.rs` | 2913 | Test file — OK |
| `src/tilemap.rs` | 2515 | TerrainDef refactor planned |
| `src/game/mod.rs` | 2592 | Acceptable post-refactor |
| `src/game/build.rs` | 2306 | Approaching limit |
| `src/ecs/systems.rs` | 2227 | Coherent — OK |
| `src/ecs/ai.rs` | 2104 | Coherent — OK |
| `src/simulation/moisture.rs` | 1867 | Candidate for split |

No files over 3000 lines (excluding test files). validate_features.sh: 0 errors, 0 warnings.

### Untested files (0 tests, still)
`fire.rs`, `water_cycle.rs`, `particles.rs`, `world_state.rs` — all 0 tests. Known issues in features.json.

### Compiler warnings
11 warnings in release build: unused variable `tick_config`, dead constant `DIRECTIONS` (chokepoint.rs:51), unused methods (`sunset_hour`, `wrapping_idx`, `map_terrain_glyph`, `draw_map_building_marker`), unused function `run_sediment_steps`. Low priority.

## Game Quality Evaluation

*Note: All runs use `--screenshot --ticks 12000` which due to `half_speed_base=true` produces only 6000 effective simulation ticks. All seeds are at Spring of Year 2 at evaluation time.*

### Per-Seed Results (effective tick 6000, auto-build)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Events |
|------|-----------|------|----------|-------------|--------|--------|----------|--------|
| 42   | 4         | 12   | 3.8      | 4           | 30.7%  | 13     | ✓        | 0      |
| 137  | 11        | 16   | 1.5      | 20          | 17.9%  | 14     | ✓        | 1 (WolfSurge) |
| 777  | 11        | 18   | 1.5      | 20          | 37.9%  | 14     | ✓        | 0      |

### Report Card Summaries
- Seed 42: Population Fragile (4, one bad winter away) | Food Tight (3.8/cap) | Housing Overcrowded (cap=4) | Terrain Diverse (13 biomes) | Threats Low (score=10.7)
- Seed 137: Population Healthy (11, growing) | Food Critical (1.5/cap, starvation imminent) | Housing Adequate (cap=20) | Terrain Diverse (14 biomes) | Threats Growing (score=25.8, WolfSurge active)
- Seed 777: Population Healthy (11, growing) | Food Critical (1.5/cap) | Housing Adequate (cap=20) | Terrain Diverse (14 biomes) | Threats Growing (score=28.0)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 4 | 13-14 biomes ✓; water 17.9-37.9% (in range) ✓; elevation_std 0.09-0.11 ✓; seeds diverge strongly (desert 0-49%, water 18-38%) ✓; slope almost entirely flat (98.4-98.7% flat, <0.01% steep) — limits geographic variety |
| Settlement Viability (30%) | 3 | All 3 survive ✓; avg pop 8.7 (ok tier); 2/3 seeds at Critical food; seed 42 Fragile at pop=4; housing varies (Overcrowded→Adequate); growth potential 0 or 9 |
| Simulation Health (20%) | 5 | pipe_water 244-305 ✓; wind_moisture 16602-41160 ✓; avg_vegetation 0.42-0.44 ✓; all simulation systems active |
| Emergent Behavior (20%) | 3 | Seeds diverge strongly (pop 4/11/11, desert 32%/49%/0%, water 18-38%) ✓; building variety ✓; 1/3 seeds had WolfSurge (improvement over 0/3) ✓; drought still structurally impossible |
| **Weighted Average** | **3.7** | Improvement from 3.5 (April 7) due to events firing |

### Baseline Check
- **3/3 baselines PASS**: seed 42 (pop=4, water=30.9%, biomes=13), seed 137 (pop=11, water=17.9%, biomes=14), seed 777 (pop=11, water=38.0%, biomes=14)
- Note: baseline notes say "water=61.6%" for seed 42 (from April 5, pre-0D-refactor). Actual values have shifted significantly but tolerances are wide enough to pass. Baseline notes should be updated.

## Harness Updates
- **Added check** to `scripts/validate_features.sh`: verifies `threat_score` field exists in `collect_diagnostics()` output (grep src/game/mod.rs). Prevents silent regression of diagnostics coverage.
- **Added check**: flags `last_verified` dates older than 30 days as a warning.

## features.json Updates
- `events.known_issues`: removed "threat_score not exposed in diagnostics JSON" (RESOLVED — field present since commit 8af4abd)
- `events.last_verified`: updated to 2026-04-08
- `game_loop.known_issues`: added note that `construction_dust_particles_spawn` is a flaky test (~0.3% failure rate) due to probabilistic particle spawning

## Priority Actions
1. **Fix flaky test `construction_dust_particles_spawn`** (src/game/tests.rs:2840) — run 50+ steps instead of 20, or use a known-good seeded sequence that guarantees particle spawn. The 1/4 probability over 20 ticks gives ~0.3% false-failure rate which will compound across CI runs.
2. **Update ARCHITECTURE.md** — file map is significantly stale (listed as priority April 7, still not done). Pre-refactor sizes and single-file descriptions for split modules confuse new agents.
3. **Document half_speed_base in evaluation harness** — add a note to the health check prompt that `--screenshot --ticks N` produces N/2 effective simulation ticks. Update baselines to reflect post-0D-refactor terrain values.
