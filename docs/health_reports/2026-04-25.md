# Daily Health Check — 2026-04-25

## Test Results
- **778 passed, 0 failed, 10 ignored** (`cargo test --lib`)
- Test suite size: 818 `#[test]` annotations in `src/` (unchanged)
- CLAUDE.md still claims "~190 lib tests" — actual count is 778 (4× stale; no code change today)

## Design Drift

**Pillar 2 gap (no change):** `VillagerMemory` is written in `ecs/systems.rs` but never read in `ecs/ai.rs` (check #22). Villagers still use global stockpile state for all decisions. Per-villager memory exists in code but is not wired to behavior.

**Pillar 1: Roads don't emerge (no change):** `ROAD_TRAFFIC_THRESHOLD=300.0` (`game/mod.rs:530`) prevents road formation in settlements of pop 4–12. Design doc says "Roads auto-build from traffic." Zero Road coverage across all 3 seeds at tick 6000.

**Event system (no change):** Drought requires `grain >= pop * 5` (`events.rs:354`). At tick 6000 seeds have grain=0–22 for pop=4–12 — threshold requires 20–60 grain. Drought is structurally impossible in the evaluation window. New harness check #27 now flags this statically.

**Fog-of-war (no change):** `sight_range=25.0` (`spawn.rs`) + initial reveal radius=15 (`game/mod.rs`) trivially reveals 100% of map in all seeds. Exploration is not a meaningful game mechanic.

**No drift newly discovered today.** All drifts are tracked known issues.

## Architecture

No new files exceeded 3000-line non-test threshold. No files created or deleted since 2026-04-24.

Notable sizes (for tracking):
- `ecs/tests.rs`: 7164 lines (test file, excluded from threshold)
- `tilemap.rs`: 2515 lines — approaching 3000-line split threshold
- `game/build.rs`: 2306 lines — approaching split threshold
- `ecs/systems.rs`: 2227 lines — approaching split threshold
- `ecs/ai.rs`: 2104 lines — approaching split threshold

`features.json` known_issues are current. No resolved issues found.

## Game Quality Evaluation

### Per-Seed Results (tick=6000, half_speed_base)

| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7   | 14     | Yes      |
| 137  | 11        | 16   | 1.46     | 20          | 17.9   | 14     | Yes      |
| 777  | 12        | 18   | 1.50     | 20          | 37.9   | 15     | Yes      |

Seeds 137 and 777 show higher population than 2026-04-24 (8→11, 8→12). No code changes — this is rand::rng() non-determinism (check #15). Seed 42 is unchanged for the 15th consecutive check.

### Simulation Vitals
| Seed | pipe_water_total | wind_moisture_total | avg_vegetation | avg_moisture |
|------|-----------------|---------------------|----------------|--------------|
| 42   | 298.19          | 17,810              | 0.44           | 0.46         |
| 137  | 244.62          | 16,603              | 0.42           | 0.32         |
| 777  | 305.27          | 41,160              | 0.42           | 0.64         |

All simulation systems active. Seed 777 wind_moisture is 2.5× seeds 42/137 — likely related to higher water coverage (37.9%).

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight (3.0/cap) | Housing: **Overcrowded** (need more huts) | Terrain: Diverse | Threats: Low
- Seed 137: Population: Healthy | Food: **Critical** (1.46/cap) | Housing: Adequate | Terrain: Diverse | Threats: Growing
- Seed 777: Population: Healthy | Food: **Critical** (1.50/cap) | Housing: Adequate | Terrain: Diverse | Threats: Growing

### Rubric Scores

| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14-15 biomes (diverse), but 98.5% flat terrain (no steep/cliff tiles), Desert dominates 2/3 seeds (32%, 48.7%). Water 17-38% (in range). |
| Settlement Viability (30%) | 2 | All 3 survive, avg pop=9. But 2/3 seeds food Critical (1.46-1.5/cap). Seed 42 locked at pop=4 with 0 growth potential for 15th check, wood=43 surplus unused. |
| Simulation Health (20%) | 4 | pipe_water, wind_moisture, vegetation all active across all seeds. Reasonable values. |
| Emergent Behavior (20%) | 3 | Seed divergence exists (pop 4 vs 11 vs 12). But 0 events fire across all seeds, no roads form, seed 42 completely static. Non-determinism (not emergent design) causes most between-run variation. |
| **Weighted Average** | **2.9** | `3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 0.9+0.6+0.8+0.6` |

**This is the 15th consecutive check at 2.9/5.0.** Score plateau check (#17) fires.

### Baseline Check
All 3 baselines PASS:
- Seed 42: PASS (pop=4, water=30.7%, biomes=13)
- Seed 137: PASS (pop=11, water=17.9%, biomes=14)
- Seed 777: PASS (pop=12, water=38.0%, biomes=15)

## Harness Updates

**New check #27 added to `scripts/validate_features.sh`:**
- Detects drought grain threshold impossibility via static analysis of `src/game/events.rs`
- Pattern: `grain >= villager_count * N` where N > 3 → warns that drought can't fire in eval window
- Fires today: `grain >= pop * 5` with pop=4-12 needing 20-60 grain; eval seeds have 0-22 grain
- Total warnings: 16 (was 15)

## features.json Updates
- Updated `last_verified` to `2026-04-25` for all 25 systems
- Added to `terrain_generation.known_issues`: Seed 42 biome count increased 13→14 (non-deterministic, worth monitoring)
- No status changes

## Priority Actions
1. **[Seed 42 housing block]** Auto-build never places a second Hut despite wood=43 surplus and pop=4 at cap. `game/build.rs` Hut placement priority is broken for this seed. 15 consecutive checks with 0 growth potential. Diagnose before changing constants.
2. **[Food crisis]** 2/3 seeds food Critical at tick 6000 (1.46-1.5/cap). Hunger rate vs farm yield imbalance. Check `system_hunger` and `system_farms` data flow before adjusting rates.
3. **[Drought threshold]** `events.rs:354` requires `grain >= pop*5`. Reduce to `pop*2` or `pop*3` so Summer drought can actually fire. This and wolf surge calibration would unlock Emergent Behavior score improvement.
4. **[VillagerMemory]** Wire `VillagerMemory` reads in `ai.rs` — addresses Pillar 2 gap and is prerequisite for local-information AI decisions.
5. **[Road threshold]** Reduce `ROAD_TRAFFIC_THRESHOLD` from 300.0 to ≤100.0 so roads emerge naturally in small settlements (Pillar 1 design: "Roads auto-build from traffic").
