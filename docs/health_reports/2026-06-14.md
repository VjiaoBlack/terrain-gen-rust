# Daily Health Check — 2026-06-14

## Test Results
- **778 passed, 0 failed, 10 ignored** (336.51s)
- Test count accuracy: within tolerance — all features.json counts match actual source
- Known-flaky: `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn` all unignored (3× CI false-failure risk per check #55); no flaky test fired today

## Design Drift

**Pillar 2 gap — VillagerMemory not wired into AI (check #22 fires)**
`ecs/components.rs` defines `VillagerMemory`; `ecs/systems.rs` writes it; `ecs/ai.rs` never reads it. AI still uses global stockpile counts (15 occurrences in `ai.rs`). Design doc (`docs/design/pillar2_emergence/per_villager_memory.md`) requires local-only decision-making. No progress since first flagged.

**Event modifier chain dead code (check #33 fires)**
`game/mod.rs:2135-2147` builds `tick_config` with drought (`rain_rate*=0.4`) and harvest (`rain_rate*=1.5`) but `step_water_cycle()` takes `(bool, f64)` — `tick_config` is silently discarded. Drought and harvest events have zero effect even when they fire.

**Fog-of-war trivially complete (check #21 fires)**
`exploration_pct=100.0` in all 3 seeds. `sight_range=22`, initial reveal radius=15 trivially fill a 70×25 map. Pillar 3 Explore phase is inoperative.

**CLAUDE.md stale module structure (check #40 fires)**
Still lists `simulation.rs` (split 2026-03-01); still missing `world_state.rs` (added in 0D refactor). No change.

**KNOWN_CONFLICTS.md gap: design doc conflicts unresolved**
8 active conflicts (resource map, fire system, entity glyphs, danger avoidance, etc.) and 6 ownership gaps (wind system, terrain variants, save/load migration, bridge priority). None resolved since filing. These block implementation of Pillars 1-2 design docs.

## Architecture

- No files over 3000 lines (threshold met; largest non-test file: `tilemap.rs` ~2515 lines)
- Six files in 2000–2600 line range: `game/mod.rs` (2592), `tilemap.rs` (2515), `game/build.rs` (2306), `terrain_pipeline.rs` (2232), `ecs/systems.rs` (2227), `ecs/ai.rs` (2104)
- `analytical_erosion.rs` and `src/scripting/` absent from ARCHITECTURE.md file map (check #38 — no change)
- `world_state.rs` (74 lines, 0 tests, `needs_work`) has no unit tests and no ARCHITECTURE.md entry

## Game Quality Evaluation

### Per-Seed Results (diagnostics run, tick=6000)
| Seed | Pop | Food | Food/Cap | Housing Cap | Growth Pot | Water% | Biomes | Flat% | Survived |
|------|-----|------|----------|-------------|------------|--------|--------|-------|---------|
| 42   | 4   | 12   | 3.00     | 4           | 0          | 30.7%  | 13     | 98.4% | yes     |
| 137  | 11  | 11   | 1.00     | 16          | 5          | 17.9%  | 14     | 98.7% | yes     |
| 777  | 11  | 16   | 1.45     | 20          | 9          | 37.9%  | 15     | 98.6% | yes     |

Note: Report-card run (sequential, same binary) showed seed 777 pop=12 (delta=1) — rand::rng() non-determinism at lower end of recent variance range.

### Report Card Summaries
- Seed 42: `Fragile (one bad winter away) | Tight (no buffer) fpc=3.0 | Overcrowded (need more huts) | Diverse (13 biomes) | Low threat`
- Seed 137: `Healthy (growing) | Critical (starvation imminent) fpc=1.4 | Adequate (room to grow) | Diverse (14 biomes) | Growing threats`
- Seed 777: `Healthy (growing) | Critical (starvation imminent) fpc=1.2 | Adequate (room to grow) | Diverse (15 biomes) | Growing threats`

### Notable Changes vs Yesterday
- Seed 137 grain=0 today (was 0 in yesterday's history entry, 15 in report-card). Food=11<15 threshold so Granary never fires — confirms rand::rng() task assignment keeps farmer count too low to accumulate food above the Granary trigger.
- Seed 777 grain=24, planks=3 today (grain up from 18 yesterday). Bakery still blocked (planks<8, Forest<1%).
- 0 events fired in all 3 seeds (no WolfSurge today; check #36 event suppression detector still reports "OK" since WolfSurge fired on 2026-06-09).
- 2/3 seeds rabbits=0 today (seed 42=0, seed 137=0, seed 777=5). Check #31 will fire after metrics_history update.

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13-15 biomes (count OK), 98.4-98.7% flat (elevation_std=0.09-0.11), Desert 32-49% in 2 seeds, Forest<1% all seeds, no Mountain/Cliff coverage. Same as yesterday. |
| Settlement Viability (30%) | 2 | All 3 survived; avg pop=8.7; food Critical (fpc≤1.45) in 2/3 seeds; seed 42 stuck at pop=4 for 36+ consecutive checks; housing saturated seed 42. Same as yesterday. |
| Simulation Health (20%) | 4 | pipe_water_total 244–305 (>0), wind_moisture_total 16K–41K (>0), avg_vegetation 0.42–0.44 (>0.1) — all systems active. Architectural debt (3 water systems) unchanged. |
| Emergent Behavior (20%) | 2 | 0 events fired (event modifier chain broken); seeds 137/777 both pop=11 (convergence); wood_skill=0.5488 pure-decay; no roads (ROAD_TRAFFIC_THRESHOLD=300). Same as yesterday. |
| **Weighted Average** | **2.4** | `2×0.3 + 2×0.3 + 4×0.2 + 2×0.2` — no change from yesterday. Score plateau concern: 7 of last 8 entries between 2.4–2.7. Root cause cluster (biome distribution + seeded RNG) still unaddressed. |

### Baseline Check
- **3/3 PASS**: seed 42 (pop=4, water=30.7%, biomes=13), seed 137 (pop=11, water=17.9%, biomes=14), seed 777 (pop=12, water=38.0%, biomes=14)

## Harness Updates
- No new checks added today. Existing 59 checks comprehensively cover known issues.
- validate_features.sh: **0 errors, 37 warnings** (down 2 from yesterday's noted 39; difference due to grain=24/planks=3 in seed 777 not triggering check #53 grain-dead-end, which requires planks=0).

## features.json Updates
- `last_verified` updated to 2026-06-14 for all systems
- `ecs_core.known_issues`: added observation — seed 777 report-card pop=12 vs diagnostics pop=11 (delta=1); smaller within-session swing than prior week (±7 documented 2026-06-09 through 2026-06-13); 2/3 seeds rabbits=0 today (seed 42 persistent, seed 137 fluctuating)

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** — Forest<1% in all seeds is the root cause of: wood starvation → planks=0 → Bakery structurally impossible (checks #41, #42, #47, #59); flat terrain=98%+ violates Pillar 1 (check #32); Desert 32-49% vs 0.1% across seeds means terrain doesn't differentiate outcomes (check #48). Single highest-leverage fix. Read `terrain_pipeline.rs` biome scoring end-to-end before touching anything. Would unblock terrain score (2→4) and emergent production chains.
2. **Seeded per-entity RNG** — Replace `rand::rng()` (3 calls in `game/mod.rs`, 3 in `ecs/systems.rs`) with entity-ID-seeded `SmallRng`. Documented in `ecs_core.known_issues`: ±7 pop and ±11 food divergence in consecutive same-seed runs. Makes evaluation metrics unreliable. Prerequisite for stable rubric scoring and confident "fix worked" verdicts.
3. **FoodToGrain Granary adaptive threshold** — Change `food > 15` to `food > max(15, pop * 2)` in `ecs/systems.rs:979,1391`. Quick targeted fix for recurring starvation in seed 137 (4× food=0 documented). Write test first: assert that at pop=11 with food=22, Granary does NOT drain below 22. Does not require biome fix and could raise settlement score (2→3) immediately.
