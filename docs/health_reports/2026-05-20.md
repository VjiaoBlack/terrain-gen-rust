# Daily Health Check — 2026-05-20

## Test Results
- **778 passed, 0 failed, 10 ignored** — CLEAN RUN
- Test count accuracy: features.json counts within tolerance; CLAUDE.md still claims ~190 (actual 818, check #29)
- Flaky tests: `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` still not marked `#[ignore]`

## Design Drift

**Pillar 1 (Geography Shapes Everything):**
- Terrain is 98.4–98.7% flat across all 3 seeds (elevation_std 0.09–0.11). Hills and mountain passes required by Pillar 1 do not exist. Maps are strategic equivalents; only tile type varies. (check #32)
- Forest biome <1% in all 3 seeds (42=0.3%, 137=0.1%, 777=0.7%). Wood-dependent economy cannot produce planks → Bakery structurally blocked. (check #42)
- Seeds 137 and 777 produce near-identical outcomes (pop=11, food=14, wood=4) despite different terrain types (137=48.7% Desert, 777=0.1% Desert). Pillar 1 criterion "two seeds produce different settlement shapes" fails: all three seeds produce the same sequence: Stockpile + Farm + Hut + Garrison + Workshop.

**Pillar 2 (Emergent Complexity):**
- VillagerMemory still not referenced in `ai.rs` — per-villager knowledge architecture not started. Villagers use global stockpile counts. (check #22, 26th+ consecutive check)
- Drought event modifier chain broken: `tick_config.rain_rate` built in `game/mod.rs:2135-2147` but never passed to `step_water_cycle`. Drought/harvest have zero effect on the simulation even if they fire. (check #33)

**Production chain analysis (new):**
- Bakery (`build.rs:1333`) requires `planks >= 8` AND `grain > 30`. `has_granary` is satisfied from game start by the pre-built `FoodToGrain` ProcessingBuilding, but: (1) planks=0 because Workshop is starved of wood (forest=0%); (2) grain stays below 30 in evaluation window. Bakery is blocked at TWO gates, not just the Granary gate claimed in check #45. The known_issue in `building_system` should note the planks and grain thresholds as the actual bottlenecks.

**Events:**
- Seed 137 fired WolfSurge in today's diagnostics run (`events=['WolfSurge']`). WolfSurge CAN fire non-deterministically based on threat thresholds. Yesterday's entry shows 0 events for all seeds. Confirms events system is partially functional (WolfSurge), not fully suppressed; drought is the structurally impossible case.

## Architecture

**No new large file violations** (3000-line threshold). Known files approaching threshold:
- `tilemap.rs`: 2515 lines (Priority 5 refactor, data-driven TerrainDef)
- `game/mod.rs`: 2592 lines (split ongoing)
- `game/build.rs`: 2306 lines
- `simulation/moisture.rs`: 1867 lines

**ARCHITECTURE.md gaps** (check #38): `analytical_erosion.rs` and `src/scripting/` absent from file map.
**CLAUDE.md stale** (check #40): lists `simulation.rs` (split), missing `world_state.rs`, claims ~190 tests.
**WorldState 0D architecture**: confirmed intact (`WorldState` referenced in `game/mod.rs`).

## Game Quality Evaluation

### Per-Seed Results
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4 / +0      | 30.7%  | 13     | Yes      |
| 137  | 11        | 14   | 1.27     | 20 / +9     | 17.9%  | 14     | Yes      |
| 777  | 11        | 14   | 1.27     | 16 / +5     | 37.9%  | 14     | Yes      |

### Report Card Summaries
- Seed 42: Population=Fragile (4) | Food=Tight (3.0/cap) | Housing=Overcrowded (4/4) | Terrain=Diverse | Threats=Low
- Seed 137: Population=Healthy (11) | Food=Critical (1.4/cap — starvation imminent) | Housing=Tight (11/12) | Threats=Growing
- Seed 777: Population=Healthy (11) | Food=Critical (1.3/cap — starvation imminent) | Housing=Adequate (11/20) | Threats=Growing

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | Biome count 13-14 adequate, but Desert 32-49% in 2/3 seeds; Forest <1%; 98%+ flat; no strategic topography |
| Settlement Viability (30%) | 2 | All survived, avg pop=8.7, but 2/3 seeds food-critical (1.27/cap); seed 42 Fragile and not growing |
| Simulation Health (20%) | 4 | pipe_water=245-305, wind_moisture=16K-41K, avg_vegetation=0.42-0.44 — all systems active |
| Emergent Behavior (20%) | 2 | 1 event across 3 seeds (WolfSurge in 137); seeds 137/777 produce identical outcomes; no roads |
| **Weighted Average** | **2.4** | 13th consecutive check at 2.4; score plateau (check #17) |

### Baseline Check
- Seed 42: PASS (pop=4, water=30.7%, biomes=13)
- Seed 137: PASS (pop=11, water=17.9%, biomes=14)
- Seed 777: PASS (pop=11, water=38.0%, biomes=14)
- **3/3 baselines PASS**

### Simulation Health Detail
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|---------|---------|
| pipe_water_total | 298.2 | 244.6 | 305.3 |
| wind_moisture_total | 17810.6 | 16602.7 | 41160.2 |
| avg_vegetation | 0.440 | 0.420 | 0.420 |

## Harness Updates

Added **check #47** to `scripts/validate_features.sh`: Bakery planks threshold impossibility check. Complements check #45 (Granary food threshold) and check #44 (wood near-zero). Detects when Bakery requires `planks >= N` and N > 2 but Workshop is starved of wood — independent of whether `has_granary` is satisfied.

## features.json Updates
- All systems: `last_verified` → 2026-05-20
- `events`: Added note that WolfSurge fires non-deterministically and is not fully suppressed; distinguish from drought (structurally impossible) and tick_config dead code (affects drought/harvest only)
- `building_system`: Updated known_issue for Bakery — actual bottlenecks are `planks >= 8` AND `grain > 30`, not just Granary; pre-built Granary satisfies `has_granary` from game start

## Priority Actions
1. **Fix forest biome distribution** (`terrain_pipeline.rs` biome scoring) — root cause of wood=0 chain: no forest → no wood → no planks → no Bakery; also explains wood_skill pure-decay. Fix this before tuning any wood/skill/production constants.
2. **Fix terrain flatness** (`terrain_pipeline.rs` height generation amplitude) — elevation_std=0.09-0.11, 98%+ flat violates Pillar 1. Rivers don't flow meaningfully, no mountain passes for defense, no geographic asymmetry between seeds.
3. **Fix event modifier chain** (`game/mod.rs:2135-2147`) — pass `tick_config` to `step_water_cycle` so drought/harvest actually affect water simulation. Currently drought fires (non-deterministically) but has zero effect.
4. **Mark flaky tests `#[ignore]`** — `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` cause non-deterministic CI failures; 778/0/10 clean today but historically hits ~1/300 runs.
