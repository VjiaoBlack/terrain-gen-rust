# Daily Health Check — 2026-04-27

## Test Results
- **778 passed, 0 failed, 10 ignored** (199.8s) — identical to all previous entries
- 818 `#[test]` annotations in src/ (above the ≥700 regression guard)
- `validate_features.sh`: 0 errors, 16 warnings (unchanged from 2026-04-26)
- All listed files exist; all split-product files present; all statuses valid

### validate_features warnings (persistent, unchanged)
- `save_load` status='needs_tests' with test_count=0
- `construction_dust_particles_spawn` not marked `#[ignore]` (flaky CI risk)
- `rand::rng()` in systems.rs (×3) and game/mod.rs (×3) — non-determinism
- Score plateau at ~2.9/5.0 for 5+ consecutive entries (now 17th)
- `world_state` status='needs_work' with test_count=0
- `sight_range=25.0` trivially reveals map (fog-of-war non-functional)
- VillagerMemory not read in ai.rs (Pillar 2 gap)
- Persistent food crisis: food_per_cap < 2.0 in 2+ seeds for 4/5 recent checks
- Seed 42 stagnation: pop=4, food=12 for 5/5 recent checks (17 total consecutive)
- `ROAD_TRAFFIC_THRESHOLD=300.0` prevents road formation
- fire.rs, water_cycle.rs, particles.rs have 0 tests
- Drought structurally impossible (grain >= pop×5 never met)
- CLAUDE.md claims ~190 lib tests; actual count is 778 (4× stale)

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Fog-of-war non-functional: all seeds hit exploration_pct=100% trivially. sight_range=25 + initial reveal radius=15 covers any 70×25 map in <200 ticks.
- Desert dominates seed 42 (32%) and seed 137 (49%); Forest <0.4% in those seeds. Seed 777 is healthier (Desert=0.1%, Grass=12.5%), but two of three evaluation seeds resemble a dry flatland.
- Terrain 98%+ flat (flat_pct=98.4–98.7%, zero cliff/steep tiles). No mountain passes, river valleys, or chokepoints.

**Pillar 2 — Emergent Complexity:**
- VillagerMemory (DangerZone, ThreatReport) written in systems.rs but never read in ai.rs — 17th consecutive day unresolved.
- Systems do NOT chain through simulation state: drought multiplies yield directly, not through water→soil→crop.

**Pillar 3 — Explore → Expand → Exploit → Endure:**
- Default seed (42): GAME OVER at tick 6668. Peak pop 4. No expansion, no exploitation, no endurance.
- 0 events across all 3 seeds at tick 6000 — 17th consecutive zero-event evaluation. Drought structurally impossible; WolfSurge fires inconsistently due to rand::rng() non-determinism.

**Roads (Anti-goal drift):** ROAD_TRAFFIC_THRESHOLD=300.0 prevents any road formation in the evaluation window. Zero Road coverage in all seeds at tick 6000. The mechanic exists in code but is calibrated out of reach.

## Architecture

Module sizes (no change from yesterday):
| File | Lines | Status |
|------|-------|--------|
| game/mod.rs | 2592 | Getting big |
| game/build.rs | 2306 | Getting big |
| terrain_pipeline.rs | 2232 | Getting big |
| tilemap.rs | 2515 | Getting big |
| ecs/systems.rs | 2227 | Getting big |
| ecs/ai.rs | 2104 | Getting big |

No files exceed 3000 lines (split threshold). Closest: tilemap.rs at 2515.
ARCHITECTURE.md accurate. world_state.rs still not documented there (known issue in features.json).
CLAUDE.md lists ~190 lib tests; actual is 778 — stale by 4×.

## Game Quality Evaluation

### Per-Seed Results (tick 6000, --screenshot --ticks 12000 with half_speed_base)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4 (full)    | 30.7%  | 14     | yes (dies ~6668) |
| 137  | 11        | 11   | 1.0      | 16          | 17.9%  | 14     | yes |
| 777  | 11†       | 15   | 1.4      | 12          | 37.9%  | 14     | yes |

†Seed 777 non-determinism: report-card run shows pop=4/Fragile/Overcrowded(cap=4); diagnostic run shows pop=11/Healthy/Tight(cap=12). Both same binary, same seed, sequential runs. Auto-build outcome itself is non-deterministic (see ecs_core known_issues).

### Report Card Summaries
- Seed 42: `Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low`
- Seed 137: `Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Growing`
- Seed 777: `Population: Healthy (growing) | Food: Tight (no buffer) | Housing: Tight (limited growth room) | Terrain: Diverse (rich landscape) | Threats: Low` *(diagnostic run; RC shows Fragile/Overcrowded)*

### Simulation Health (all systems active)
| Metric | Seed 42 | Seed 137 | Seed 777 |
|--------|---------|----------|---------|
| pipe_water_total | 298.19 | 244.62 | 306.16 |
| wind_moisture_total | 17810.57 | 16602.66 | 41159.05 |
| avg_vegetation | 0.44 | 0.42 | 0.42 |
| avg_moisture | 0.46 | 0.32 | 0.65 |

All simulation systems active (values > 0).

### Baseline Check
All 3 seeds: **PASS** (check_baselines.sh: 3 passed, 0 failed).

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 14 biomes ✓, water 17-38% ✓; slopes 98%+ flat ✗, Desert 32-49% in 2/3 seeds ✗ |
| Settlement Viability (30%) | 2 | Default seed dies at tick 6668; seed 42 stuck at capacity for 17 entries; food Critical/Tight in 2/3 seeds |
| Simulation Health (20%) | 4 | All systems active with above-zero values; moisture chain still has known issues |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4 vs 11, footprint 496 vs 1101); 0 events across all seeds; no roads formed |
| **Weighted Average** | **2.9** | 17th consecutive plateau at 2.9 |

Calculation: 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = 0.9 + 0.6 + 0.8 + 0.6 = **2.9**

### Baseline Check
- check_baselines.sh: 3 passed, 0 failed — all seeds within expected ranges

## Harness Updates

Added **Check #29: CLAUDE.md test count staleness** to validate_features.sh.
- Extracts documented test count from CLAUDE.md and compares against actual `#[test]` annotation count.
- Warns if off by 2× or more — stale estimate misleads new contributors about test suite size.
- Result on current code: fires (claims ~190, actual 778 = 409% off).

## features.json Updates
- Updated `last_verified` for all systems to 2026-04-27.
- Updated `ecs_core` known_issues: seed 42 stagnation now at 17 consecutive health checks (was 16).
- Updated `ecs_core` known_issues: added auto-build non-determinism note (seed 777 diag vs RC run produces different building counts in same session, proving auto-build path is affected by rand::rng()).

## Priority Actions
1. **Fix seed 42 auto-build failure** (`ecs_core` known_issue, 17th entry): housing capacity=4, pop=4, wood=43, yet no second Hut is placed. Diagnose `game/build.rs` hut-placement scoring — this is the single highest-impact fix for Settlement Viability score. Note: auto-build itself is affected by rand::rng() non-determinism (seed 777 runs get different building counts), so the fix must address determinism too.

2. **Fix `construction_dust_particles_spawn` flaky test** (`game_loop` known_issue): add `#[ignore]` annotation in `src/game/tests.rs`. One-line fix, eliminates false CI failures. Has fired twice (Apr 13, Apr 15).

3. **Lower `ROAD_TRAFFIC_THRESHOLD`** (`events` known_issue): current value 300.0 unreachable for pop=4-13 settlements. Diagnose actual traffic accumulation at high-frequency tiles via `--diagnostics` overlay, then tune to ≤100. Emergent roads are core to the design (not an anti-goal — only *manual* road placement is the anti-goal).

4. **Fix biome distribution skew** (`terrain_generation` known_issue): Desert 32-49% in seeds 42/137, Forest <0.4%. Check `terrain_pipeline.rs` biome assignment thresholds. This is the root cause blocking Terrain Quality from scoring above 3.
