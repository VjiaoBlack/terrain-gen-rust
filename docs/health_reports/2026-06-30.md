# Daily Health Check — 2026-06-30

## Test Results
- **777 passed, 1 failed, 10 ignored** (326.78s)
- Failure: `game::tests::build_site_gets_completed_in_game` — known flaky test (rand::rng() in AI/build paths; not a regression)
- validate_features.sh: **0 errors, 44 warnings** (unchanged from yesterday)
- Test count accuracy: 818 #[test] annotations in src/ (unchanged); CLAUDE.md still claims ~190 (check #29, stale ×4)

## Design Drift
- **Pillar 1 (Geography Shapes Everything):** VIOLATED — Seeds 137 (Desert=49%, Forest=0.1%) and 777 (Grass=12.5%, Desert=0.1%) both produce pop=11 settlements. Check #48 fires (4/5 recent entries with pop diff ≤1). Terrain biome imbalance in `terrain_pipeline.rs` is the root cause.
- **Pillar 2 (Emergent Complexity):** NOT STARTED — `VillagerMemory` written in `systems.rs`, never read in `ai.rs`. Villagers still use global stockpile counts. Check #22 fires.
- **Event modifier chain (drought/harvest):** Still dead code. `tick_config` modifiers built in `game/mod.rs:2135-2147` but never passed to `step_water_cycle`. Check #33 fires. Events=0 all 3 seeds today.
- **Fog-of-war:** Trivially 100% revealed (sight_range=22, reveal_radius=15 on a 70×25 map). Not a meaningful mechanic. Checks #21 fire.
- **Road formation:** `ROAD_TRAFFIC_THRESHOLD=300.0` — no roads in evaluation window across any seed. Check #25 fires.

## Architecture
- No files exceed 3000-line threshold today (largest: `game/mod.rs` 2592, `game/build.rs` 2306, `terrain_pipeline.rs` 2232, `ecs/systems.rs` 2227, `ecs/ai.rs` 2104)
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` still absent from `docs/ARCHITECTURE.md` (checks #38)
- `CLAUDE.md` still lists `simulation.rs` (split long ago) and omits `world_state.rs` (checks #40)
- `save_load` at `needs_tests` with `test_count=0`: no progress (check #9 fires)
- `world_state` at `needs_work` with `test_count=0`: no coverage (check #18 fires)

## Game Quality Evaluation

### Per-Seed Results
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 14     | Yes      |
| 137  | 4 (diag) / 11 (rc) | 29 (diag) | 7.25 (diag) / 1.4 (rc) | 4 (diag) / 16 (rc) | 17.9%  | 14     | Yes      |
| 777  | 11        | 18   | 1.64     | 20          | 38.0%  | 14     | Yes      |

Seed 137 shows the daily bidirectional flip: pop=4 (diagnostic run) vs pop=11 (report-card run). Same binary, same seed, sequential runs. rand::rng() in `game/build.rs` placed 1 Hut vs 4 Huts.

### Report Card Summaries
- Seed 42: Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded (need more huts) | Terrain: Diverse (rich landscape) | Threats: Low (quiet settlement)
- Seed 137: Population: Healthy (growing) | Food: Critical (starvation imminent, fpc=1.4) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)
- Seed 777: Population: Healthy (growing) | Food: Critical (starvation imminent, fpc=1.5) | Housing: Adequate (room to grow) | Terrain: Diverse (rich landscape) | Threats: Growing (attracting attention)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 14 biomes (diverse count) but Desert=32-49%, Forest<1%, slope 98%+ flat; Pillar 1 not functional |
| Settlement Viability (30%) | 2 | All 3 survive; seed 42 stuck pop=4 (45th run); 2/3 seeds food Critical; RNG non-determinism hides true viability |
| Simulation Health (20%) | 4 | pipe_water=244-313, wind_moisture=16K-41K, vegetation=0.41-0.44; all active |
| Emergent Behavior (20%) | 2 | Events=0 all seeds (zero-event streak resumes after yesterday's WolfSurge break); 137/777 convergence persists |
| **Weighted Average** | **2.4** | terrain×0.3 + settlement×0.3 + simulation×0.2 + emergent×0.2 = 0.6+0.6+0.8+0.4 |

**Score plateau: 5th consecutive health check at 2.4/5.0** (2026-06-26 through 2026-06-30). Check #17 will fire on next run after metrics update.

### Baseline Check
- Seed 42: PASS (pop=4, water=30.7%, biomes=13)
- Seed 137: PASS (pop=11, water=17.9%, biomes=14)
- Seed 777: PASS (pop=11, water=38.0%, biomes=14)

## Key Observations (new today)
1. **Seed 42: 45th consecutive stuck diagnostic run** — pop=4, food=12, wood=43 surplus, housing_growth_potential=0. Auto-build never places second Hut in this RNG state. Check #65 fires.
2. **Seed 777 grain/planks regression** — grain: 28→12, planks: 6→3 from yesterday's diagnostics. rand::rng() unfavorable task assignment in today's run. Non-systematic — Workshop still produces planks when RNG is favorable.
3. **Seed 777 seek_pct=81.8%** (9/11 villagers in Seek state with Farming=1). Above 80% check #60 threshold — will fire on next validate_features run after metrics update.
4. **Flaky test hit** — build_site_gets_completed_in_game failed today (777/1/10). All 3 known-flaky tests still unignored (check #55). Expected CI failure rate ~3× per suite run.
5. **Zero-event diagnostic streak resumes** — Yesterday's WolfSurge (seed 777, day 12000-ago) was not reproduced today. All 3 seeds events=0 again.

## Harness Updates
- No new checks added today — harness at 65+ checks is comprehensive for current failure modes.
- Plateau condition (check #17) will auto-fire on next run once today's 2.4 entry is logged.
- Check #60 (seek_pct > 80%) will auto-fire on next run for seed 777 (81.8% in today's diagnostics).

## features.json Updates
- All systems `last_verified` updated to 2026-06-30
- `ecs_core.known_issues`: added 45th consecutive stuck run note for seed 42; grain/planks regression for seed 777; seed 137 bidirectional flip continues
- `events.known_issues`: zero-event streak resumes (yesterday's WolfSurge not reproduced)

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** (lowest rubric category: Terrain=2) — Desert dominant (32-49%), Forest<1% blocks wood supply, disables Bakery chain, violates Pillar 1. This is the root cause of 5+ downstream failures (checks #32, #41, #42, #44, #47, #48). See `docs/BACKLOG.md` Priority 0B.
2. **Replace `rand::rng()` with seeded per-entity RNG** (Settlement Viability=2) — Within-session divergence of ±7 population makes health checks unreliable. Root cause of checks #15, #30, #35, #65. Fix in `game/build.rs` and `ecs/systems.rs`. See `docs/BACKLOG.md` Priority 1.
3. **Fix tick_config event modifier chain** (Emergent=2) — Drought/harvest modifiers built but never passed to `step_water_cycle`. Events structurally dead. Fix in `game/mod.rs:2135-2147`. See features.json `events.known_issues`.
