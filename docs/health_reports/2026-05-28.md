# Daily Health Check — 2026-05-28

## Test Results
- **778 passed, 0 failed, 10 ignored** (cargo test --lib, ~224s)
- Test count in CLAUDE.md is stale: claims ~190, actual 818 (check #29 fires, 4× off)
- Known-flaky tests `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` remain not marked `#[ignore]` (checks #14, #35 fire). Risk of sporadic CI failures.

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Terrain is 98.4–98.7% flat across all 3 seeds (slope_distribution: steep=0%, cliff=0%, elevation_std=0.09–0.12). No mountains. No strategic chokepoints. Violates the design principle that terrain shapes settlement growth.
- Desert=32–49% in 2/3 seeds, Forest=0.1–0.7% in all 3 seeds. Wood skill in pure decay (wood_skill=0.5488=1.0×0.9999^6000, confirmed check #41). Settlement is structurally wood-dependent but no wood source exists.
- Roads never form (ROAD_TRAFFIC_THRESHOLD=300.0 unreachable at pop=4–15; check #25 fires).

**Pillar 2 — Emergent Complexity:**
- `VillagerMemory` is written in `ecs/systems.rs` but never read in `ecs/ai.rs`. Villagers use global stockpile counts, not per-villager memory (check #22 fires). This gap is design doc–required Core behavior.

**Event System:**
- `tick_config` drought/harvest modifiers built at `game/mod.rs:2135–2147` but never passed to `step_water_cycle` (check #33 fires). Events have zero effect on simulation. Check #36 fires: 0 events in 2+ seeds for 5/5 consecutive health checks.
- Drought grain threshold (`grain >= pop*5`) structurally impossible in eval window (check #27 fires).

**Fog-of-war:** `sight_range=25` + `initial_reveal=15` trivially exposes all 70×25 map tiles by tick 6000. Exploration not a meaningful mechanic (check #21 fires).

## Architecture
- No non-test source file exceeds 3000 lines (all clear)
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` file map (check #38)
- `CLAUDE.md` module structure lists the removed `simulation.rs` and omits `world_state.rs` (check #40)
- `save_load` at `needs_tests` with 0 tests; `world_state` at `needs_work` with 0 tests (checks #9, #18)

## Game Quality Evaluation

### Per-Seed Results (Diagnostics, tick=6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 13     | Yes      |
| 137  | 11        | 0    | 0.0      | 20          | 17.9%  | 14     | Yes      |
| 777  | 15        | 18   | 1.2      | 20          | 38.1%  | 14     | Yes      |

**Notable:** Seed 137 `food=0` at tick 6000 with `pop=11` — total starvation (new check #50 fires). Pre-built FoodToGrain Granary (`game/mod.rs:1064`, `food>15`) consumed the entire food buffer before farms replenished it. Seed 777 `threat_score=32.11` is a new all-time high (prior max ~27).

*Note: Report-card runs are separate from diagnostics runs and produce different values (rand::rng() non-determinism).*

### Report Card Summaries
- Seed 42: `Fragile` (pop) / `Tight` (food) / `Overcrowded` (housing, cap=4) / `Diverse` (terrain) / `Low` (threat)
- Seed 137: `Healthy` (pop=11) / `Critical` (food, starvation imminent) / `Adequate` (housing) / `Diverse` / `Growing` (threat)
- Seed 777: `Healthy` (pop=11 in report run) / `Critical` (food) / `Tight` (housing) / `Diverse` / `Growing` (threat)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes by count but Desert 32–49% in 2/3 seeds, Forest <1% all seeds, Mountain=0.0%, 98%+ flat; no strategic geography |
| Settlement Viability (30%) | 2 | All seeds survive tick 6000; seed 42 structurally stagnant (36th+ consecutive pop=4 stuck run); seed 137 food=0 (total starvation); rand::rng() makes outcomes unreliable |
| Simulation Health (20%) | 4 | pipe_water=244–314 (active), wind_moisture=16K–41K (active), avg_vegetation=0.42–0.44 (healthy); all subsystems functioning |
| Emergent Behavior (20%) | 2 | 0 events in all seeds for 5+ consecutive checks (tick_config chain broken); check #48 fires (seeds 137/777 convergent 4/5 recent checks); non-determinism obscures genuine variance |
| **Weighted Average** | **2.4** | 2×0.3 + 2×0.3 + 4×0.2 + 2×0.2 = 2.4; score plateau (check #17 fires, 6th+ consecutive check at 2.4) |

### Baseline Check
All 3 seeds **PASS** (seed 42: pop=4, water=30.7%, biomes=14; seed 137: pop=11, water=17.9%, biomes=15; seed 777: pop=12, water=38.0%, biomes=15).

### Validate Features
**0 errors, 35 warnings** (check #43 multi-seed housing saturation stopped firing; check #50 added and fires).

## Harness Updates
- **Added check #50** (`validate_features.sh`): "Total starvation detection" — fires when `food=0` with `population>0` in any seed in the most recent metrics_history entry. More acute than check #39 (`food_per_cap < 1.5`). Fires today: seed 137 `food=0, pop=11`. Includes diagnosis hint: pre-built FoodToGrain Granary (`food>15` threshold) may deplete buffer faster than farms replenish at high population.

## features.json Updates
- `ecs_core`: added 4 new known issues:
  - Seed 777 pop=15 (+11 from yesterday, largest single-day positive swing documented)
  - Seed 137 `food=0` at tick 6000 (total starvation, new all-time low food)
  - `threat_score=32.11` for seed 777 (new all-time max)
  - Pre-built Granary `food>15` threshold causes food depletion at high population
- All 25 systems: `last_verified` updated to `2026-05-28`

## Priority Actions
1. **Fix seeded per-entity RNG** (`ecs_core` / `rand::rng()` in `game/build.rs`, `ecs/systems.rs`): Population variance of ±11 in sequential same-seed runs makes every other metric untrustworthy. This is the root cause blocking all quality measurement. Fix: replace `rand::rng()` calls in breeding/death/auto-build hot paths with a deterministic seeded RNG keyed on entity ID + tick.
2. **Fix biome distribution in `terrain_pipeline.rs`** (`terrain_generation`): Desert 32–49% dominant, Forest <1%, Mountain=0% in all eval seeds. Wood skill at pure decay equilibrium — no trees exist to cut. Buildings all require wood; the entire construction chain depends on a non-existent resource. Investigate biome scoring weights and height amplitude post-WorldState refactor.
3. **Fix event modifier chain** (`events`): `tick_config` rain_rate modifiers at `game/mod.rs:2135–2147` are built but never plumbed into `step_water_cycle` (signature is `(bool, f64)`, not `tick_config`). Zero emergent events in all seeds for 5+ consecutive checks. One targeted fix: pass `tick_config.rain_rate` through to `step_water_cycle`.
