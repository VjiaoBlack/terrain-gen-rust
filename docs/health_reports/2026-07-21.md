# Daily Health Check — 2026-07-21

## Test Results
- Total: 777 passed, 1 failed, 10 ignored (215.74s)
- Failure: `build_site_gets_completed_in_game` (known flaky — rand::rng() in AI/build path; `src/game/tests.rs`)
- Test suite: 818 `#[test]` annotations in src/ (>= 700 threshold)
- Test count accuracy: No new discrepancies; prior-documented gaps unchanged

## Design Drift

### Pillar 1: Geography Shapes Everything (VIOLATED)
- **Flat terrain**: slope_flat_pct = 98.4–98.7% in all 3 seeds; 0% steep/cliff tiles. Design requires hills and mountain passes for strategic constraints (`terrain_pipeline.rs`).
- **Forest near-zero**: Forest biome 0.1–0.6% in all seeds. Wood required for every building but no trees to cut. Wood skill pure-decay (0.5488 = 1.0 × 0.9999^6000) in all 3 seeds.
- **Seed convergence**: Seeds 137 (Desert=48.7%, Forest=0.1%) and 777 (Grass=12.5%, Desert=0.1%) both produce pop=11 at tick 6000 — identical outcomes despite radically different terrain. Violates "two maps → two different settlements."

### Pillar 2: Emergent Complexity (PARTIAL GAP)
- `VillagerMemory` defined in `components.rs`, written in `systems.rs`, but has zero references in `ai.rs` (check #22). Villagers still query global stockpile counts (15 occurrences in `ai.rs`). Per-villager knowledge architecture unimplemented.

### Pillar 3: Explore → Expand → Exploit → Endure (EVENT CHAIN BROKEN)
- `tick_config` builds drought/harvest rain_rate modifiers in `game/mod.rs:2135-2147` but `step_water_cycle(bool, f64)` never receives them — dead code (check #33). Drought structurally impossible (grain>=pop×5 threshold never met in eval window).
- events_fired=0 in all 3 seeds today (4th consecutive zero-event diagnostic run since WolfSurge on 2026-07-16 in seeds 137+777).

### Unimplemented design features (Phase 1 deliverables)
- **Fog of war**: `sight_range=22` + initial reveal radius=15 → `exploration_pct=100%` trivially in all seeds. Design lists fog-of-war as a Phase 1 deliverable.
- **Road auto-build**: `ROAD_TRAFFIC_THRESHOLD=300.0` unreachable at pop 4–11; 0% Road coverage all seeds.

## Architecture

No source files exceed 3000 lines (non-test threshold). Files approaching split threshold:
- `game/mod.rs` 2592 lines, `game/build.rs` 2306, `terrain_pipeline.rs` 2232, `ecs/systems.rs` 2227, `ecs/ai.rs` 2104, `tilemap.rs` 2515
- `ecs/tests.rs` 7164 lines (test file — excluded from threshold, but notable)

Other issues:
- `analytical_erosion.rs` and `scripting/mod.rs` not documented in `docs/ARCHITECTURE.md` (check #38)
- `CLAUDE.md` stale: lists `simulation.rs` (split 2026-03), missing `world_state.rs` (check #40)
- `CLAUDE.md` claims ~190 tests; actual 818 (check #29)
- 11 compiler warnings in `game/mod.rs` (unused mut, tick_config assigned-but-never-read, DIRECTIONS constant)

## Game Quality Evaluation

### Per-Seed Results (diagnostics run, --ticks 12000 ≈ game.tick 6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|--------|--------|----------|
| 42   | 4         | 15   | 3.75     | 4           | 30.9%  | 13     | Yes      |
| 137  | 11        | 11   | 1.00     | 20          | 17.9%  | 14     | Yes      |
| 777  | 11        | 16   | 1.45     | 20          | 38.0%  | 14     | Yes      |

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight (3.0/cap) | Housing: Overcrowded | Terrain: Diverse | Threats: Low
- Seed 137: Population: Healthy | Food: Critical (1.3/cap, starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing (23.77)
- Seed 777: Population: Healthy | Food: Critical (1.5/cap, starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing (27.96)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | Biomes 13–14 (diverse); water 18–38% (acceptable). But 98%+ flat, Forest<1%, Desert=49% in seed 137 — Pillar 1 requirement for hills/passes not met. |
| Settlement Viability (30%) | 2 | All 3 survived. Seed 42 stuck at pop=4 (65th+ diagnostic run). 2/3 seeds food Critical (fpc<1.5). Housing ok for seeds 137/777. |
| Simulation Health (20%) | 5 | pipe_water_total: 245–313 (active); wind_moisture_total: 16K–41K (active); avg_vegetation: 0.42–0.43 (>0.1). All systems running. |
| Emergent Behavior (20%) | 1 | events_fired=0 all seeds (4th consecutive zero-event run). Seeds 137/777 pop=11/11 (convergent). Building variety limited to same Farm+Hut+Workshop+Garrison set. |
| **Weighted Average** | **2.7** | 3×0.3 + 2×0.3 + 5×0.2 + 1×0.2 = 0.9+0.6+1.0+0.2 |

### Baseline Check
- 3/3 PASS (seed 42: pop=4, seed 137: pop=8, seed 777: pop=12)
- Within-session RNG divergence: seed 137 diag=11 vs baseline=8 (Δ=3); seed 777 diag=11 vs baseline=12 (Δ=1) — rand::rng() non-determinism continues

## Harness Updates
- No new checks added. validate_features.sh: **0 errors, 41 warnings**.
- All existing checks functional; no new failure patterns discovered today.

## features.json Updates
- Updated `last_verified` to `2026-07-21` for all systems
- Added known_issue note to `ecs_core` for 65th consecutive stuck seed-42 run
- Added known_issue note to `events` for 4th consecutive zero-event run

## Priority Actions
1. **Fix biome distribution** (`src/terrain_pipeline.rs`): Forest<1% starves wood supply; Desert 49% in seed 137 crowds out all other biomes. Root cause of: no woodcutting, construction stalled, Bakery impossible, Pillar 1 terrain convergence. One fix unblocks checks #32, #41, #42, #44, #47, #48.
2. **Fix rand::rng() non-determinism** (`src/game/build.rs:631`, `src/ecs/systems.rs`): Seed 42 stuck 65 consecutive diagnostic runs; ±7 pop divergence within-session. Replace with seeded per-entity RNG (BACKLOG.md item).
3. **Fix FoodToGrain Granary threshold** (`src/ecs/systems.rs`): `food > 15` too low for pop≥10; at pop=11 buffer lasts ~70–100 ticks before rand::rng() task assignment exhausts it. Fix: `max(15, population * 2)`.
4. **Fix tick_config event chain** (`src/game/mod.rs:2135-2147`): drought/harvest modifiers built but silently dropped; step_water_cycle never receives them. 4 consecutive zero-event diagnostic runs.
5. **Mark 3 flaky tests `#[ignore]`** (`src/game/tests.rs`): `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn` — all confirmed flaky via rand::rng(); causing intermittent CI failures (hit today on `build_site_gets_completed_in_game`).
