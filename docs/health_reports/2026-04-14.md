# Daily Health Check — 2026-04-14

## Test Results
- **778 passed, 0 failed, 10 ignored** (173s, cargo test --lib)
- **IMPROVED** from April 13 (777 passed, 1 failed) — `construction_dust_particles_spawn` did not fire today (probabilistic; ~0.3% per run)
- validate_features.sh: 0 errors, **4 warnings** (was 2 on April 13):
  1. `save_load` status=needs_tests, test_count=0 — no progress (5th consecutive report)
  2. `construction_dust_particles_spawn` not `#[ignore]` — false CI failures expected
  3. NEW: `src/ecs/systems.rs` uses `rand::rng()` (3 occurrences) — check #15
  4. NEW: `src/game/mod.rs` uses `rand::rng()` (3 occurrences) — check #15

## Design Drift

### 1. RNG non-determinism in game simulation (new documentation)
`rand::rng()` (thread-local, OS-seeded) in `systems.rs:119,866,1460` and `game/mod.rs:854,882,1189`. Causes divergent food values across same-seed runs: seed 137 food was 4 on April 13, 16 today — 4× difference. In the BACKLOG.md as "simulation non-determinism" (NOT STARTED). Now also flagged by harness check #15.

### 2. Road auto-build non-functional (5th consecutive day)
`ROAD_TRAFFIC_THRESHOLD = 300.0` (game/mod.rs:530). 0% Road coverage in all evaluation seeds at tick 6000. Design principle "Roads auto-build from traffic" unobservable. Documented in features.json.

### 3. Events never fire in evaluation window (4th consecutive day)
`events: []` across all 3 seeds. Drought structurally impossible (grain threshold). Other events under-calibrated for evaluation window.

### 4. ExplorationMap trivially 100% (persistent)
`exploration_pct=100.0` all seeds tick 6000. Fog-of-war nonfunctional as game mechanic.

### 5. VillagerMemory unread in AI (persistent, Pillar 2)
Memory written in `systems.rs`, not consulted in `ai.rs`. 15 global stockpile reads remain.

## Architecture

### File sizes (unchanged)
| File | Lines | Status |
|------|-------|--------|
| src/ecs/tests.rs | 7164 | Test file — excluded |
| src/game/tests.rs | 2927 | Test file — excluded |
| src/game/mod.rs | 2592 | Known, tracked in features.json |
| src/tilemap.rs | 2515 | TerrainDef refactor planned |
| src/game/build.rs | 2306 | Known, tracked (as of April 13) |
| src/terrain_pipeline.rs | 2232 | Known, tracked |
| src/ecs/systems.rs | 2227 | Known, tracked |
| src/ecs/ai.rs | 2104 | Known, tracked |

No non-test files over 3000 lines. No structural changes since April 13.

### ARCHITECTURE.md (resolved April 13)
Updated by commit `fde2fd4` — world_state.rs, simulation/ split, game/ submodules all reflected. Previously flagged 4 consecutive days.

## Game Quality Evaluation

*No code changes since April 13 (`fde2fd4`). Scores unchanged. Non-determinism shown in seed 137 food.*

### Per-Seed Results (tick 6000, auto-build, diagnostics run)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Events |
|------|-----------|------|----------|-------------|--------|--------|----------|--------|
| 42   | 4         | 12   | 3.0      | 4           | 30.7%  | 13     | ✓        | 0      |
| 137  | 11        | 16   | 1.5      | 20          | 17.9%  | 14     | ✓        | 0      |
| 777  | 11        | 15   | 1.4      | 12          | 37.9%  | 14     | ✓        | 0      |

*Seed 137 food: 4 (April 13) → 16 (today) at same tick — thread_rng() non-determinism.*

### Report Card Summaries
- Seed 42: Population: Fragile | Food: Tight (3.0/cap) | Housing: Overcrowded (cap=4) | Terrain: Diverse | Threats: Low (10.68)
- Seed 137: Population: Healthy | Food: Critical (1.1/cap in report run) | Housing: Tight (cap=20) | Terrain: Diverse | Threats: Growing (19.75)
- Seed 777: Population: Healthy | Food: Critical (1.5/cap) | Housing: Adequate (cap=20) | Terrain: Diverse | Threats: Growing (28.19)

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 3 | 13-14 biomes ✓; water 17.9-37.9% ✓; 98%+ flat terrain (persistent); no Mountain tiles; Desert 32-49% in 2/3 seeds, Forest 0.1-0.7% |
| Settlement Viability (30%) | 2 | All 3 survive ✓; avg pop 8.7; food Critical/Tight on 2/3 seeds; seed 42 Fragile (pop 4); no Granary/Bakery/Smithy built; growth_potential 0/9/1 |
| Simulation Health (20%) | 4 | pipe_water 244-306 ✓; wind_moisture 16.6K-41.2K ✓; avg_veg 0.42-0.44 ✓; moisture chain not fully connected (needs_work) |
| Emergent Behavior (20%) | 3 | Seeds diverge (pop 4 vs 11; Desert 0-49%; water 18-38%) ✓; 0 events fired; 0% Road coverage; building variety limited (5 of 9 types built) |
| **Weighted Average** | **2.90** | 3×0.3 + 2×0.3 + 4×0.2 + 3×0.2 = **2.90** — 5th consecutive day at plateau |

### Baseline Check
- **3/3 PASS**: seed 42 (pop=4, water=30.9%, biomes=13), seed 137 (pop=11, water=17.9%, biomes=15), seed 777 (pop=11, water=38.0%, biomes=14)

## Harness Updates

### Check #15: rand::rng() non-determinism guard (NEW)
Added to `scripts/validate_features.sh`: warns if `rand::rng()` appears in `ecs/systems.rs`, `game/mod.rs`, or `ecs/ai.rs` hot paths. Fires today for systems.rs (3) and game/mod.rs (3). Connects to BACKLOG.md "simulation non-determinism" item. Exits 0.

### Check #16: metrics_history.json freshness (NEW)
Added to `scripts/validate_features.sh`: warns if last entry in `docs/metrics_history.json` is older than 14 days. Passes today (April 11 = 3 days). Catches health-check gaps. Exits 0.

Also backfilled metrics_history.json with April 12 and April 13 entries (were missing — health reports existed but history wasn't updated).

## features.json Updates
- All `last_verified` dates updated to 2026-04-14

## Priority Actions
1. **Fix simulation non-determinism** (BACKLOG.md, NOT STARTED) — `rand::rng()` in systems.rs:119/866/1460 and game/mod.rs:854/882/1189. Replace with seeded `SmallRng` passed through `Game` struct. Write a test: two same-seed runs must produce identical state at tick N. This also fixes the score variability that makes rubric evaluation noisy.
2. **Mark construction_dust_particles_spawn `#[ignore]`** — check #14 fires every run. 1-line fix. Move to integration test or use seeded RNG. Blocks clean CI signal.
3. **Fix road auto-build threshold** — `ROAD_TRAFFIC_THRESHOLD=300.0` never reached at tick 6000 with pop 4-11. Pillar 1 "Roads auto-build from traffic" unobservable for 5 days. Requires diagnostic data: what traffic values are actually generated? Add `--diagnostics` output for max_traffic_value, then tune threshold.
4. **Add save_load tests** — status=needs_tests, test_count=0, 5th consecutive report. Simplest tests: save a fresh `Game`, load it, assert tick=0 and resource counts match.
5. **Calibrate event triggers** — 0 events in 3 evaluation seeds for 4 consecutive days. WolfSurge and harvest events should fire in the 0-6000 tick window given actual threat_score values (10-28 range).
