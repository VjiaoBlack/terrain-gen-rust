# Daily Health Check — 2026-06-05

## Test Results
- **775 passed, 3 failed, 10 ignored** (finished in 233s)
- 818 `#[test]` annotations in src/

### Failures
| Test | Status | Notes |
|------|--------|-------|
| `construction_dust_particles_spawn` | Known flaky | Probabilistic particle spawn, ~0.3% rate; not marked `#[ignore]` |
| `build_site_gets_completed_in_game` | Known flaky | rand::rng() villager behavior, not marked `#[ignore]` |
| `mining_sparkle_particles_spawn` | **NEW FAILURE** (tests.rs:2885) | Same mechanism: stone-mining particle spawn uses rand::rng(); 50-tick window may not contain sparkle before gather state ends |

`mining_sparkle_particles_spawn` is not documented in features.json and is not marked `#[ignore]`. Recommend marking it `#[ignore]` and adding to known_issues — same fix as the other two particle tests.

### Test Count Accuracy
- features.json claims align within tolerance for all systems.
- CLAUDE.md claims "~190 tests" — actual is 818 annotations / 775 passing (4× stale; known issue).

---

## Design Drift

| System | Drift | Reference |
|--------|-------|-----------|
| Pillar 2 (Agent Memory) | `VillagerMemory` defined in `components.rs`, written in `systems.rs`, **never read in `ai.rs`** — villagers still use global stockpile counts | game_design.md Pillar 2 |
| Fog-of-war | `sight_range=22`, initial reveal radius=15 → 100% map revealed by tick 200 in all eval seeds; exploration not a meaningful mechanic | game_design.md Pillar 3 (Explore phase) |
| Event modifier chain | `tick_config` drought/harvest multipliers built in `game/mod.rs:2135-2147` but never passed to `step_water_cycle` — drought/harvest have zero simulation effect | features.json:events |
| Three water systems | `WaterMap` + `PipeWater` + `Terrain::Water` all coexist; WorldState refactor incomplete | docs/ARCHITECTURE.md "Water (partially resolved)" |
| Roads | `ROAD_TRAFFIC_THRESHOLD=300.0` — no roads form in any eval seed (pop 4–11 at tick 6000); anti-goal "Roads auto-build from traffic" not manifesting | game_design.md Design Principles |

No drift found between `src/analytical_erosion.rs` and its design intent, but it remains undocumented in `docs/ARCHITECTURE.md`.

---

## Architecture

- **No files over 3000 lines** (excluding test files) ✓
- Files approaching split threshold: `moisture.rs` (1867), `build.rs` (2306), `terrain_pipeline.rs` (2232), `tilemap.rs` (2515)
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` not in `docs/ARCHITECTURE.md` file map (check #38 firing, known since 2026-05-08)
- `CLAUDE.md` module structure lists `simulation.rs` (split 2026-03), omits `world_state.rs` (added 0D refactor) — stale

---

## Game Quality Evaluation

### Per-Seed Results (tick 6000, --ticks 12000 with half_speed_base)

| Seed | Pop | Food | Food/Cap | Housing Cap | Water% | Biomes | Rabbits | Survived |
|------|-----|------|----------|-------------|--------|--------|---------|----------|
| 42   | 4   | 12   | 3.00     | 4 (0 slack) | 30.7%  | 13     | 0       | ✓ (dies tick 6668 in playtest) |
| 137  | 11  | 12   | 1.09     | 12 (1 slack)| 17.9%  | 14     | 1       | ✓ |
| 777  | 11  | 17   | 1.55     | 16 (5 slack)| 37.9%  | 14     | 0       | ✓ |

**Terrain (all seeds):** elevation_std=0.09–0.12, slope_flat=98.4–98.7%, cliff=0%, steep≤0.01%. Desert=32–49% in seeds 42/137, Forest<1% in all seeds. pipe_water=244–302, wind_moisture=15944–40786, avg_veg=0.41–0.44.

### Report Card Summaries
- **Seed 42**: "Fragile (one bad winter away) | Tight (no buffer) | Overcrowded (need more huts) | Diverse terrain | Low threats" — confirmed GAME OVER at tick 6668 in playtest, peak pop=4
- **Seed 137**: "Healthy (growing) | Critical (starvation imminent) | Adequate (room to grow) | Diverse terrain | Growing threats"
- **Seed 777**: "Healthy (growing) | Critical (starvation imminent) | Adequate (room to grow) | Diverse terrain | Growing threats"

Seeds 137 and 777 produce identical report card summaries despite radically different biome distributions (Desert=49% vs Desert=0.1%) — direct Pillar 1 violation.

### Rubric Scores

| Category | Score (1–5) | Notes |
|----------|-------------|-------|
| Terrain Quality (30%) | **2** | 13–14 biome types (good count), but Desert dominates 32–49% in 2/3 seeds; Forest<1% in all 3; terrain 98%+ flat (elevation_std≤0.12, cliff=0%). Water 17.9–37.9% in range. |
| Settlement Viability (30%) | **2** | All survive tick 6000 in diagnostics, but seed 42 confirmed dead at tick 6668 in playtest. 2/3 seeds show Critical food (fpc=1.09, 1.55). Seed 42 housing permanently saturated. No advanced production (bread=0, planks=0 in all seeds). |
| Simulation Health (20%) | **4** | All systems active: pipe_water_total=244–302, wind_moisture=15944–40786, avg_vegetation=0.41–0.44. Water, wind, vegetation, day/night all functioning. |
| Emergent Behavior (20%) | **1** | Zero events fired in all 3 seeds (event modifier chain broken). Seeds 137/777 both pop=11 despite different terrain. Seed 42 locked at pop=4 for 35+ checks. No roads, no outposts, no Bakery/Smithy/Bridge. |
| **Weighted Average** | **2.2** | Regression from 2.4 (previous 5 checks). Emergent drop from 2→1 driven by zero events this run. |

Score formula: `2×0.3 + 2×0.3 + 4×0.2 + 1×0.2 = 2.2`

### Baseline Check
- Seed 42: **PASS** (pop=4, water=30.7%, biomes=13)
- Seed 137: **PASS** (pop=11, water=17.9%, biomes=14)
- Seed 777: **PASS** (pop=8, water=38.0%, biomes=14)

All 3 golden baselines pass. Note: seed 777 showed pop=8 in the baseline run vs pop=11 in the diagnostics run — same binary, same seed, rand::rng() divergence.

---

## Harness Updates

No new checks added this cycle. Existing checks performing correctly:
- Check #17 (score plateau) fired: 2.4/5.0 flat for 5+ entries prior to today's drop
- Check #36 (event suppression) fired: events_fired=0 in 2+ seeds for 5/5 consecutive checks
- Check #39 (acute food crisis) fired: fpc<1.5 in seeds 137 (1.09) and 777 (1.36)
- Validate_features.sh: 0 errors, 34 warnings

---

## features.json Updates

- `game_loop`: Added `mining_sparkle_particles_spawn` to `known_issues` as a new flaky test (same mechanism as `construction_dust_particles_spawn` — probabilistic particle spawn via rand::rng()). Recommends `#[ignore]`.
- All `last_verified` dates updated to 2026-06-05.

---

## Harness Review (1st of month — June 2026)

**CLAUDE.md:**
- Rule "cargo test --lib (~7s, ~190 tests)" is stale (actual: 233s, 775 passing). The "~7s" estimate appears to be from before the large test suite extraction. Should be updated.
- Module structure section lists `simulation.rs` (doesn't exist) and lacks `world_state.rs`.

**validate_features.sh:**
- All 52 checks pass on current code. No checks fire false-positives on passing code.
- Check #14 (`construction_dust_particles_spawn`) fires as WARN — still correct, the test is not `#[ignore]`.
- No checks to retire: all still have active warning conditions.

---

## Priority Actions

1. **[NEW BLOCKER] Mark `mining_sparkle_particles_spawn` as `#[ignore]`** — new test failure causing CI noise. Root cause: rand::rng() in particle spawning (tests.rs:2885). Same fix as `construction_dust_particles_spawn`.

2. **[CRITICAL] Fix biome distribution in `terrain_pipeline.rs`** — Forest<1% in all 3 eval seeds means zero woodcutting, zero planks, Bakery permanently blocked, wood_skill pure-decaying. This is the single root cause of: check #41 (wood skill), check #42 (forest near-zero), check #44 (wood stall), check #47 (Bakery blocked), and Terrain Quality score=2. Fix biome scoring before touching any wood/skill/Bakery constants.

3. **[HIGH] Fix event modifier chain** — `tick_config` drought/harvest modifiers (game/mod.rs:2135-2147) never reach `step_water_cycle`. Zero events in all 3 seeds across all 5 recent checks (check #36 persistent). Fix this before investigating event trigger rates or thresholds. This is also why Emergent score dropped to 1 today.

4. **[HIGH] Replace `rand::rng()` with seeded per-entity RNG** — 6 hot-path calls (systems.rs×3, mod.rs×3) cause population, food, building count variance of ±11 within the same session. Makes evaluation metrics unreliable and masks real game quality. See `ecs_core.known_issues` for documented variance data.
