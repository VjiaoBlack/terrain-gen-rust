# Daily Health Check — 2026-07-22

## Test Results
- **Total: 778 passed, 0 failed, 10 ignored** (CLEAN — 194.83s)
- Test count accuracy: within tolerance (818 `#[test]` annotations in src/ vs 700+ threshold)
- CLAUDE.md claims ~190 tests; actual 818 — 4× stale (check #29 fires, ongoing)
- Known-flaky tests still not `#[ignore]`d: `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn` (check #55 fires — 3× CI false-failure risk)

## Design Drift
1. **Pillar 1 (Geography Shapes Everything)** — terrain 98%+ flat across all 3 seeds (steep=0%, cliff=0%, elevation_std=0.09-0.12). game_design.md:14 requires hills, mountain passes, geographic asymmetry between maps. Confirmed failure since 2026-04-30.
2. **Pillar 2 (Emergent Complexity)** — `VillagerMemory` defined in `components.rs` and written in `systems.rs` but never read in `ai.rs` (check #22). Villagers still use global stockpile counts for all decisions (15 occurrences in `ai.rs`). Design doc phase 2 not started.
3. **Events broken** — drought impossible (grain threshold=pop×5, never met at tick 6000), harvest modifier chain dead (`tick_config` built at `game/mod.rs:2135-2148` but never passed to `step_water_cycle`). `events_fired=0` in all 3 seeds today (check #36 fires).
4. **Fog-of-war trivial** — sight_range=22 + initial reveal=15 means exploration_pct=100% in all seeds within ~200 ticks. Fog-of-war not a meaningful game mechanic despite design doc brainstorm (check #21 fires).
5. No implemented features found that contradict design docs — all code aligns with documented pillars; gap is unimplemented features, not drifted ones.

## Architecture
- Files over 2000 lines (documented, below 3000-line validate threshold): `game/mod.rs` (2592), `game/build.rs` (2306), `terrain_pipeline.rs` (2232), `ecs/systems.rs` (2227), `ecs/ai.rs` (2104) — all flagged in `features.json` known_issues
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38 fires)
- `CLAUDE.md` lists `simulation.rs` (split) and omits `world_state.rs` (added in 0D refactor) — check #40 fires
- 11 compiler warnings remain in `game/mod.rs` (unused_mut line 1220, dead tick_config chain lines 2135-2147, unused `n` variable, `DIRECTIONS` constant)
- `validate_features.sh`: **0 errors, 40 warnings**

## Game Quality Evaluation

### Per-Seed Results
| Seed | Population | Food | Food/Cap | Housing Cap | Growth Pot | Water% | Biomes | Survived |
|------|-----------|------|----------|-------------|------------|--------|--------|----------|
| 42   | 4         | 12   | 3.00     | 4           | 0          | 30.7%  | 13     | Yes      |
| 137  | 11        | 12   | 1.09     | 12          | 1          | 17.9%  | 14     | Yes      |
| 777  | 11        | 17   | 1.55     | 20          | 9          | 38.0%  | 14     | Yes      |

Notes: seed 42 has wood=43 (surplus) but housing_cap=4 = pop (auto-build stuck, 66th consecutive run). Seeds 137/777 wood=4 (near-exhausted, Forest<1% means no replenishment). Grain: 42=4, 137=0, 777=20. Planks: 42=0, 137=0, 777=3. Events: 0 all seeds. seek_pct: 42=75.0%, 137=81.8% (above 80% threshold), 777=63.6%.

### Report Card Summaries
- Seed 42: *Population: Fragile (one bad winter away) | Food: Tight (no buffer) | Housing: Overcrowded | Terrain: Diverse | Threats: Low*
- Seed 137: *Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing*
- Seed 777: *Population: Healthy (growing) | Food: Critical (starvation imminent) | Housing: Adequate | Terrain: Diverse | Threats: Growing*

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13-14 biome types present; Desert 32-49% in 2/3 seeds, Forest <1% all seeds, terrain 98%+ flat (steep=0%, cliff=0%). Biome count OK; distribution pathologically skewed. |
| Settlement Viability (30%) | 2 | All 3 survived; avg pop=8.7 (ok-range). Food Critical in 2/3 seeds (fpc=1.09, 1.55). Seed 42 structurally stuck at pop=4. 0 events fired. |
| Simulation Health (20%) | 4 | pipe_water=244-313, wind_moisture=16k-41k, avg_vegetation=0.42-0.44 — all systems active. Good variation across seeds. |
| Emergent Behavior (20%) | 1 | events_fired=0 all 3 seeds; seeds 137/777 converge at pop=11 (Pillar 1 convergence). Buildings differ across seeds (5/8/12) but no emergent events or divergent outcomes. |
| **Weighted Average** | **2.20** | Down from 2.7/5.0 yesterday (RNG-unfavorable run: 0 events vs WolfSurge in 2/3 seeds yesterday) |

### Baseline Check
- **3/3 PASS** — seed 137 (pop=11, water=17.9%, biomes=14), seed 42 (pop=4, water=30.7%, biomes=14), seed 777 (pop=11, water=38.0%, biomes=14)

## Harness Updates
- No new checks added today — the 40 existing warnings from validate_features.sh fully cover identified issues.
- Existing check coverage is comprehensive; adding more checks for the same root issues (biome distribution, RNG non-determinism, event chain broken) would duplicate existing checks #15, #33, #41, #42, #47.

## features.json Updates
- Updated `ecs_core` known_issues with today's stuck-run note (66th consecutive seed 42 pop=4; seeds 137/777 both pop=11, events_fired=0 5th consecutive zero-event run)
- Updated `events` known_issues with today's zero-event note (5th consecutive)
- Updated `last_verified` dates to 2026-07-22

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** — Desert dominant (32-49% in 2/3 seeds), Forest <1% in all seeds. Root cause of: zero woodcutting (wood_skill=0.5488 pure decay), Bakery chain permanently blocked (planks=0), and Pillar 1 terrain-outcome convergence between seeds 137/777. This single fix unblocks the most cascading failures.
2. **Fix simulation non-determinism** — replace `rand::rng()` in `game/build.rs`, `ecs/systems.rs`, `game/mod.rs` with seeded per-entity RNG. Root cause of ±7 population swings between sequential runs of same binary+seed, making rubric scores and health metrics unreliable.
3. **Wire `tick_config` event modifiers to `step_water_cycle`** (`game/mod.rs:2135-2148`) — drought/harvest have zero effect on simulation. Events_fired=0 in all diagnostic runs; event system functionally dead for these two event types.
4. **Mark 3 known-flaky tests as `#[ignore]`** — `construction_dust_particles_spawn`, `build_site_gets_completed_in_game`, `mining_sparkle_particles_spawn` in `src/game/tests.rs`. All 3 fail due to `rand::rng()` non-determinism. Current CI false-failure rate ~3× per run.
