# Daily Health Check — 2026-06-01

## Test Results
- **778 passed, 0 failed, 10 ignored** (cargo test --lib, ~232s)
- Test count in CLAUDE.md stale: claims ~190, actual 818 (check #29 fires, 4× off)
- Known-flaky tests `construction_dust_particles_spawn` and `build_site_gets_completed_in_game` remain not `#[ignore]` (checks #14, #35 fire). CI false-failure risk persists.

## Design Drift

**Pillar 1 — Geography Shapes Everything:**
- Terrain 98.4–98.7% flat all 3 seeds (elevation_std=0.09–0.12, steep=0%, cliff=0%). No strategic terrain.
- Desert=32–49% in 2/3 seeds; Forest=0.1–0.6% in all 3. Wood skill at pure decay (0.5488) in all seeds.
- Roads never form (ROAD_TRAFFIC_THRESHOLD=300.0 unreachable at pop=4–13; check #25).

**Pillar 2 — Emergent Complexity:**
- `VillagerMemory` written in `ecs/systems.rs` but never read in `ecs/ai.rs`. Villagers use global state (check #22).

**Event System:**
- `tick_config` drought/harvest modifiers at `game/mod.rs:2135–2147` never passed to `step_water_cycle` (check #33). Check #36: events_fired=0 in 2+ seeds for 5/5 consecutive checks.
- Drought grain threshold (`grain >= pop*5`) structurally impossible in eval window (check #27).
- **Note:** Report-card run produced food_per_cap=0.7 for seed 137 (vs diagnostics 1.27, same binary/seed) — largest within-session food divergence this month, confirming rand::rng() task-assignment variance.

No new drift beyond the above (all pre-existing, documented issues).

## Architecture
- No non-test source file exceeds 3000 lines ✓
- `src/analytical_erosion.rs` and `src/scripting/mod.rs` absent from `docs/ARCHITECTURE.md` (check #38; ongoing)
- `CLAUDE.md` module structure lists removed `simulation.rs`, omits `world_state.rs` (check #40; ongoing)
- `save_load` at `needs_tests` with 0 tests; `world_state` at `needs_work` with 0 tests (checks #9, #18)
- 11 compiler warnings in `game/mod.rs` (unchanged)
- validate_features.sh: **0 errors, 36 warnings** (1 fewer than yesterday — check #48 Pillar 1 convergence guard not firing this session; seeds 137/777 show pop=11/13, diff=2)

## Game Quality Evaluation

### Per-Seed Results (Diagnostics, game.tick=6000)
| Seed | Population | Food | Food/Cap | Housing Cap | Water% | Biomes | Survived | Events |
|------|-----------|------|----------|-------------|--------|--------|---------|--------|
| 42   | 4         | 12   | 3.00     | 4 (full)    | 30.7%  | 13     | Yes (dies ~6668) | 0 |
| 137  | 11        | 14   | 1.27     | 12 (+1)     | 17.9%  | 14     | Yes      | 0      |
| 777  | 13        | 17   | 1.31     | 16 (+3)     | 38.0%  | 14     | Yes      | 0      |

Notable: seed 777 shows planks=6, grain=28 — closest combined approach to Bakery threshold (needs planks≥8 AND grain>30). Still 2 planks and 3 grain short. Non-reproducible.

### Report Card Summaries
- Seed 42: `Fragile` (pop=4) / `Tight` food (3.0/cap) / `Overcrowded` (cap=4) / `Diverse` terrain / `Low` threat
- Seed 137: `Healthy` (pop=11, growing) / `Critical` food (starvation imminent, 1.27/cap) / `Adequate` housing / `Diverse` terrain / `Growing` threat
- Seed 777: `Healthy` (pop=12 report-run) / `Critical` food (starvation imminent, 1.3/cap) / `Adequate` housing / `Diverse` terrain / `Growing` threat

### Rubric Scores
| Category | Score (1-5) | Notes |
|----------|------------|-------|
| Terrain Quality (30%) | 2 | 13–14 biomes by count, but Desert 32–49% in 2/3 seeds, Forest<1% all seeds, Mountain=0%, slopes 98%+ flat; geography cannot shape settlement |
| Settlement Viability (30%) | 2 | Seed 42 dies ~tick 6668 (39th confirmation); seeds 137/777 survive but food Critical (1.27–1.31/cap); FoodToGrain Granary aggravating food buffer at pop≥11 |
| Simulation Health (20%) | 4 | pipe_water=244–314 (active), wind_moisture=16K–41K (active), avg_vegetation=0.42–0.44, avg_moisture=0.32–0.65; all subsystems functional |
| Emergent Behavior (20%) | 2 | Events=0 all seeds; broken tick_config chain; seed 42 always dies same tick; roads never form; wood_skill=0.5488 (pure decay) all seeds |
| **Weighted Average** | **2.4** | 0.6+0.6+0.8+0.4=2.4; plateau at 2.4 for **8th consecutive** health check (check #17 fires) |

### Baseline Check
All 3 seeds **PASS** (seed 42: pop=4, water=30.7%, biomes=13; seed 137: pop=11, water=17.9%, biomes=14; seed 777: pop=12, water=38.0%, biomes=14).

## Harness Updates

**Check #45 warning text corrected (monthly hygiene):** The prior text said "Bakery also blocked (requires Granary)" — misleading, since a pre-built FoodToGrain Granary is always present from game start (`game/mod.rs:1064`). Updated text clarifies: (1) food→grain IS running via pre-built Granary; (2) actual Bakery blockers are planks≥8 (check #47) and grain>30; (3) what check #45 detects is that auto-build of a SECOND Granary is structurally impossible (food>80 unreachable).

## features.json Updates
- All 25 systems: `last_verified` updated to `2026-06-01`
- `ecs_core` known_issues: Added — seed 137 rabbits recovered to 3 (was 0 on 2026-05-30); seed 777 rabbits=12 (up from 3); rabbit population non-deterministic and seed-dependent.
- `ecs_core` known_issues: Added — report-card food_per_cap=0.7 for seed 137 (vs diagnostics 1.27, same binary/seed, 2026-06-01) — largest within-session food divergence recorded.

## Harness Review (Monthly — June 1st)

1. **CLAUDE.md rules**: No rules are obsolete. Test count and module structure are stale (tracked by checks #29, #40) — not rule problems, just doc debt.
2. **docs/workflow.md**: Workflow matches reality. One gap: health check agent role not documented, but this is a meta-process concern, not a workflow accuracy issue.
3. **Checks that never fire**: Checks #11, #12, #20, #34 are regression guards that always pass — they should stay (they catch accidental reverts). Checks #13, #16 pass but are meaningful positive signals.
4. **Check #45 corrected**: Warning text was misleading about Bakery/Granary blocking relationship (see Harness Updates above). Updated to accurately describe what IS blocked (second Granary auto-build) vs what IS working (pre-built FoodToGrain Granary).
5. **No checks removed**: All 51 checks remain relevant. Score plateau (8 checks at 2.4) indicates the needed fixes are in application code, not harness coverage.

## Priority Actions
1. **Fix biome distribution in `terrain_pipeline.rs`** (`terrain_generation`): Desert 30–49%, Forest <1%, Mountain=0% in all seeds. Unblocks wood skill, Workshop/Bakery chain, road formation, and Pillar 1 terrain differentiation. Single highest-impact fix.
2. **Fix seeded per-entity RNG** (`ecs_core`, `game_loop`): Until seeded, rubric scores remain unreliable noise. Replace `rand::rng()` in `game/build.rs` and `ecs/systems.rs` (3 calls each).
3. **Fix FoodToGrain Granary threshold** (`game_loop`): Raise `food > 15` to `food > max(15, population * 2)` at `src/ecs/systems.rs:979,1391`. Prevents food=0 crashes at pop≥10.
4. **Fix event modifier chain** (`events`): Thread `tick_config.rain_rate` into `step_water_cycle` at `game/mod.rs:2135–2148`. One-function-signature change unlocks drought/harvest simulation effects.
